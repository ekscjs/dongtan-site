# 작업지시서 — 페이지 전환 대기 표시 + /blog 캐시 문제 (2026-07-27)

## 앞선 지시서의 오류 정정 (중요)

`작업지시서-블로그목록-SSR실패-useSearchParams제거-0726.md` **파트 2의 전제가 틀렸다.**

내가 쓴 전제: "`notFound()`가 `generateMetadata`에서 던져지므로 HTML flush 전에 상태코드가 확정된다 → `loading.tsx`를 되살려도 404가 안 깨진다."

실제: **틀렸다.** 로컬에서 격리 검증한 결과 `loading.tsx`가 존재하는 것만으로 라우트가 스트리밍 모드로 커밋되어, `generateMetadata`가 무엇을 던지든 상태코드가 200으로 고정된다. `generateMetadata`가 flush 전에 완료되는 것은 맞지만 **"완료 시점"과 "상태코드 확정 시점"이 같지 않다.**

로컬 CC가 이걸 반증하고 지시서 12번 지침대로 되돌린 판단이 **옳다.** 파트 2는 폐기한다.

**→ `loading.tsx`를 다시 시도하지 말 것.** 404 상태코드가 스켈레톤보다 우선순위가 높다(구글이 없는 페이지를 계속 크롤·색인하는 게 더 큰 손해).

또한 파트 1이 이미 push·배포된 상태였으므로 "한 커밋" 요구를 위해 강제 push하지 않은 판단도 옳다. 배포된 커밋 rebase/force-push는 앞으로도 승인 없이 하지 않는다.

한 가지 남겨둘 것: 파트 2 작업 중 발견한 **"/blog 연구노트 카드가 마지막 스켈레톤 수정 시점(1ab71c9) 이후 추가돼서 옛 스켈레톤에는 그 자리가 없었다"**는 지적은 정확하다. 스켈레톤 자체를 폐기하므로 이번엔 무효지만, 나중에 로딩 UI를 다시 만들면 이 카드 자리를 포함해야 한다.

---

# 파트 A — `/blog` 캐시 문제 (아직 미해결, 우선)

081682a 배포 후에도 **쿼리 없는 `/blog`** 가 옛 HTML을 서빙하고 있었다(코워크가 확인). 구글이 크롤하는 주소가 정확히 이것이므로 이게 안 풀리면 SSR 수정이 크롤러 입장에선 없던 일이 된다.

## A-1. 현재 상태 재확인 (먼저 이것부터)

```bash
echo "--- 쿼리 없음 (구글이 크롤하는 주소) ---"
curl -s 'https://www.bodymiso.com/blog' | grep -o 'href="/blog/[^"]*"' | wc -l
curl -s 'https://www.bodymiso.com/blog' | grep -o '<link rel="canonical"[^>]*>'
curl -s 'https://www.bodymiso.com/blog' | grep -o '<title>[^<]*'

echo "--- 쿼리 있음 (캐시 우회) ---"
curl -s 'https://www.bodymiso.com/blog?x=1' | grep -o 'href="/blog/[^"]*"' | wc -l
```

**둘이 같으면 시간이 지나 이미 해소된 것이다 — A-2 이후는 진행하지 말고 그 사실만 보고할 것.**
다르면 A-2로 간다.

## A-2. 캐시 계층 진단

```bash
curl -sI 'https://www.bodymiso.com/blog' | grep -i 'x-vercel-cache\|x-nextjs\|^age\|cache-control\|x-vercel-id'
curl -sI 'https://www.bodymiso.com/blog?x=1' | grep -i 'x-vercel-cache\|x-nextjs\|^age\|cache-control'
```

두 응답의 헤더 차이를 **그대로 보고**할 것. `x-vercel-cache`가 `HIT`/`STALE`/`MISS` 중 무엇인지, `age` 값이 `revalidate = 60`을 넘는지가 핵심이다.

## A-3. 원인에 맞게 해소

진단 결과에 따라 하나만 고른다. **추측으로 여러 개 동시에 바꾸지 말 것** — 무엇이 효과가 있었는지 구분이 안 된다.

- CDN/Full Route Cache 잔존이면 → Vercel 대시보드에서 캐시 퍼지 또는 재배포 후 A-1 재확인
- `revalidate = 60`이 이 라우트에서 의도대로 안 도는 것이면 → `src/app/blog/page.tsx`의 캐시 전략을 검토해서 **원인과 근거를 먼저 보고**하고, 코드 변경은 승인 후

## A-4. 보고에 포함할 것

- A-1·A-2 출력값 원문
- `/blog`가 매 요청마다 Supabase를 조회하는지, 캐시된 HTML을 서빙하는지 (파트 B의 속도 판단에 필요)

---

# 파트 B — 페이지 전환 대기 표시 (`useLinkStatus`)

## 문제

관장이 모바일에서: "센터 소개에서 상단 '칼럼'을 누르면 센터소개가 잠깐 그대로 있다가 블로그로 넘어간다."

`loading.tsx`가 없으면 Next.js는 새 라우트 준비가 끝날 때까지 이전 화면을 그대로 둔다. 클릭했는데 아무 반응이 없어 보이는 게 이 현상이다.

## 방침

**`loading.tsx` 대신 클라이언트 측 대기 표시를 쓴다.** 서버 렌더·상태코드·메타데이터에 전혀 관여하지 않으므로 404나 SSR을 깨뜨릴 위험이 구조적으로 없다.

Next.js 16.2.7 사용 중이므로 `next/link`의 **`useLinkStatus()`** 를 쓸 수 있다. 공식 문서가 명시한 사용 사례가 정확히 이 상황이다 — *"목적지 라우트가 동적이고 loading.js가 없어서 즉시 전환이 안 되는 경우"*.

제약: **`useLinkStatus()`는 `<Link>`의 자손 컴포넌트 안에서만 동작한다.** 따라서 `<Link>` 밖에 전역 바를 두고 상태를 끌어오는 구조는 안 된다.

## B-1. 신규 컴포넌트 — `src/components/NavPendingBar.tsx`

```tsx
"use client";
import { useLinkStatus } from "next/link";

/** <Link> 자손으로 렌더돼야 동작. pending일 때만 화면 최상단에 진행 바를 띄운다. */
export default function NavPendingBar() {
  const { pending } = useLinkStatus();
  if (!pending) return null;
  return (
    <span
      aria-hidden
      className="fixed top-0 left-0 right-0 h-0.5 z-50 bg-[#7B2D8B] animate-pulse"
    />
  );
}
```

`position: fixed`라서 `<Link>` 안에서 렌더돼도 화면 최상단에 전역 바처럼 보인다. 이게 이 제약을 우회하는 방법이다.

레이아웃 영향 없어야 한다 — `<Link>`의 기존 flex/텍스트 정렬이 이 `<span>` 때문에 밀리지 않는지 확인할 것(밀리면 `absolute` 컨테이너로 감싸지 말고 `NavPendingBar` 쪽만 조정).

## B-2. 적용 위치 — `src/components/Header.tsx`

상단 내비게이션의 **내부 링크에만** 넣는다:

- 센터 소개 `/about`
- 프로그램 `/programs`
- 칼럼 `/blog`
- 몸 상태 체크 `/check`
- 바디 리셋 세션 `/class/breathing`
- 로고 링크 `/`

```tsx
<Link href="/blog" className="...">
  칼럼
  <NavPendingBar />
</Link>
```

**"무료 상담"(카카오 외부링크)에는 넣지 않는다** — 외부 이동이라 pending 개념이 없다.

모바일 메뉴가 별도 마크업이면 거기에도 같이 넣는다(관장이 겪은 게 모바일이다).

## B-3. 범위 밖

- `/blog` 목록 내부의 카테고리·페이지네이션 링크 — 우선 상단 내비만 적용해서 관장 확인 받고, 필요하면 다음에 확장
- 글 카드 링크
- 상단 바 외의 스켈레톤·플레이스홀더

## B-4. 검증 항목

1. `npx tsc --noEmit`, `npm run build` 통과
2. **404 회귀 (가장 중요)** — 클라이언트 컴포넌트 추가가 상태코드에 영향 없어야 한다:
   ```bash
   curl -s -o /dev/null -w "%{http_code}\n" "http://localhost:3000/blog?page=9999"      # 404
   curl -s -o /dev/null -w "%{http_code}\n" "http://localhost:3000/blog/없는슬러그"      # 404
   ```
3. **SSR 회귀** — 파트 1이 깨지지 않았는지:
   ```bash
   curl -s http://localhost:3000/blog | grep -o 'href="/blog/[^"]*"' | wc -l    # 10 이상
   curl -s http://localhost:3000/blog | grep -o '<link rel="canonical"[^>]*>'   # /blog
   ```
   (`grep -c` 쓰지 말 것 — SSR 출력이 한 줄이라 매칭 개수와 무관하게 1이 나온다)
4. `curl -s http://localhost:3000/blog | grep -c 'fixed top-0'` 로 **서버 HTML에 진행 바가 안 들어가 있는지** 확인 → `pending`이 초기엔 false라 0이어야 한다. 1 이상이면 초기 렌더에 바가 보이는 버그
5. 브라우저 확인 — `/about`에서 "칼럼" 클릭 시 **클릭 즉시** 상단 바가 보이고, 새 페이지 도착 시 사라짐
6. **모바일 뷰포트(개발자도구 기기 모드)에서 같은 동작 확인** — 관장이 겪은 환경
7. 다른 페이지 이동(프로그램·센터소개·몸상태체크)에서도 동작, 레이아웃 밀림 없음
8. 카카오 상담 링크에는 바가 안 뜨는지
9. 커밋 후 push. 배포 후 라이브에서 2·3번 재확인

## 주의

`useLinkStatus`는 `prefetch`가 완료된 정적 링크에서는 pending이 거의 안 잡힐 수 있다(즉시 전환되므로 정상). `/blog`처럼 느린 동적 라우트에서 보이는 것이 목적이다. **`/about` 같은 빠른 페이지에서 바가 안 보여도 실패가 아니다** — 5번은 "칼럼(/blog)" 기준으로 판정한다.
