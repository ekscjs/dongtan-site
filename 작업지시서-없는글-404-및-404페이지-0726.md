# 작업 지시서 — 없는 글 404 처리 + 404 페이지 만들기 (d4e3e1d 후속, 2026-07-26)

d4e3e1d 검증 전부 통과했습니다(`?page=999/abc/0/-1/2.5`, `?cat=임상노트&page=99` 모두 404, 정상 페이지 200 유지).
아래 두 건이 마지막입니다.

---

## 1. `/blog/{없는슬러그}`가 200을 반환하는 문제

확인 결과:
```
/blog/this-slug-does-not-exist-zzz  → 200 OK, canonical 1개, title = 홈 기본값
```

원인은 `/blog?page=999` 때와 **완전히 같은 패턴**입니다.
`src/app/blog/[slug]/page.tsx`의 `generateMetadata`가 `if (!post) return {};` (78행)로 조용히 넘어가고,
본문 컴포넌트에서만 `notFound()`를 부르고 있습니다.

이건 구글이 "소프트 404"로 분류하는 전형적인 케이스입니다. 페이지네이션 건보다 더 중요합니다 — 실제 글 URL 영역이고, 서치콘솔에 소프트 404로 잡히면 사이트 전체 크롤 신뢰도에 영향을 줍니다.

**수정:**

- `generateMetadata`에서 `if (!post) return {};` → `if (!post) notFound();`
- 본문 컴포넌트의 `notFound()`는 그대로 둘 것 (양쪽 다 있어야 함)
- **주의:** d4e3e1d에서 `src/app/blog/loading.tsx`를 지워야 404가 먹혔던 것과 같은 함정이 있는지 확인할 것.
  `src/app/blog/[slug]/` 아래에 `loading.tsx`가 있으면 스트리밍이 먼저 200으로 커밋돼서 이번에도 상태코드가 안 바뀝니다.
  있으면 지우지 말고 **먼저 상태코드부터 확인**하고, 원인이 맞으면 그때 판단할 것 (글 상세는 목록과 달리 로딩 UI가 필요할 수 있음 — 필요하면 `loading.tsx` 대신 `Suspense`로 옮기는 방향)

## 2. 404 페이지 만들기 (`src/app/not-found.tsx`)

지금 404가 뜨면 Next.js 기본 화면(흰 배경에 "404 This page could not be found")이 나옵니다.
헤더도 없어서 방문자가 다른 데로 갈 방법이 없고, 브랜드 사이트에 남의 페이지가 껴 있는 꼴입니다.

- `Header` / `Footer` 포함
- 문구: 제목 "페이지를 찾을 수 없습니다", 본문 "주소가 바뀌었거나 삭제된 글일 수 있습니다."
- 버튼 2개: `블로그 글 보기 →` (`/blog`), `홈으로` (`/`)
- 기존 톤 유지 (`#7B2D8B`, `rounded-full`, `max-w-3xl mx-auto`)
- `export const metadata = { title: "페이지를 찾을 수 없습니다 | 내몸에미소", robots: { index: false } }`

---

## 확인

```
curl -s -o /dev/null -w "%{http_code}\n" "https://www.bodymiso.com/blog/this-slug-does-not-exist-zzz"  # 404
curl -s -o /dev/null -w "%{http_code}\n" "https://www.bodymiso.com/blog/pain-gone-not-fully-recovered-function"  # 200
curl -s -o /dev/null -w "%{http_code}\n" "https://www.bodymiso.com/blog?page=999"  # 404 (기존 동작 유지)
curl -s "https://www.bodymiso.com/blog/this-slug-does-not-exist-zzz" | grep -c "페이지를 찾을 수 없습니다"  # 1 이상
```

- [ ] 위 4개 통과
- [ ] 정상 글 상세 페이지의 title·canonical·OG 이미지가 그대로인지 (metadata 건드리므로 깨지기 쉬움)
- [ ] 404 화면에 헤더/푸터 나오고 블로그·홈 버튼이 동작하는지
- [ ] `npm run build` 통과 → commit + push
