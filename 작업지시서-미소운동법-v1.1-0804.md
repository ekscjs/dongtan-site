# 작업지시서 — 미소 운동법 v1.1 반영

작성: 2026-08-04 · 코워크 → 로컬 Claude Code
**전제: 관장 v1.1 판정란 승인 후 착수. 승인 전에는 손대지 말 것.**

원고 원본: `naemiso-brand/동작원리정의서/09_미소운동법-v1.1-원고.md`
대상 파일: `dongtan-site/src/app/method/page.tsx` (현재 759줄)

---

## 이번 작업의 성격

문구 교체가 아니라 **장 구조 변경**입니다. 장이 3개 늘고 번호가 밀립니다.
`SectionHeading n={...}` 값을 전부 다시 매겨야 합니다.

| v1.0 | v1.1 |
|---|---|
| — | **0. 무엇을 만들려고 하는가** ← 신설 |
| 1. 정의 | 1. 정의 (변경 없음) |
| 2. 전제 | 2. 전제 (변경 없음) |
| 3. 용어 | 3. 용어 — **「알아차리기」 추가** |
| 4. 원칙 | 4. 원칙 — **7번 문장 교체** |
| 5. 절차 (5-1~5-3) | 5. 절차 — **5-1 전면 교체, 5-2~5-6으로 확장** |
| 6. 감별 | 6. 감별 — **재정의 + 표 2행 추가** |
| 7. 하지 않는 것 | **7. 어디서 왔는가** ← 신설 |
| 8. 한계 | 8. 하지 않는 것 (구 7 + 2항목 추가) |
| 9. 개정 이력 | 9. 한계 (구 8 + 확장) |
| | **10. 이 방법을 쓰려는 분께** ← 신설 |
| | 11. 개정 이력 (구 9 + v1.1 행) |

---

## 1. 상수·메타 교체

```
9행   TITLE           "미소 운동법 v1.0 —" → "미소 운동법 v1.1 —"
11행  (description)   v1.0 → v1.1
36행  headline        v1.0 → v1.1
173행 "Ver 1.0 · 2026년 7월" → "Ver 1.1 · 2026년 8월"
```

**127~130행 개정 이력 배열** — `current` 플래그를 v1.1로 옮기고 v1.1 행 신설:

```js
{ version: "v1.0", when: "2026.07", desc: "첫 공개", current: false },
{ version: "v1.1", when: "2026.08", desc: "무엇을 만들려고 하는지 · 절차 다섯 단계 · 알아차리기 · 어디서 왔는가", current: true },
{ version: "v1.2", when: "예정", desc: "어디가 아플 때 원인이 주로 어디인지", current: false },
{ version: "v2.0", when: "예정", desc: "40~50대 여성 체형 측정에서 실제로 나온 것", current: false },
```

v1.1 행의 `desc`가 길면 줄바꿈 처리에 주의. **아래 3번 항목 필수 확인.**

---

## 2. 본문 반영

원고 `09_` 파일의 `# 미소 운동법` 이하 ~ CTA 한 줄까지가 페이지 본문 전체입니다.
**원고 문장을 그대로 씁니다. 요약·재작성 금지.**

주의할 것 두 개:

- **원장 발화를 인용블록(`blockquote`)에 넣지 말 것.** 본문 문장으로 올립니다.
  기존 확정 규칙이고, v1.0 톤수정에서 이미 4곳 고쳤습니다.
  단 v1.0에 원래 있던 최상단 헤드라인 2줄과 4장 3번 보충 박스는 그대로 유지합니다.
- **원고의 `<!-- -->` 주석은 페이지에 옮기지 않습니다.** 관장 검토용입니다.

---

## 3. ⚠️ 한글 줄바꿈 — 이번에 특히 확인할 것

`CLAUDE.md` 줄바꿈 규칙 그대로 적용합니다. **이번 원고에 걸릴 자리가 많습니다.**

의미 단위가 갈라지면 안 되는 구절:

```
정렬 → 깨우기 → 알려주기 → 저항 → 알아차리기     ← 절차 5단계 (5-1)
① 코어 → ② 골반 → ③ 상체 → ④ 말단              ← 5-3
하복부 · 엉덩이 속근육 · 등 속근육 · 옆구리 ·
견갑 주변 · 다리를 모으는 근육                    ← 5-4, · 나열은 flex 항목으로 쪼갤 것
자기 범위 안에서 못 버팀 / 자기 범위를 넘겨서 씀    ← 6장 표
```

`word-break: keep-all`만으로는 안 막힙니다. 의미 단위를 `inline-block` span으로 묶으세요.
**단계 화살표는 모바일에서 세로로 떨어지게** 처리하는 게 안전합니다.

---

## 4. SEO — 원본 HTML로 검증

`curl`로 받은 원본 HTML에서 확인합니다. **브라우저 DOM·Playwright로 확인한 건 근거가 안 됩니다.**

```bash
curl -s https://www.bodymiso.com/method | grep -o '<title>[^<]*</title>'
curl -s https://www.bodymiso.com/method | grep -c '알아차리기'
curl -s https://www.bodymiso.com/method | grep -o 'Ver 1.1'
```

- [ ] title에 v1.1
- [ ] "알아차리기"가 원본 HTML에 있음 (JS 렌더 후가 아니라)
- [ ] 0장 목적 본문이 원본 HTML에 있음
- [ ] JSON-LD `headline` v1.1 · `dateModified` 2026-08-04
- [ ] JSON-LD author는 실명(Person) 유지 — 화면에는 브랜드만 노출
- [ ] **`/method` 원본 HTML에 `/class/breathing` 링크가 있는지** (10장 CTA)
- [ ] `/class/breathing` 원본 HTML에 "가르치기 위해 오시는 분"이 있는지

---

## 5. 하지 말 것

- **원고에 없는 문장을 만들지 마세요.** 분량이 비어 보여도 채우지 않습니다
- **다음 5개는 절대 넣지 않습니다** (인터뷰에서 나왔지만 발행 금지로 확정):
  암 관련 발언 · 회원 시술 판단 · 회원 병력 상세(디스크·주사) ·
  감각 저하의 원인 추정 · "인간은 태생적으로 오른쪽 패턴" 주장
  → 목록 원문 = `동작원리정의서/08_원장인터뷰-2회차-추출.md` 「발행 금지」
- **0장의 회원 사례는 이름·병명 없이 그대로 씁니다.** 실명이나 진단명을 보태지 마세요

---

## 5-2. 바디 리셋 세션 페이지도 같이 고칩니다 — 2026-08-04 추가

대상 파일: `src/app/class/breathing/page.tsx`

### 왜 고치는가

`/method` 10장에서 이 페이지로 링크를 걸었습니다. 그래서 오는 사람이 두 부류로 갈립니다.
지금 `FOR_YOU` 배열은 4개가 나란히 있어서, 트레이너는 통증 환자용으로 읽고
통증 있는 분은 선수용으로 읽을 수 있습니다. **넷 다 적혀 있는데 아무도 자기 얘기로 안 읽는 상태입니다.**

### 고칠 것 — `FOR_YOU`(28~33행)를 두 갈래로 묶기

**과정을 나누지 않습니다. 4주 구성도 가격도 페이지도 그대로 하나입니다.**
앞부분에서 "왜 왔는지"만 갈라 보여줍니다.

```js
const FOR_YOU_GROUPS = [
  {
    label: "내 몸을 고치려고 오시는 분",
    items: [
      "병원에 가도 원인을 알 수 없는 통증에 시달리는 분",
      "이것저것 다 해봐도 늘 그대로이신 분",
    ],
  },
  {
    label: "가르치기 위해 오시는 분",
    items: [
      "현업에서 운동을 가르치는 트레이너·강사",
      "프로 선수, 혹은 운동선수를 준비 중인 분",
    ],
  },
];
```

렌더링은 기존 카드 스타일(`flex items-start gap-3 bg-[#FAF5FB] rounded-xl`)을 유지하고,
그룹 라벨만 위에 얹습니다. **새 색·새 컴포넌트 만들지 마세요.**

그룹 아래에 한 줄 덧붙입니다.

> 과정은 같습니다. 4주 동안 같은 순서를 밟고, 도착지가 다릅니다.

### 같이 볼 것

- `metadata.description`(10행) 순서를 바꿉니다: 현재 "프로 선수, 현업 트레이너, 원인 불명 통증"
  → **"원인 불명 통증, 현업 트레이너, 프로 선수"**. 모수가 큰 쪽을 앞으로
- **가격은 계속 "문의"로 둡니다**(67행). 숫자 넣지 마세요 (2026-07-20 확정)
- 4주 구성(`FLOW`)·시간·장소는 손대지 않습니다

### 하지 않을 것

- **URL은 그대로 `/class/breathing`으로 둡니다.** 상품명과 안 맞지만 이번 작업에서는 건드리지 않습니다.
  리다이렉트까지 묶이면 작업이 커집니다. 별도 지시서로 나중에
- 트레이너 전용 페이지를 새로 만들지 않습니다. **메뉴를 늘리지 않는 게 이번 결정입니다**

---

## 6. 같이 고칠 문서 (코드 아님, 원고 정합성)

`naemiso-brand/동작원리정의서/02_RMS-설계도-초안.md`가 아직
`측정 → 감별 → 토대 → 적재` 구조로 되어 있습니다. v1.1과 어긋납니다.
**이건 코워크가 다음 세션에 정리합니다. 로컬에서 손대지 마세요.**

---

## 7. 끝나고

```powershell
cd C:\Users\송경천\Documents\Claude\Projects\naemiso-brand\dongtan-site
npx tsc --noEmit
git add -A
git commit -m "feat: 미소 운동법 v1.1 — 목적·절차 5단계·알아차리기·계보 추가"
git push
```

배포 후 4번 검증을 실행하고, 실패 항목이 있으면 그것만 보고해주세요.
