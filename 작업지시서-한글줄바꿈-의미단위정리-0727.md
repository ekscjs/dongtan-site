# 작업지시서 — 한글 줄바꿈 의미 단위 정리 (2026-07-27)

## 배경

관장이 모바일에서 확인: 한글 문장이 아무 데서나 끊긴다. 새 화면을 만들 때마다 반복되는 문제라 **규칙을 `dongtan-site/CLAUDE.md`에 새로 박아뒀다. 이번 작업 전에 그 파일부터 읽을 것.** 앞으로 모든 작업에 자동 적용된다.

`globals.css`의 `word-break: keep-all`은 어절 내부만 막을 뿐, 어절 사이 공백에서 끊기는 위치는 통제하지 못한다는 게 원인이다.

## 확인된 사례 2건 (반드시 수정)

### A. `src/app/class/breathing/page.tsx` 189행

현재 모바일 렌더: `…프로그램과 진행` / `방식을 안내합니다` — "진행 방식"이 갈라짐

```tsx
// 현재
<p className="text-sm md:text-base lg:text-lg text-gray-500 text-center mb-6">
  문의 남기시면 원장이 직접 연락드려 프로그램과 진행 방식을 안내합니다
</p>

// 수정
<p className="text-sm md:text-base lg:text-lg text-gray-500 text-center mb-6 text-pretty">
  <span className="inline-block">문의 남기시면 원장이 직접 연락드려</span>{" "}
  <span className="inline-block">프로그램과 진행 방식을 안내합니다</span>
</p>
```

### B. `src/app/research-notes/4050-women/page.tsx` 143행

현재 모바일 렌더: `…2026-07-20 · 다음` / `업데이트 예정 2026년 12월` — "다음 업데이트"가 갈라짐

```tsx
// 현재
<div className="border-t border-gray-100 pt-6 flex items-center gap-2 text-sm text-gray-400">
  <span className="w-2 h-2 rounded-full bg-green-500 animate-pulse shrink-0" />
  표본 {report4050.sampleSize}명 · 마지막 업데이트 {report4050.lastUpdated} · 다음 업데이트 예정 {report4050.nextUpdate}
</div>

// 수정
<div className="border-t border-gray-100 pt-6 flex items-start gap-2 text-sm text-gray-400">
  <span className="w-2 h-2 rounded-full bg-green-500 animate-pulse shrink-0 mt-1.5" />
  <p className="flex flex-wrap items-center gap-x-2 gap-y-1">
    <span className="whitespace-nowrap">표본 {report4050.sampleSize}명</span>
    <span aria-hidden>·</span>
    <span className="whitespace-nowrap">마지막 업데이트 {report4050.lastUpdated}</span>
    <span aria-hidden>·</span>
    <span className="whitespace-nowrap">다음 업데이트 예정 {report4050.nextUpdate}</span>
  </p>
</div>
```

초록 점이 여러 줄일 때 첫 줄에 맞춰 정렬되도록 `items-start` + `mt-1.5`로 바꾼 것에 유의.

### B-2. 같은 패턴 1곳 더

`src/app/research-notes/page.tsx` 53행에도 같은 `표본 N명 · 마지막 업데이트 …` 문구가 있다. 짧아서 지금은 안 갈라질 수 있지만 **동일 방식으로 통일**할 것.

## C. 전수 점검 (범위를 여기까지로 한정)

아래 페이지의 **문장형 서브카피와 `·` 나열 메타 라인만** 훑는다. 본문 블로그 글(DB에서 오는 콘텐츠)은 건드리지 않는다.

- `src/app/page.tsx` (홈)
- `src/app/about/page.tsx`
- `src/app/programs/page.tsx`
- `src/app/class/breathing/page.tsx`
- `src/app/check/page.tsx`
- `src/app/research-notes/page.tsx`, `src/app/research-notes/4050-women/page.tsx`
- `src/components/Footer.tsx`
- `src/app/blog/BlogPageClient.tsx` (목록 페이지의 안내 문구·설명문만)

기준:
- 두 어절 이상이 한 덩어리로 읽혀야 하는 구가 줄 끝에서 갈라질 수 있으면 `inline-block`으로 묶는다
- `·`이나 `|`로 항목을 나열하는 줄은 flex 항목으로 쪼갠다
- 제목류에 `text-balance`, 긴 단락에 `text-pretty`를 얹는다 (보조)

**애매하면 바꾸지 말고 보고에 목록으로 올릴 것.** 과잉 분해해서 문장을 조각내면 더 나빠진다.

## 하지 말 것

- `<br>` 삽입으로 해결 (화면폭마다 달라져 데스크톱에서 어색해짐)
- 글자 크기·색·여백 등 줄바꿈과 무관한 디자인 변경
- DB에서 오는 블로그 본문 텍스트 가공

## 검증

1. `npx tsc --noEmit`, `npm run build` 통과
2. **SSR·404 회귀 없음** (마크업만 바꿨어도 확인)
   ```bash
   curl -s -o /dev/null -w "%{http_code}\n" "http://localhost:3000/blog?page=9999"   # 404
   curl -s http://localhost:3000/blog | grep -o 'href="/blog/[^"]*"' | wc -l         # 10 이상
   ```
3. **모바일 폭 375px에서 눈으로 확인** — A·B 두 곳이 의도한 자리에서 끊기는지
   - A: `문의 남기시면 원장이 직접 연락드려` / `프로그램과 진행 방식을 안내합니다`
   - B: `표본 10명 · 마지막 업데이트 2026-07-20` / `· 다음 업데이트 예정 2026년 12월`
4. 데스크톱(1280px)에서 한 줄로 잘 들어가고 이상한 여백이 생기지 않는지
5. 커밋 후 push

## 보고에 포함할 것

- C에서 실제로 고친 곳 목록 (파일:행)
- C에서 애매해서 안 건드린 곳 목록
- 3번 항목 통과 여부
