# [내몸에미소 센터] 버그리포트 — ✅ **종결: 사이트 버그 아님 (오진)**

작성: 2026-07-27 / 종결: 2026-07-27
**코드 수정 불필요. 이 문서는 기록용으로만 남긴다.**

---

## 결론

**사이트는 정상이다.** 로컬 Claude Code가 Playwright 프로덕션 빌드에서 5/5 통과시킨 게 맞았다.

시크릿 창(확장 프로그램 비활성) 검증 결과:

- `www.bodymiso.com/check/pain` → "무릎" 버튼 클릭 → **보라색으로 선택됨**
- "무릎 체크리스트 시작 →" 버튼 정상 노출

→ `?area=` 딥링크 포함, 통증지도 기능 전부 정상 동작.

---

## 오진 경위 (같은 실수 반복 방지용)

코워크가 **관장님 Chrome**으로 라이브 검증을 하다가
"버튼이 안 눌린다 → `?area=` 파싱 버그"로 잘못 보고했다.

실제 원인은 **관장님 Chrome에 설치된 확장 프로그램**이었다.

- 확장 ID: `cadiboklkpojfamcoggejbbdjcoiljjk`
- `inpage.js` / `content/index.js` 를 페이지에 주입
- 이 주입이 React 하이드레이션을 깨뜨림
- 결과: **화면은 멀쩡한데 아무것도 안 눌리는 상태**

공개 페이지(`/check`, `/check/pain`)만 영향받았고 `/admin/*` 은 정상 동작했다.
일반 방문자에게는 영향 없다.

### 코워크가 잘못 해석한 근거들

| 관측 | 실제 의미 |
|---|---|
| 버튼 클릭 무반응 | 확장 때문. 사이트 문제 아님 |
| `main.innerText` 길이 0 | 하이드레이션 실패의 부수 증상 |
| 청크에 `entry_area`·`checklist` 존재 | 로컬 CC 지적대로 약한 신호였음 |
| `/api/track` 미호출 | Tracker가 admin 방문자를 의도적으로 제외하는 정상 동작 |

---

## 정해진 규칙

**코워크는 라이브 사이트에서 클릭·상태변화·effect 실행을 검증하지 않는다.**

- 신뢰 가능: 문구, 링크 `href`, 메타태그, SSR 원본 HTML 등 **읽기 검증**
- 신뢰 불가: 클릭, 화면 전환, effect 실행 등 **상호작용 검증**
  → 로컬 Claude Code의 Playwright 프로덕션 빌드 테스트로 넘긴다

---

## 두 번째 오진 — "파일 손상"도 사실이 아니었다 (2026-07-27 종결)

코워크가 `layout.tsx`의 한글이 `동???`로 깨졌다고 보고했으나 **이것도 오진**이었다.
조치할 것 없다.

| 확인 | 결과 |
|---|---|
| Windows git status | **clean** (커밋 안 된 변경 없음) |
| `grep -c "동탄" src/app/layout.tsx` | **15건** 정상 |
| `cat -A` 바이트 검사 | UTF-8 정상 (`M-kM-^OM-^Y M-mM-^CM-^D` = 동탄) |
| 라이브 JSON-LD / meta description | **"동탄" 온전, 깨진 문자 0** |

**원인:** 코워크 샌드박스는 마운트를 통해 레포를 보는데, 줄바꿈(CRLF↔LF) 변환 때문에
샌드박스 git이 **모든 줄을 변경된 것으로 인식**한다. diff 출력의 한글 깨짐은 렌더링 문제였다.

**정해진 규칙:** 코워크는 이 레포들에서 `git status` / `git diff` 결과를 근거로
"커밋 안 된 변경"이나 "파일 손상"을 보고하지 않는다. 신뢰할 수 없다.
손상 의심 시엔 diff가 아니라 `grep -c` · 바이트 검사 · **라이브 사이트 실제 출력**으로 확인한다.

(부수 문제: 샌드박스 git 명령이 `.git/index.lock` 쓰기를 시도하다 실패한다.
동시 쓰기 손상 위험이 있으므로 이 레포에서 git 명령 자체를 최소화한다.)

---

## 최종 상태

**조치할 것 없음.** 사이트 정상, 파일 정상, 커밋 상태 정상.
이 문서는 같은 오진을 반복하지 않기 위한 기록으로만 남긴다.
