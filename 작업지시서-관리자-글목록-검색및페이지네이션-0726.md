# 작업 지시서 — 관리자 글목록: 검색 + 상태필터 + 숫자 페이지네이션 (2026-07-26)

**대상 파일 2개:**
1. `src/app/admin/page.tsx` — 검색 + 상태필터 + 숫자 페이지네이션 (아래 1·2번)
2. `src/app/blog/page.tsx` — 숫자 페이지네이션 + 크롤 가능한 링크 (아래 3번)

---

## 배경

- 관리자 글목록은 `posts.slice(0, visibleCount)` + "더보기 (N개 남음)" 구조 (line 592, 662~671).
  글이 쌓이면 이전 글을 찾으려고 더보기를 계속 눌러야 함. 관리자는 "훑는" 화면이 아니라 "찾는" 화면이므로 검색 + 번호 이동이 맞음.
- 2026-07-10 스냅샷 31건 → 하루 1개 페이스로 2026-07-26 현재 약 40건. 100건은 9월 말~10월 초 도달 예정.

---

## 1. 검색 + 상태필터 (우선순위 1 — 이게 페이지 번호보다 효과 큼)

테이블 컨테이너(`<div className="bg-white rounded-2xl shadow overflow-x-auto">`, line 572) **바로 위**에 툴바 한 줄 추가.

```tsx
const [query, setQuery] = useState("");
const [statusFilter, setStatusFilter] = useState<"all" | "published" | "scheduled" | "draft">("all");
const [page, setPage] = useState(1);
const PER_PAGE = 20;
```

- 검색 입력창: 제목 + slug + tag 대상, 소문자 변환 후 `includes` 비교. 디바운스 불필요(클라이언트 배열 필터라 즉시 반영해도 됨).
- 상태 드롭다운: 전체 / 공개 / 예약됨 / 비공개
  - 판정 로직은 line 609~611 및 line 487~489에 이미 있는 것과 **동일한 기준**을 쓸 것 (별도로 새로 짜지 말고 헬퍼 함수 하나로 추출해서 대시보드 카드 집계와 공유)
- 필터링 결과를 `filtered` 변수로 만들고, 아래 페이지네이션은 `filtered` 기준으로 계산
- `query` 또는 `statusFilter`가 바뀌면 `setPage(1)`로 리셋 (useEffect)
- 검색 결과 0건일 때: 기존 "글이 없습니다" 대신 `"'{query}' 검색 결과가 없습니다"` + 필터 초기화 버튼

**상단 대시보드 카드(공개/예약/비공개 개수, line 487~489)는 필터와 무관하게 항상 전체 `posts` 기준 유지할 것.**

## 2. 숫자 페이지네이션

- `visibleCount` state와 "더보기" 버튼(line 56, 662~671) 제거
- 렌더링: `filtered.slice((page - 1) * PER_PAGE, page * PER_PAGE)`
- 테이블 하단 페이지네이션 UI:
  - `[이전] 1 2 3 4 5 [다음]` 형태, 현재 페이지는 `bg-[#7B2D8B] text-white`, 나머지는 `text-gray-500 hover:bg-gray-100`
  - 총 페이지 7개 초과 시 축약: `1 ... 4 5 6 ... 12` (현재 페이지 앞뒤 1개 + 첫/끝 페이지 항상 노출)
  - 페이지 1개뿐이면 페이지네이션 영역 자체를 숨김
  - 좌측에 `전체 N개 중 X–Y` 표시
- 페이지 이동 시 테이블 상단으로 스크롤 (`window.scrollTo({ top: 0, behavior: "smooth" })`)

## 3. 공개 블로그 `/blog` — 숫자 페이지네이션 + 크롤 가능한 링크

> 이 3번 섹션만 빼고 싶으면 "3번은 빼고 해줘"라고 지시받은 경우에만 생략할 것. 기본은 포함.

**왜 지금 하는가 (SEO):**
`src/app/blog/page.tsx`는 `"use client"` + `useEffect`에서 `/api/posts` fetch → **초기 HTML에 글 링크가 0개**.
"더보기"도 클라이언트 state(`visible`, line 35·160~173)라 크롤러가 누를 수 없음 → **/blog에서 내부링크가 걸리는 글은 최신 10개뿐**.
나머지 30개는 `sitemap.ts`로 발견만 되고 내부링크가 없어서 크롤 우선순위가 낮음(서치콘솔 "발견됨-미크롤링"과 직결 가능).
`?count=N` 파라미터(line 32·166)는 `router.replace`로만 쓰여서 크롤러가 발견 못 함.

**수정 내용:**

- `INITIAL_COUNT`/`LOAD_MORE_COUNT`/`visible` state 및 "더보기" 버튼(line 160~173) 제거
- 페이지당 10개, 현재 페이지는 `?page=N` 쿼리로 관리 (`useSearchParams`로 읽기, 기존 `count` 파라미터는 제거)
- **페이지 번호는 `<button>`이 아니라 `<Link href={/blog?page=N}>`으로 렌더링할 것.** 크롤러가 따라갈 수 있는 실제 `<a href>`여야 이 작업의 목적이 달성됨. (`prefetch` 기본값 유지)
- 카테고리 필터(임상노트 / 몸 이야기) 전환 시 `page=1`로 리셋. 카테고리도 URL 파라미터로 옮겨 `?cat=임상노트&page=2` 형태로 만들 것 — 그래야 카테고리별 목록도 크롤 가능해짐
- 축약 규칙·`전체 N개 중 X–Y` 표시는 관리자와 동일
- 기존 스크롤 복원 로직(`SCROLL_KEY`, line 48~68)은 **그대로 유지** — 글 읽고 뒤로가기 했을 때 위치 복원되는 동작이 깨지면 안 됨. 페이지 이동 시에만 상단으로 스크롤
- `src/app/sitemap.ts`에 `/blog?page=N` 목록 페이지들도 추가 (총 페이지 수만큼)

**중복 콘텐츠 방지:** 2페이지 이상에는 `<link rel="canonical">`을 해당 페이지 자기 자신(`/blog?page=2`)으로 지정. `/blog`로 몰아주지 말 것 (구글 권장 방식).

## 4. 스타일

기존 톤 그대로. 메인 컬러 `#7B2D8B`, 라운드는 `rounded-full`(버튼) / `rounded-2xl`(카드), 폰트 크기 `text-sm` 이하(관리자) / `text-sm md:text-base lg:text-lg`(공개 블로그) 유지.

---

## 완료 후 확인

**관리자:**
- [ ] 검색어 입력 시 즉시 필터링되고 페이지가 1로 리셋되는지
- [ ] 상태 필터 + 검색어 동시 적용 시 정상 동작하는지
- [ ] 상단 대시보드 카드 숫자가 필터와 무관하게 전체 기준 그대로인지
- [ ] 페이지 번호 클릭 시 해당 구간 글이 나오는지, 마지막 페이지 잔여 개수 처리 정상인지

**공개 블로그:**
- [ ] 페이지 번호가 실제 `<a href="/blog?page=2">`로 렌더링되는지 (개발자도구 Elements에서 확인)
- [ ] 글 상세 → 뒤로가기 시 원래 스크롤 위치로 복원되는지 (기존 동작 유지 확인, 이거 잘 깨짐)
- [ ] 카테고리 필터 + 페이지 조합이 URL에 반영되고 새로고침해도 유지되는지
- [ ] 2페이지에서 canonical이 자기 자신으로 잡히는지
- [ ] sitemap.xml에 목록 페이지들이 들어갔는지 (`/sitemap.xml` 직접 열어서 확인)

**공통:**
- [ ] `npm run build` 통과
- [ ] `cd C:\Users\송경천\Documents\Claude\Projects\naemiso-brand\dongtan-site` → `git add -A` → `git commit -m "feat: 관리자 글목록 검색+상태필터+페이지네이션"` → `git push`
