# 작업지시서 — /check 인라인 질문 입력창 추가

작성일: 2026-08-01
대상: dongtan-site (www.bodymiso.com)
작업 주체: 로컬 Claude Code

---

## 배경 (왜 하는가)

2026-08-01 2주 리뷰 결과:

| 이벤트 | 30일 수치 |
|---|---|
| ai_concierge_click (미소AI 카드 클릭) | **27** |
| ai_ask_submit (질문 제출) | **2** |
| ai_ask_click_post | 1 |
| ai_ask_click_tool | 0 |

카드 클릭은 27건인데 실제 질문은 2건.
**"카드를 눌렀더니 또 페이지가 바뀐다"는 한 단계에서 이탈하는 것으로 판단.**

수요는 이미 27건으로 증명됐으므로, 신기능을 만드는 게 아니라 **기존 경로의 단계를 하나 줄이는 작업**이다.

---

## 작업 내용

### 1. `src/app/check/page.tsx`

페이지 최상단(카드 3개 위)에 인라인 질문 입력창을 추가한다.

- 라벨/플레이스홀더: **"어디가 불편하세요?"**
- 입력 후 엔터 또는 버튼 클릭 → 입력값을 들고 `/ask` 로 이동
- 전달 방식: 쿼리스트링 (`/ask?q=${encodeURIComponent(값)}`)
- 빈 값이면 이동하지 않음
- 기존 카드 3개(체형진단 · 통증지도 · 미소AI에게 물어보기)는 **아래에 그대로 유지** (삭제·이동 금지)

### 2. `src/app/ask/page.tsx`

- `q` 쿼리 파라미터가 있으면 입력창에 채우고 **자동으로 제출 처리**
- 자동 제출은 최초 1회만 (리렌더 시 중복 호출 방지)
- `q` 없으면 기존 동작 그대로

### 3. 이벤트 추적

새 경로의 효과를 따로 봐야 하므로 **기존 이벤트와 분리**한다.

- 인라인 입력창 제출 시: `check_inline_ask_submit`
- `/ask` 도착 후 실제 응답 생성 시점의 `ai_ask_submit` 은 기존대로 유지
  (즉 인라인 경로는 두 이벤트가 모두 찍힘 → 인라인 유입분을 분리 집계 가능)

### 4. admin/analytics 반영

`src/app/admin/analytics/page.tsx` 의 "미소AI 사용 현황" 카드 섹션에
`check_inline_ask_submit` 카운트 1칸 추가.

---

## 제약

- 새 페이지·새 라우트 만들지 말 것. 기존 2개 파일 + admin 1개만 수정.
- `/ask` 의 API 로직(`src/app/api/ask/route.ts`)은 건드리지 말 것. 레이트리밋(IP당 일 15회, 전체 일 300회) 유지.
- 한글 줄바꿈 규칙 적용: 의미 단위를 `inline-block` span 으로 묶을 것.
  ("어디가 / 불편하세요?" 처럼 구가 갈라지면 안 됨)

---

## 검증

1. 로컬 dev 서버에서 확인 (라이브 반복 접속 금지 — 애널리틱스 오염)
2. 입력창에 "무릎이 시큰거려요" 입력 → 엔터 → `/ask?q=...` 로 이동 후 자동 답변 확인
3. 빈 값 엔터 시 이동 안 하는지 확인
4. 기존 카드 3개 정상 노출·클릭 확인
5. 타입체크 통과 확인
6. `curl` 로 `/check` 원본 HTML 확인 (JS 렌더 후 DOM 아님)

---

## 판단 기준 (다음 리뷰 = 2026-08-15)

- `check_inline_ask_submit` 이 2주간 **5건 이상**이면 → 미소AI 홈 히어로 승격 검토 진입
- 여전히 1~2건이면 → 수요 자체를 재검토 (승격 보류 연장)
