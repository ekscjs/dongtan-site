# 작업지시서 — 관리자 인증 쿠키를 서명 토큰으로 전환 (보안, 2026-07-26)

> **우선순위: 최상.** 주간 리포트 메일 작업보다 먼저 처리한다. 배포까지 완료할 것.

## 문제 (확인된 취약점)

관리자 인증이 `admin_auth` 쿠키의 **값이 문자열 `"1"` 인지만** 검사한다. 서명·암호화·서버측 세션 대조가 전혀 없다.

```ts
if (cookieStore.get("admin_auth")?.value !== "1") { /* 401 */ }
```

`httpOnly`는 **브라우저 JS의 읽기**만 막는다. 위조 방지 기능이 아니다. 요청에 `admin_auth=1` 쿠키를 실어 보내면 비밀번호 없이 관리자 API 전체가 열린다.

노출되는 것:

| 경로 | 영향 |
|---|---|
| `GET /api/admin/leads` | **상담 신청자 전원의 이름·전화번호·메모 (`select("*")`) — 개인정보** |
| `DELETE /api/admin/leads` | 신청 데이터 삭제 |
| `/api/admin/posts`, `/api/admin/posts/[id]` | 블로그 글 작성·수정·삭제 |
| `/api/admin/upload`, `/api/admin/generate-alt` | 파일 업로드, Claude API 호출(과금) |
| `/api/admin/analytics`, `/ga4`, `/search-console`, `/events`, `/marketing` | 전체 통계 열람 |

**계정 탈취는 불가**(비밀번호 변경이 `currentPassword`를 요구함 — 이 부분은 올바르게 구현돼 있어 그대로 둔다). 로그인 시도 제한·bcrypt 해싱도 정상이므로 건드리지 않는다. **고칠 것은 "발급된 세션을 증명하는 방법" 하나뿐이다.**

## 방침

쿠키 값을 `"1"` 대신 **서버 비밀키로 HMAC 서명한 토큰**으로 교체한다.

- **DB 마이그레이션 없음** — 새 테이블 만들면 Supabase SQL 에디터를 브라우저로 조작해야 해서 비용이 크다. 관리자가 1명이라 서버측 세션 저장소의 이득(개별 강제 로그아웃)이 작다. 비밀키를 교체하면 전체 세션이 한 번에 무효화되므로 실질적으로 충분하다.
- 쿠키 이름을 `admin_auth` → **`admin_session`** 으로 바꾼다. 이미 어딘가에 남아 있는 `admin_auth=1` 값이 즉시 무의미해지고, 검사 로직 누락 지점이 있으면 401로 바로 드러난다(조용히 통과되는 것보다 낫다).

---

## 1. 신규 파일 — `src/lib/adminAuth.ts`

```ts
import { cookies } from "next/headers";
import { createHmac, timingSafeEqual, randomBytes } from "crypto";

const COOKIE_NAME = "admin_session";
const MAX_AGE_SEC = 60 * 60 * 24 * 30; // 30일

function secret(): string {
  const s = process.env.ADMIN_SESSION_SECRET;
  if (!s || s.length < 32) {
    throw new Error("ADMIN_SESSION_SECRET 환경변수가 없거나 32자 미만입니다");
  }
  return s;
}

function sign(payload: string): string {
  return createHmac("sha256", secret()).update(payload).digest("base64url");
}

/** 로그인 성공 시 쿠키에 넣을 토큰 생성 — `<만료초>.<난수>.<서명>` */
export function createSessionToken(): string {
  const exp = Math.floor(Date.now() / 1000) + MAX_AGE_SEC;
  const nonce = randomBytes(16).toString("base64url");
  const payload = `${exp}.${nonce}`;
  return `${payload}.${sign(payload)}`;
}

/** 토큰 검증 — 서명 불일치·형식 오류·만료 전부 false */
export function verifySessionToken(token: string | undefined): boolean {
  if (!token) return false;
  const parts = token.split(".");
  if (parts.length !== 3) return false;
  const [expStr, nonce, sig] = parts;

  const expected = sign(`${expStr}.${nonce}`);
  const a = Buffer.from(sig);
  const b = Buffer.from(expected);
  if (a.length !== b.length) return false;      // timingSafeEqual은 길이 다르면 throw
  if (!timingSafeEqual(a, b)) return false;     // 타이밍 공격 방지 — === 쓰지 말 것

  const exp = Number(expStr);
  if (!Number.isFinite(exp)) return false;
  return exp > Math.floor(Date.now() / 1000);
}

/** 라우트에서 쓰는 인증 확인 — 환경변수 누락 시에도 절대 통과시키지 않는다 */
export async function isAdmin(): Promise<boolean> {
  try {
    const cookieStore = await cookies();
    return verifySessionToken(cookieStore.get(COOKIE_NAME)?.value);
  } catch {
    return false;   // secret() throw 포함 — fail-closed
  }
}

export const ADMIN_COOKIE = COOKIE_NAME;
export const ADMIN_COOKIE_MAX_AGE = MAX_AGE_SEC;
```

**중요 — `fail-closed`**: 환경변수가 빠져 있으면 예외를 삼키고 `false`를 반환해야 한다. 실수로 `true`가 되면 사이트 전체가 열린다.

---

## 2. 로그인 라우트 — `src/app/api/admin/login/route.ts`

비밀번호 검증부(bcrypt·시도 제한·env 폴백·자동 시드)는 **전혀 건드리지 않는다.** 마지막 쿠키 설정 부분만 교체:

```ts
// 변경 전
cookieStore.set("admin_auth", "1", { httpOnly: true, ... });

// 변경 후
import { createSessionToken, ADMIN_COOKIE, ADMIN_COOKIE_MAX_AGE } from "@/lib/adminAuth";

cookieStore.set(ADMIN_COOKIE, createSessionToken(), {
  httpOnly: true,
  secure: process.env.NODE_ENV === "production",
  sameSite: "lax",
  maxAge: ADMIN_COOKIE_MAX_AGE,
  path: "/",
});
```

`admin_flag` 쿠키(httpOnly: false)는 **그대로 유지**한다 — 트래킹 제외용 표시일 뿐 권한과 무관하다(`src/components/Tracker.tsx`에서 사용 중).

## 3. 로그아웃 라우트 — `src/app/api/admin/logout/route.ts`

```ts
cookieStore.delete(ADMIN_COOKIE);
cookieStore.delete("admin_auth");   // 구 쿠키 잔재 정리 — 반드시 같이 삭제
cookieStore.delete("admin_flag");
```

## 4. 검사 지점 13곳 전부 교체

아래 파일들의 `cookieStore.get("admin_auth")?.value === "1"` (또는 `!== "1"`) 를 `await isAdmin()` 으로 교체한다. **`admin_auth` 문자열이 코드에 하나도 남지 않아야 한다**(3번의 구 쿠키 삭제 한 줄만 예외).

```
src/app/api/admin/analytics/route.ts
src/app/api/admin/events/route.ts
src/app/api/admin/ga4/route.ts
src/app/api/admin/generate-alt/route.ts
src/app/api/admin/leads/route.ts
src/app/api/admin/marketing/route.ts
src/app/api/admin/password/route.ts
src/app/api/admin/posts/route.ts
src/app/api/admin/posts/[id]/route.ts
src/app/api/admin/search-console/route.ts
src/app/api/admin/upload/route.ts
```

각 파일에 있던 로컬 `checkAuth()` 헬퍼는 삭제하고 `isAdmin()`을 직접 쓴다(중복 정의 제거).

`password/route.ts` 는 `isAuthed` 변수만 `await isAdmin()` 으로 바꾸고, **`currentPassword` 검증 로직은 절대 제거하지 말 것**(이게 계정 탈취를 막고 있는 유일한 장치).

## 5. 환경변수 — `.env.local` + Vercel 둘 다

```
ADMIN_SESSION_SECRET=<64자 랜덤 hex>
```

생성: `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`

- Vercel 대시보드 → Settings → Environment Variables → Production/Preview/Development 전부 체크
- **Vercel에 넣고 재배포해야 적용된다.** 안 넣으면 `isAdmin()`이 항상 false → 관리자 로그인이 아예 안 되는 상태가 된다(사이트 공개 페이지는 정상)

---

## 6. 범위 밖 (하지 말 것)

- **`middleware.ts` 추가하지 않는다.** 미들웨어는 Edge 런타임에서 돌아 `node:crypto`의 `createHmac`을 못 쓴다(Web Crypto로 다시 써야 함). `/admin/*` 페이지는 껍데기만 렌더되고 데이터는 전부 API에서 401로 막히므로 이번 범위에서 제외.
- 비밀번호 정책·시도 제한·bcrypt 라운드 조정
- `leads` 의 `select("*")` 컬럼 축소
- 신규 테이블 생성

---

## 7. 검증 항목 (전부 통과해야 완료)

1. `npx tsc --noEmit`, `npm run build` 통과
2. `grep -rn "admin_auth" src/` → **logout 라우트의 삭제 한 줄만** 남는지 확인
3. **로컬 dev에서 인증 우회가 막혔는지 확인** — 브라우저 개발자도구 Application 탭에서 쿠키를 `admin_session=1` 로 수동 지정한 뒤 `/api/admin/leads` 호출 → **401** 이어야 함. (같은 방법으로 `admin_auth=1` 도 401 확인)
4. 정상 로그인 동작 확인 — 실제 비밀번호로 `/admin` 로그인 → leads·analytics·posts 목록이 전부 정상 표시
5. 만료 검증 — `createSessionToken()`의 `MAX_AGE_SEC`를 임시로 `-1`로 바꿔 토큰 생성 후 401 나오는지 확인하고 **원복**
6. 서명 위조 검증 — 정상 토큰의 마지막 서명 부분 한 글자를 바꿔서 요청 → 401
7. `ADMIN_SESSION_SECRET`를 임시로 비운 상태에서 `/api/admin/leads` 호출 → **401**(500이나 200이 나오면 fail-closed 구현 실패). 확인 후 원복
8. 로그아웃 → 다시 `/api/admin/leads` 호출 → 401
9. **라이브 사이트로 검증하지 말 것** — 로컬 dev에서 전부 확인
10. 커밋 후 **push 진행**. 배포 후 관장이 `/admin`에서 한 번 재로그인 필요(기존 세션 무효화됨 — 정상 동작)
11. 배포 후 확인: 로그인 전 상태에서 `/api/admin/leads` 가 401인지, 로그인 후 정상인지

## 배포 후 관장에게 알릴 것

- 관리자 로그인이 한 번 풀립니다 → 비밀번호로 다시 로그인하면 끝
- 이후 비밀키를 교체하면 모든 세션이 즉시 무효화됩니다(비밀번호 유출 의심 시 사용)
