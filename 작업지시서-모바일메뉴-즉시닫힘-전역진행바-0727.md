# 작업지시서 — 모바일 메뉴 즉시 닫힘 + 전역 진행 바 (2026-07-27)

## 앞선 지시서(`작업지시서-페이지전환-대기표시-및-blog캐시-0727.md` 파트 B)의 설계 오류 정정

파트 B에서 `useLinkStatus()`를 쓰라고 지시했다. `useLinkStatus`는 **`<Link>`의 자손에서만 동작**한다는 제약이 있고, 그 제약을 맞추려고 `Header.tsx`가 다음 구조를 갖게 됐다:

```tsx
// 링크 클릭 즉시 메뉴를 닫으면 그 Link의 자손인 NavPendingBar가 전환 중에
// 바로 언마운트돼 진행 바가 뜰 틈이 없다. 실제 라우트가 바뀐 뒤 닫아야 한다.
useEffect(() => { setMenuOpen(false); }, [pathname]);
```

**이 트레이드오프가 잘못됐다.** 진행 바를 보여주려고 메뉴 닫힘을 URL 변경에 종속시킨 결과:

- 이미 `/blog`에 있는 상태에서 모바일 메뉴 → "칼럼"을 누르면 **pathname이 안 바뀌어 메뉴가 영원히 안 닫힌다.** 바만 한 번 번쩍하고 화면은 메뉴에 갇힌다. (관장 실제 제보, 모든 메뉴 항목에서 동일)
- 쿼리만 바뀌는 이동(`/blog?cat=...`)도 같은 문제.

**표준 동작:** 모바일 오버레이 메뉴는 항목을 누르는 즉시 닫는다. 같은 페이지든 아니든 예외 없다. 이게 우선이고, 진행 바가 거기 맞춰야 한다.

**→ 진행 바를 `<Link>` 자손에서 전역 컴포넌트로 옮긴다.** 링크·메뉴가 언마운트돼도 바가 살아남으므로 두 요구가 충돌하지 않는다.

`loading.tsx`는 여전히 폐기 상태다. **이 지시서 어디서도 `loading.tsx`를 만들지 말 것** (404 상태코드가 200으로 고정되는 문제, 이미 검증됨).

---

## 1. 신규 — `src/components/RouteProgress.tsx`

전역 진행 바 + 컨텍스트를 한 파일에 둔다.

```tsx
"use client";

import { createContext, useContext, useCallback, useEffect, useRef, useState } from "react";
import { usePathname, useSearchParams } from "next/navigation";

const Ctx = createContext<{ start: () => void }>({ start: () => {} });

export function useRouteProgress() {
  return useContext(Ctx);
}

export function RouteProgressProvider({ children }: { children: React.ReactNode }) {
  const [active, setActive] = useState(false);
  const timer = useRef<ReturnType<typeof setTimeout> | null>(null);

  const clear = useCallback(() => {
    if (timer.current) { clearTimeout(timer.current); timer.current = null; }
  }, []);

  const start = useCallback(() => {
    clear();
    setActive(true);
    // 안전장치: 어떤 이유로든 전환이 안 끝나도 바가 영구히 남지 않게 한다.
    timer.current = setTimeout(() => setActive(false), 10000);
  }, [clear]);

  return (
    <Ctx.Provider value={{ start }}>
      <ProgressStopper onStop={() => { clear(); setActive(false); }} />
      {active && (
        <div
          aria-hidden
          className="fixed top-0 left-0 right-0 h-[3px] z-[100] bg-[#7B2D8B]/15 overflow-hidden"
        >
          <div className="h-full w-2/5 bg-[#7B2D8B] rounded-full animate-[routebar_1.1s_ease-in-out_infinite]" />
        </div>
      )}
    </Ctx.Provider>
  );
}

/** URL이 실제로 바뀌면(=전환 완료) 바를 끈다. useSearchParams는 Suspense 경계가 필요해 분리. */
function ProgressStopper({ onStop }: { onStop: () => void }) {
  const pathname = usePathname();
  const searchParams = useSearchParams();
  useEffect(() => { onStop(); }, [pathname, searchParams]); // eslint-disable-line react-hooks/exhaustive-deps
  return null;
}
```

### 1-1. ⚠️ `useSearchParams` 주의 (0726 회귀 재발 방지)

`081682a`에서 **`useSearchParams` 때문에 `/blog` 원본 HTML이 통째로 비어 크롤러가 내부링크를 하나도 못 받는 사고**가 있었다. `useSearchParams`를 쓰는 클라이언트 컴포넌트는 `<Suspense>` 경계가 없으면 상위 트리 전체를 클라이언트 렌더로 밀어낸다.

따라서 `ProgressStopper`는 **반드시 `<Suspense>`로 감싼 채** 렌더한다. 위 코드의 `<ProgressStopper .../>` 자리를:

```tsx
<Suspense fallback={null}>
  <ProgressStopper onStop={...} />
</Suspense>
```

로 바꾸고 `Suspense`를 import 할 것. **검증 4번에서 이걸 curl로 실측한다.**

`searchParams` 추적이 부담스러우면 `pathname`만으로 끊고 `useSearchParams`를 아예 안 쓰는 쪽도 허용한다(쿼리만 바뀌는 이동은 10초 타임아웃 대신 3초로). **둘 중 어느 쪽을 골랐는지 보고에 명시할 것.**

### 1-2. 애니메이션 키프레임 — `src/app/globals.css`

기존 `animate-pulse`(깜빡임)는 "한 번 지나가고 멈춘 것"처럼 보인다는 지적이 있었다. 좌→우로 계속 흐르는 무한 애니메이션으로 바꾼다.

```css
@keyframes routebar {
  0%   { transform: translateX(-100%); }
  100% { transform: translateX(350%); }
}
```

Tailwind v4 `@theme` 방식을 쓰고 있으면 그 규약에 맞춰 등록할 것. 임의 값 클래스(`animate-[routebar_...]`)가 안 잡히면 `.route-bar` 유틸 클래스를 직접 정의해도 된다.

---

## 2. `src/app/layout.tsx` — Provider 배치

`<body>` 최상단 자식으로 `RouteProgressProvider`가 children 전체를 감싸게 한다. Header가 그 안에 들어와야 `useRouteProgress()`가 동작한다.

**서버 컴포넌트 레이아웃이 클라이언트 컴포넌트로 통째 전환되지 않는지 확인할 것** — Provider는 클라이언트지만 children은 서버 렌더 그대로 유지된다(정상). 검증 3번으로 실측한다.

---

## 3. `src/components/Header.tsx` — 핵심 수정

### 3-1. 삭제
- `import NavPendingBar` 및 **모든 `<NavPendingBar />` 사용처 6곳** (로고 2곳·데스크톱 5개·모바일 5개)
- `src/components/NavPendingBar.tsx` 파일 삭제
- `useEffect(() => setMenuOpen(false), [pathname])` — **삭제하지 말고 남겨둔다** (뒤로가기 등 링크 클릭 외 경로로 URL이 바뀌는 경우의 보험). 단 이제 주된 닫힘 경로가 아니다. 위의 잘못된 주석은 지운다.

### 3-2. 링크 클릭 핸들러 (모바일 오버레이 + 데스크톱 공통)

```tsx
const { start } = useRouteProgress();

const handleNav = (href: string) => {
  setMenuOpen(false);                 // 항상 즉시 닫는다. 같은 페이지여도 닫는다.
  if (href === pathname) {
    window.scrollTo({ top: 0, behavior: "smooth" });  // 같은 페이지면 맨 위로
    return;                            // 진행 바 띄우지 않음(전환이 없으므로)
  }
  start();
};
```

- 모바일 오버레이의 5개 링크 + 오버레이 상단 로고 링크 → `onClick={() => handleNav(link.href)}`
- 데스크톱 nav 5개 + 헤더 로고 → 동일하게 `onClick`. (데스크톱은 `setMenuOpen(false)`가 무해)
- **카카오 "무료 상담" 버튼에는 넣지 않는다** (외부 이동)

`onClick` 대신 Next 16의 `<Link onNavigate>`를 써도 되지만, `onNavigate`는 같은 URL 클릭 시 발화가 보장되지 않아 **"같은 페이지에서도 닫힌다"는 이번 수정의 핵심 요구를 못 지킬 수 있다. `onClick`을 쓸 것.**

### 3-3. 오버레이 z-index

오버레이는 `z-[60]`이고 새 바는 `z-[100]`이다. 다만 오버레이가 `sticky z-50` 헤더의 자손이라 헤더의 쌓임 맥락에 갇혀 있다. 바가 오버레이 위에 제대로 보이는지 **모바일 뷰포트에서 눈으로 확인**할 것. 안 보이면 오버레이를 헤더 밖(레이아웃 레벨 포털)으로 빼지 말고, **바를 Provider가 렌더하므로 이미 헤더 밖**이라 정상일 것 — 그래도 실측 확인은 필수.

---

## 4. 검증 (전부 로컬 `npm run build` 후, 라이브 아님)

1. `npx tsc --noEmit`, `npm run build` 통과
2. **404 회귀 (최우선)**
   ```bash
   curl -s -o /dev/null -w "%{http_code}\n" "http://localhost:3000/blog?page=9999"   # 404
   curl -s -o /dev/null -w "%{http_code}\n" "http://localhost:3000/blog/없는슬러그"   # 404
   ```
3. **SSR 회귀 — 원본 HTML로 확인 (브라우저 DOM 금지)**
   ```bash
   curl -s http://localhost:3000/blog | grep -o 'href="/blog/[^"]*"' | wc -l   # 10 이상
   curl -s http://localhost:3000/blog | grep -o '<link rel="canonical"[^>]*>'  # /blog
   curl -s http://localhost:3000/ | grep -o 'href="/blog"' | wc -l             # 1 이상
   ```
   (`grep -c` 쓰지 말 것 — SSR 출력이 한 줄이라 항상 1이 나온다)
4. **초기 HTML에 바가 없어야 함**
   ```bash
   curl -s http://localhost:3000/blog | grep -c 'z-\[100\]'   # 0
   ```
5. **모바일 뷰포트(개발자도구 기기 모드) 수동 확인 — 이번 수정의 본질**
   - `/blog`에서 햄버거 → "칼럼"(같은 페이지) 탭 → **메뉴가 즉시 닫히고** 맨 위로 스크롤. 바는 안 뜸
   - `/about`에서 햄버거 → "칼럼" 탭 → **메뉴가 즉시 닫히고** 상단 바가 좌→우로 계속 흐르다가 `/blog` 도착 시 사라짐
   - 나머지 4개 메뉴 + 오버레이 로고에서도 동일
   - 메뉴 열었다 X로 닫기 → `body` 스크롤 잠금이 정상 해제되는지
   - 뒤로가기(브라우저) 후에도 바가 남아있지 않은지
6. 데스크톱에서 상단 nav 5개 동작 + 레이아웃 밀림 없음, 카카오 버튼엔 바 안 뜸
7. 커밋 후 push. 배포되면 라이브에서 2·3번만 재확인(그 외 라이브 반복 접속 금지 — 애널리틱스에 방문자로 잡힘)

## 보고에 포함할 것

- 1-1에서 `useSearchParams` 포함/제외 중 어느 쪽을 골랐는지와 이유
- 검증 2·3·4의 출력 원문
- 5번 항목별 통과 여부

## 하지 말 것

- `loading.tsx` 재도입
- 스켈레톤 UI 추가 (별건, 나중에 하면 `/blog` 연구노트 카드 자리 포함해야 함)
- 배포된 커밋 rebase / force-push
- 추측으로 여러 개 동시 변경
