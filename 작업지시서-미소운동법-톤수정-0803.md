# `/method` 톤 수정 — 문안만 교체

작성일: 2026-08-03 (코워크 기획, 구현은 로컬 Claude Code)
기준 원고: `naemiso-brand/동작원리정의서/06_미소운동법-v1.0-원고v3.md` **(2026-08-03 톤 수정본)**
문체 규칙: `naemiso-brand/블로그팀/guide/DECISIONS.md` 4.11

> **착수 조건 없음. 지금 시작한다.**

---

## 0. 이 작업의 성격

**구조·레이아웃·컴포넌트는 하나도 건드리지 않는다. 문자열만 바꾼다.**

관장이 라이브 페이지를 읽고 "AI가 쓴 티가 난다"고 지적한 표현을 걷어내는 작업이다.
`src/app/method/page.tsx` **단일 파일**만 수정한다.

- `SectionHeading` / `Chunks` / `MOVEMENT_CARDS` / `report4050` — 손대지 않음
- 섹션 순서·개수 — 손대지 않음
- `Chunks parts={[...]}` 의 **의미 단위 분할 구조는 유지**하되, 문장이 바뀌면 분할도 새 문장에 맞춰 다시 나눈다
  (규칙: `작업지시서-한글줄바꿈-의미단위정리-0727.md`)

---

## 1. 문자열 교체표 — 이대로 그대로

줄번호는 2026-08-03 기준 참고값. **문자열로 찾을 것.**

### 1-1. 상수 블록 (`PROCEDURE` / `REVISIONS`)

| 위치 | 현재 | 교체 |
|---|---|---|
| L75 | `"빈 깡통이 어디인지가 판정입니다."` | `"빈 깡통이 어디인지 찾는 게 이 단계입니다."` |
| L77 | `label: "적재"` | `label: "얹기"` |
| L77 | `"토대가 생긴 다음에 무게를 얹습니다."` | `"토대가 생긴 다음에 무게를 올립니다."` |
| L114 | `"평가 기준 — 무엇을 어떤 순서로 보는가"` | `"몸을 볼 때 무엇부터 보는지"` |
| L115 | `"증상 → 원인 매핑 확장"` | `"어디가 아플 때 원인이 주로 어디인지"` |
| L116 | `"40~59세 여성 체형 데이터 반영"` | `"40~50대 여성 체형 측정에서 실제로 나온 것"` |

**v1.0 행의 `desc`도 확인할 것.** `"정의 · 전제 · 용어 · 원칙 · 절차 · 감별 공개"` →
`"첫 공개 — 지금 보고 계신 내용"`

### 1-2. 리드 · 1장

| 현재 | 교체 |
|---|---|
| `새로 만든 것이 아니라, 이미 하고 있던 것을 적었습니다.` | `새로 만든 건 없습니다. 이미 하고 있던 걸 적었습니다.` (두 문장으로 분리) |
| `저희는 그렇게 가르지 않습니다.` | `저희는 그렇게 나누지 않습니다.` |
| `무엇은 운동이고 무엇은 아니다,` (앞 구절) | `이건 운동이고 저건 아니고,` |
| `이건 운동의 부작용이 아니라 운동의 성질입니다.` | `부작용이 아닙니다. 운동이 원래 그런 겁니다.` |
| `이미 일하고 있는 자리가 그 일까지 더 하게 됩니다.` | `이미 일하고 있는 자리가 그 일까지 떠맡습니다.` |
| `몇 킬로, 몇 회가 아니라 그 자리에 힘이 들어오는 것이 그날의 목표입니다.` | `몇 킬로를 들었는지, 몇 회를 했는지는 세지 않습니다. **그 자리에 힘이 들어오면 그날은 된 겁니다.**` |

`한 회차의 성과도 그래서 다릅니다` → `한 회차의 목표도 그래서 다릅니다`

### 1-3. 2장 전제 — ★ 인용블록 승격 포함

| 현재 | 교체 |
|---|---|
| 전제 1 제목 `몸은 부위가 아니라 사슬이다` | `몸은 부위별로 따로 놀지 않는다` |
| 전제 1 본문 `몸은 부위별로 따로 놀지 않습니다. 한 곳이…` | 앞 문장 삭제. `한 곳이 제 역할을 못 하면…` 부터 시작 |
| L263 **인용블록** `마사지하는 그 근육은 계속 피해자예요…돌아오지 않아요.` | **인용블록(blockquote) 제거하고 본문 문단으로.** 문안: `마사지하는 그 근육은 계속 피해자입니다. 원인은 따로 있는데 거기만 자꾸 눌러댄다고 돌아오지 않습니다.` |
| L272–273 `여기가 이 방법의 중심축입니다. 임상 기록 30편 중 22편에 같은 판단이 반복해서 나옵니다.` | `이걸 잘못 보면 뒤에 하는 게 전부 어긋납니다. 뻣뻣해 보여서 오시는 분들, 거의 다 여기에 해당합니다.` |
| `움직이는 범위가 없어서 못 움직이는 게 아니라, 범위는 있는데 그 안에서…` | `범위가 없어서 못 움직이는 경우는 생각보다 적습니다. 범위는 있는데 그 안에서 스스로 버텨주지 못하는 경우가 훨씬 많습니다.` |
| `유연성이 부족해 보이는 것의 정체가 사실은 안정성 결핍인 것입니다.` | `유연성이 부족해 보이는 것의 정체가 사실은 안정성 결핍입니다.` |

> **인용블록 승격이 이 작업의 핵심 중 하나다.** 원장 실제 발화가 `>` 안에만 갇혀 있어서
> 본문이 죽어 있었다. 승격하면서 어미만 존댓말로 고치고 내용은 손대지 않는다.

### 1-4. 3장 용어

| 현재 | 교체 |
|---|---|
| `자극을 시켜봤을 때` | `자극을 줘봤을 때` |
| `이 방법에서 가장 중요한 단어입니다.` | `저희가 제일 많이 쓰는 말입니다. 센터에서는 "여기는 약간 깡통 느낌이 난다"고 표현합니다.` |
| `각도나 점수가 아니라 이것이 판정 단위입니다.` | `각도나 점수보다 이게 먼저입니다.` |
| `…좁혀주는 도구일 뿐입니다. 판정은 시켜봐야 나옵니다.` | `…좁혀주는 도구일 뿐이고, 어디가 비어 있는지는 시켜봐야 압니다.` |
| `없던 힘을 만드는 게 아니라 연결이 끊긴 자리를 다시 잇는 쪽에 가깝습니다.` | `없던 힘을 새로 만드는 일은 아닙니다. **끊긴 연결을 다시 잇는** 쪽에 가깝습니다.` |
| 소제목 `토대 / 적재` | `토대 / 얹기` |
| `적재 — 그 위에 무게와 부하를 올리는 일.` | `얹기 — 그 위에 무게와 부하를 올리는 일.` |
| `다음 단계로 넘어가도 되는지 판단하는 기준.` | `다음 단계로 넘어가도 되는지 보는 기준.` |
| `저희의 게이트는 무게나 횟수가 아니라 인지입니다. 그 자리에…` | `저희는 무게나 횟수로 정하지 않습니다. 그 자리에 힘이 들어오는 걸 본인이 느끼면 넘어갑니다.` |

### 1-5. 4장 원칙 — 3번 부연

| 현재 | 교체 |
|---|---|
| `3번이 흔한 말과 갈리는 지점입니다.` | `3번은 좀 다릅니다.` |
| `"호흡이 먼저다"는 어디서나 하는 말이지만, 저희 순서는 골반 → 호흡입니다.` | `"호흡부터"라는 말은 어디서나 합니다. 저희는 골반을 먼저 잡습니다.` |
| `호흡이 1번이 아니라, 호흡이 열리려면 골반이 먼저 잡혀야 합니다.` | `골반이 안 잡히면 호흡이 안 열리거든요.` |

원칙 8개 문장 자체는 **손대지 않는다** (관장 검토 대기 중).

### 1-6. 5장 절차

| 현재 | 교체 |
|---|---|
| 5-1 도식 `측정 → 감별 → 토대 → 적재` | `측정 → 감별 → 토대 → 얹기` |
| L450 `발목·발바닥 통증을 무릎·고관절 문제로 취급합니다.` | `그래서 저희는 손목·팔꿈치 통증을 어깨에서, 발목·발바닥 통증을 무릎과 고관절에서 봅니다.` (앞 구절과 합쳐 재분할) |
| 5-2에 **한 줄 추가** (말단 설명 뒤) | `어깨가 좋아지면 손목도 손가락도 같이 좋아집니다. 발도 똑같습니다.` |
| L458 `몸 전체를 만지지 않습니다.` **앞에 한 줄 추가** | `깨워야 할 자리는 그렇게 많지 않습니다.` |

### 1-7. 6장 감별

| 현재 | 교체 |
|---|---|
| `이 방법에서 가장 중요한 단계이고, 여기서 갈리면 나머지가 전부 헛일이 됩니다.` | `여기서 잘못 갈리면 뒤에 하는 게 전부 헛일이 됩니다.` |
| `같은 0점이라도 앞쪽에는 스트레칭을 주면` | `같은 0점이라도 앞쪽에 스트레칭을 주면` |
| 소제목 `공개 범위` | `여기까지만 적습니다` |
| L542 `그 판정 기준은 공개하지 않습니다.` | `그 기준은 적지 않습니다.` |
| L547 `판정 기준선, 실제 동작, 회차 구성은 이 문서에 넣지 않습니다.` | `기준선, 실제 동작, 회차 구성은 이 문서에 넣지 않습니다.` |
| `글로 옮길 수 있는 부분까지 적었고, 나머지는 몸으로 확인해야 하는 영역입니다.` | `글로 옮길 수 있는 데까지 적었고, 나머지는 몸으로 확인해야 하는 부분입니다.` |

**왼쪽 세로선 박스 스타일은 유지. "비공개" 배지 넣지 않는다.**

### 1-8. 7장 — ★ 인용블록 승격 + 항목 1개 추가

| 현재 | 교체 |
|---|---|
| L94 `가동 범위를 넘겨서 시키지 않습니다` | 같은 항목 안에 이어서: `내 가동 범위가 아니면 가지 마세요. 다치는 경우는 두 가지밖에 없습니다. 너무 무거웠거나, 너무 갔거나.` |
| 그 아래 **인용블록** `내 가동 범위가 아니면 가지 마세요…` | **삭제** (위 항목으로 흡수됨) |
| `저희 선에서 판단하지 않고` | `저희 선에서 보지 않고` |
| 7-2 제목 `시도했고 기각한 것` | `해봤고 남기지 않은 것` |
| 7-2 도입 `실제로 해보고 남기지 않은 것들입니다.` | `실제로 해보고 버린 것들입니다.` |

**7-2에 항목 하나 추가 (기존 2개 뒤에 3번째로):**

```
method: "힘센 근육의 톤을 눌러놓고 약한 쪽을 쓰게 한다",
result: "마사지로 아무리 눌러놔도 힘센 쪽이 계속 힘을 씁니다. 해봤는데 안 됐습니다."
```

> 출처: 원장 인터뷰 1회차(`03_원장인터뷰-1회차-추출.md` 107행). 원고에 빠져 있던 실제 기각 사례다.

### 1-9. 8장

| 현재 | 교체 |
|---|---|
| `이런 케이스는 아직 저희도 기준이 서지 않았습니다.` | `평가할 때 이런 케이스는 아직 저희도 헷갈립니다.` |
| `알고 있는 것을 총동원해 시켜보고` | `알고 있는 걸 총동원해서 시켜보고` |
| `틀렸으면 바로 수정합니다.` | `틀렸으면 바로 고칩니다.` |
| `이 방법은 완성된 체계가 아니라 계속 고쳐지는 체계입니다.` | `이 방법은 아직 완성되지 않았습니다. 계속 고치고 있습니다.` |

### 1-10. 연구노트 부속 블록

L668 `40~59세 여성 회원 {n}명의 실측 데이터를…` → `40~50대 여성 회원 {n}명의 …` (표기 통일)

---

## 2. 검증 — 커밋 전 전부 통과해야 함

### 2-1. 금지어 0건

```bash
grep -nE "가르지|판정|취급|규정|매핑|적재|인지입니다|중심축|30편|22편|갈리는 지점|59세" src/app/method/page.tsx
```
→ **출력 0줄.**

### 2-2. 자기 평가어 0건

```bash
grep -nE "가장 중요한|핵심입니다|중요합니다|지점입니다|단위입니다" src/app/method/page.tsx
```
→ **출력 0줄.**

### 2-3. "아니라" 2회 이하

```bash
grep -c "아니라" src/app/method/page.tsx
```
→ **2 이하.** 남겨도 되는 것은 이 둘뿐:
- `뻣뻣한 게 아니라 못 버티는 것이다` (전제 3 제목 / 원칙 2번 — 같은 문장이므로 하나로 센다)
- `다른 종류의 운동이 아니라, 운동을 하기 전에 밟아야 하는 순서입니다` (관장 확정 프레임, 되돌리지 말 것)

### 2-4. 빌드 · SSR

```bash
npx tsc --noEmit
npm run build
```
배포 후 **원본 HTML로** 확인 (`작업지시서-...-0726.md` 규칙 — 브라우저 DOM 말고):

```bash
curl -s https://www.bodymiso.com/method | grep -c "빈 깡통"
curl -s https://www.bodymiso.com/method | grep -oE "얹기|적재"
```
→ 앞은 1 이상, 뒤는 `적재`가 안 나와야 함.

### 2-5. 줄바꿈 육안 확인

문장이 바뀐 자리는 `Chunks parts` 분할도 다시 맞춰야 한다.
모바일 폭에서 `"뻣뻣해 / 보여서"` 처럼 구가 갈라지지 않는지 확인.

---

## 3. 하지 말 것

- 문안을 "더 자연스럽게" 임의로 다듬지 말 것. **위 표의 문장이 최종 문장이다**
- 원칙 8개 문장 수정 금지 (관장 검토 대기)
- CTA를 버튼으로 늘리지 말 것. 맨 아래 한 줄 링크 유지
- 실적 배지(회원 수·재등록률) 추가 금지
- `MOVEMENT_CARDS` 빈 슬롯 구조 변경 금지

---

## 3-1. ⚠️ 남은 것 1건 — 다음 작업 때 같이 처리 (2026-08-03 라이브 검증에서 발견)

커밋 `b6344c5` 배포 후 라이브 원본 HTML 검증 결과, **1-10 항목이 절반만 반영됨.**

연구노트 부속 블록 안에서 표기가 서로 어긋난다:

| 위치 | 현재 | 고칠 것 |
|---|---|---|
| 본문 | `40~50대 여성 회원 10명의 실측 데이터를…` | ✅ 반영됨 |
| **부제** | `바디닷 3D 체형측정으로 쌓은 **40~59세** 여성 회원 관찰 기록` | `40~50대` 로 통일 |

두 줄 간격으로 `40~50대`와 `40~59세`가 같이 보인다. 단독 배포할 정도는 아니니
`/method` 나 연구노트 쪽 다음 작업 때 함께 고칠 것.

나머지 검증(2-1 ~ 2-5)은 라이브 원본 HTML + 390px 실제 렌더로 전부 통과 확인함.

## 4. 커밋

```
fix(method): 미소 운동법 문안 톤 수정 — AI 티 표현 제거 (관장 지적 0803)
```
