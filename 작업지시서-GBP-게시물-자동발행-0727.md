# 작업지시서 — GBP 게시물 자동 발행

작성: 2026-07-27 (코워크) · **갱신: 2026-08-01 (코워크)** · 담당: 로컬 Claude Code
**착수 조건 해제됨 — 승인 확인 완료, 바로 시작하면 된다.**

---

## 0. 현재 상태 (2026-08-02 갱신)

### 🚨🚨 프로젝트가 바뀌었다 — miso-network 아니고 `gen-lang-client-0240544169`

**2026-08-01에 코워크가 `miso-network`에 세팅한 건 전부 무효다.** 그 프로젝트는 미승인이다.

승인 메일을 열어보니 케이스 번호가 **2-9123000040964**(7/12 신청분)였고,
그 신청은 **`gen-lang-client-0240544169` / 프로젝트 번호 766369871736**로 제출됐다.
7/27 재신청분(7-2471000041200, miso-network)은 아직 심사 중.

| 프로젝트 | Account Management QPM | 판정 |
|---|---|---|
| **gen-lang-client-0240544169** | **300** | ✅ 승인 — **여기를 쓴다** |
| miso-network | 0 | ❌ 미승인 |

### 🚨 승인 판정 기준 (틀렸던 걸 바로잡음)

- **v4(`mybusiness.googleapis.com`) 쿼터는 승인 근거가 아니다.**
  미승인 프로젝트에서도 `Requests per minute 600` / `V4 General 3,000`이 똑같이 표시된다
  (2026-08-02 두 프로젝트 대조 확인). 2026-08-01에 이 값을 보고 "승인됨"이라 판단한 게 오류였다.
- **판정은 신형 API의 `Requests per minute`으로 한다. 0 = 미승인, 300 = 승인.**
  `https://console.cloud.google.com/apis/api/mybusinessaccountmanagement.googleapis.com/quotas?project=gen-lang-client-0240544169`
- 화면값이 미덥지 않으면 **실제 호출**이 최종 근거다. 미승인이면
  `429 RESOURCE_EXHAUSTED` + `quota_limit_value: "0"`가 JSON으로 온다.

### ✅ 끝난 것 — 다시 하지 말 것 (전부 gen-lang-client-0240544169 기준)

| 항목 | 상태 |
|---|---|
| GBP API 승인 (케이스 2-9123000040964) | **300 QPM 확인** |
| `mybusiness.googleapis.com` (v4, 게시물용) | 사용 설정 완료 |
| `mybusinessaccountmanagement.googleapis.com` | 사용 설정 완료 |
| `mybusinessbusinessinformation.googleapis.com` | 사용 설정 완료 |
| OAuth 동의화면 | 생성 완료 — "내몸에미소 GBP 자동화", ekscjs5@gmail.com |
| OAuth 스코프 | `https://www.googleapis.com/auth/business.manage` 등록 완료 |
| 게시 상태 | **프로덕션** (테스트 모드 아님 → 리프레시 토큰 만료 없음) |
| OAuth 클라이언트 | 생성 완료 — 웹 애플리케이션 "GBP 자동 포스팅" |
| `.env.local` | `GBP_CLIENT_ID`/`SECRET`/`REDIRECT_URI` **새 값으로 교체 완료** |
| Supabase 테이블 2개 | 생성 완료 (프로젝트 무관, 그대로 유효) |

**클라이언트 ID**
`766369871736-1a0lg0rsmje318gsp4gb2oh51207ut0l.apps.googleusercontent.com`

> ⚠️ **2026-08-01에 발급받은 리프레시 토큰은 폐기됐다.** 클라이언트가 바뀌었으므로 재발급 필수.
> `.env.local`의 `GBP_REFRESH_TOKEN`은 비워둔 상태.
> 백업: `.env.local.bak-gbp` (구 miso-network 값, 문제 없으면 삭제해도 됨)

**클라이언트 보안 비밀번호** — ✅ **`dongtan-site/.env.local`에 이미 들어있다(`GBP_CLIENT_SECRET`).**
거기서 읽어 쓸 것. 채팅으로 값을 요구하지 말 것.

> ⚠️ 콘솔에서 다시 못 본다. 구글은 생성 시점 1회만 보여주고 이후 **"더 이상 보거나 다운로드할 수 없습니다"**로 마스킹한다
> (2026-08-01 확인 — 이 문서 구버전의 "콘솔에서 복사하세요" 안내는 틀렸다).
> `.env.local` 값을 잃어버리면 콘솔에서 **새 보안 비밀번호를 추가**해야 한다(기존 것은 살아있음).
> 값 검증은 아래 프로브로 가능 — `invalid_grant`면 정상, `invalid_client`면 secret이 틀린 것:
> ```
> POST https://oauth2.googleapis.com/token
>   grant_type=authorization_code&code=dummy&client_id=...&client_secret=...&redirect_uri=...
> ```
> (2026-08-01 이 방법으로 현재 secret이 유효함을 확인함)

**승인된 리디렉션 URI** (토큰 발급 스크립트가 이 주소로 받아야 함)
`http://localhost:5555/oauth2callback`

> 스코프 `business.manage`는 콘솔에서 **"민감하지 않은 범위"**로 분류됐다.
> → 프로덕션 게시했지만 구글 앱 인증(verification) 심사 불필요, "확인되지 않은 앱" 경고도 안 뜰 것으로 예상.

### ⚠️ 확인된 함정 — 여기서 시간 날리지 말 것

1. **구버전 이 지시서의 "0번 확인법"은 틀렸다.** `mybusinessbusinessinformation` / `mybusinessaccountmanagement` 쿼터 페이지의 `Requests per minute`는 **승인 후에도 0으로 표시된다** (2026-08-01 실측). 이걸 보고 "미승인"이라 판단하면 안 된다.
   승인 여부는 **v4 쿼터 페이지**로 판단할 것:
   `https://console.cloud.google.com/apis/api/mybusiness.googleapis.com/quotas?project=miso-network`
   현재 값: `Requests per minute 600` · `V4 General Requests per minute 3,000` · `Create requests per day 100` · `Create requests per minute 60`

2. 위 1번 때문에, **accountId/locationId 조회도 신형 API 말고 v4로 먼저 시도할 것.**
   `GET https://mybusiness.googleapis.com/v4/accounts`
   신형 `mybusinessaccountmanagement`가 429/quota 에러를 뱉으면 그게 원인이다. 권한 문제로 오해하지 말 것.

3. **게시물 API는 신형이 아니라 레거시 v4다.** 신형 분리 API에는 `localPosts`가 없다.

4. **GBP 소유 계정 확인.** 토큰 발급 시 로그인하는 구글 계정이 "내몸에 미소" 등록정보의 소유자여야 한다.
   `v4/accounts` 응답이 비어 있으면 → 다른 계정으로 로그인해야 한다는 뜻. Cloud 프로젝트 계정(ekscjs5@gmail.com)과 GBP 소유 계정이 다를 수 있음.

---

## 0-B. 리프레시 토큰 발급 (첫 작업 — 1회만)

`dongtan-site/scripts/gbp-get-refresh-token.mjs`를 새로 만들어 실행한다.

```
1) localhost:5555 로 임시 http 서버를 띄운다
2) 아래 URL을 콘솔에 출력 → 관장님이 브라우저로 열어 로그인·동의
   https://accounts.google.com/o/oauth2/v2/auth
     ?client_id={CLIENT_ID}
     &redirect_uri=http://localhost:5555/oauth2callback
     &response_type=code
     &scope=https://www.googleapis.com/auth/business.manage
     &access_type=offline
     &login_hint=ekscjs5@gmail.com
     &prompt=select_account%20consent
       ↑ consent 없으면 refresh_token이 안 온다.
       ↑ select_account 없으면 크롬에 여러 계정이 로그인돼 있을 때
         계정 선택창 없이 최근 세션 계정으로 조용히 진행된다.
         2026-08-02에 이것 때문에 엉뚱한 개인계정("델라호야오스카")으로 토큰이 발급돼
         accounts.list에 "내몸에 미소"가 안 나왔다. 둘 다 필수.
3) 콜백으로 받은 code를 POST https://oauth2.googleapis.com/token 으로 교환
4) 응답의 refresh_token을 출력
```

받은 값을 저장할 곳:

- 로컬: `dongtan-site/.env.local`
- Vercel: dongtan-site 프로젝트 환경변수 (Production)

```
GBP_CLIENT_ID=...
GBP_CLIENT_SECRET=...
GBP_REFRESH_TOKEN=...
GBP_ACCOUNT_ID=...     # 0-C에서 조회 후 채움
GBP_LOCATION_ID=...    # 0-C에서 조회 후 채움
```

액세스 토큰은 매 실행 시 refresh_token으로 갱신해서 쓴다(별도 저장 안 함).

## 0-C. account / location ID 확정

### 🚨 v4 목록 조회는 죽었다 (2026-08-01 실측)

```
GET https://mybusiness.googleapis.com/v4/accounts                      → 404 (text/html, GFE 라우팅 단계)
GET https://mybusiness.googleapis.com/v4/accounts/{id}/locations       → 404 (text/html)
GET .../v4/accounts/{id}/locations/{id}/localPosts                     → 404 (application/json) ✅ 살아있음
```

`text/html` 404는 **경로 자체가 서버에 없다**는 뜻(권한·쿼터 문제 아님).
공식 레퍼런스도 이 두 메서드를 deprecated로 표시하고 Account Management API를 쓰라고 안내한다.
**localPosts는 v4에서 정상 동작하므로 게시물 구현 자체는 문제없다.** 목록 조회 경로만 없어진 것.

### 대신 신형 API로 조회한다

승인된 프로젝트(`gen-lang-client-0240544169`)에서는 신형 API가 **300 QPM**으로 살아 있다.
2026-08-01에 miso-network에서 이게 막혔던 건 **프로젝트를 잘못 잡았기 때문**이지 API 문제가 아니다.

```
GET https://mybusinessaccountmanagement.googleapis.com/v1/accounts

GET https://mybusinessbusinessinformation.googleapis.com/v1/accounts/{accountId}/locations
    ?readMask=name,title,storeCode,storefrontAddress
```

- `readMask`는 **필수**다. 빼면 400
- 응답의 `name`은 `accounts/123...` / `locations/456...` 형태 → **숫자 부분만** 잘라서 환경변수에 넣는다
- **이 locationId를 그대로 v4 localPosts 경로에 쓰면 된다** (v1과 v4의 location ID는 같은 값)
- 진짜로 `429 RESOURCE_EXHAUSTED`가 오면 그때가 쿼터 문제 → 보고할 것

### ✅ 확정됨 (2026-08-02) — 다시 조회하지 말 것

```
GBP_ACCOUNT_ID  = 107830495154639118606
GBP_LOCATION_ID = 2531869411278699933
```
`.env.local`에 기입 완료. 리프레시 토큰도 발급 완료(103자).

- 교차검증 ①: 상점코드 `13409326693092269785` + 주소 일치
- 교차검증 ②: GBP UI 주소 `business.google.com/n/2531869411278699933/profile` 의 숫자와 동일
- ⚠️ `accounts.list`의 `accountName`이 **"델라호야오스카"**(관장님 개인계정 표시명)로 나오는 건 정상이다.
  그 계정 아래에 "내몸에 미소"와 "그린테이블"이 같이 들어있는 구조라 **계정명만 보고 잘못됐다고 판단하지 말 것.**
  구분은 반드시 locationId / 상점코드로 한다.

### 대상 식별

- 상점코드 `13409326693092269785` / 주소 "경기도 화성시 동탄지성로 134 한솔프라자 5층 503호"로 대조
- ⚠️ 같은 계정에 **폐업한 "그린테이블"**이 있다. 절대 대상으로 잡지 말 것
- `accounts` 응답이 비어 있으면 → 동의 때 로그인한 구글 계정이 GBP 소유 계정이 아닌 것
- 확정한 두 ID를 위 환경변수에 넣는다

---

## 1. 목표

지금은 코워크가 매달 크롬으로 GBP에 게시물을 손으로 예약 등록하고 있다(2026-07-27 5건 등록 완료).
이걸 **API 자동 발행으로 대체**한다. 성공하면 예약작업 `gbp-monthly-post-batch`는 폐기한다.

구현 위치: `dongtan-site/gbp-sync.mjs`의 `postToGoogleBusinessProfile()` (현재 미구현 스텁)

---

## 2. API 스펙 (2026-07-27 공식 문서에서 확인함)

### 엔드포인트

```
POST https://mybusiness.googleapis.com/v4/accounts/{accountId}/locations/{locationId}/localPosts
```

### 본문 (우리가 쓸 형태 — 업데이트 + 자세히 알아보기 + 사진)

```json
{
  "languageCode": "ko",
  "summary": "게시물 문안 200~350자",
  "callToAction": {
    "actionType": "LEARN_MORE",
    "url": "https://www.bodymiso.com/blog/{슬러그}"
  },
  "media": [
    {
      "mediaFormat": "PHOTO",
      "sourceUrl": "https://www.bodymiso.com/{대표이미지경로}"
    }
  ],
  "topicType": "STANDARD"
}
```

- `actionType: LEARN_MORE` = UI의 **"자세히 알아보기"**. (지시서 구버전에 적힌 "자세히 보기"는 실제 UI 문구가 아님)
- **`media.sourceUrl`은 공개 URL이면 된다. 파일 업로드 아님.** 블로그 대표 이미지가 이미 공개돼 있으므로 그 주소를 그대로 넣으면 끝
- OAuth 스코프: `https://www.googleapis.com/auth/business.manage`

### 수정 / 삭제

```
PATCH  .../localPosts/{localPostId}?updateMask=summary
DELETE .../localPosts/{localPostId}
```

---

## 3. ⚠️ 설계상 중요한 제약 — 예약 발행 필드가 없다

**localPosts API에는 예약 발행(scheduleTime) 필드가 없다.** POST하면 즉시 게시된다.
UI의 "게시물 예약하기"는 UI 전용 기능이다.

그래서 **"한 달치를 미리 걸어두는" 현재 방식은 API로 그대로 옮길 수 없다.** 대신:

- **발행일에 Cron이 돌아서 그날 1건을 POST**하는 구조로 바꾼다
- Vercel Cron 사용 (주간리포트 메일과 동일 패턴 — `작업지시서-주간리포트-메일자동발송-0726.md` 참고)
- 스케줄: **매주 화요일 10:00 KST**
- 발행 대기열은 Supabase 테이블 하나로 관리 (예: `gbp_post_queue` — 문안·슬러그·이미지URL·예정일·발행여부)

이 구조가 오히려 낫다: 큐만 채워두면 코워크가 크롬을 열 일이 아예 없어진다.

---

## 4. 작업 순서

1. **0-B** 리프레시 토큰 발급 → `.env.local` + Vercel 환경변수 등록
2. **0-C** accountId / locationId 확정 → 환경변수 등록
   - `business.google.com/n/2531869411278699933/profile?fid=12868433839712585760` 의 숫자들은 UI용이라 API id와 다를 수 있음. **추측하지 말고 API로 조회해서 확정할 것**
3. ~~Supabase에 `gbp_post_queue` 테이블 생성~~ **✅ 2026-08-01 코워크가 생성 완료. 다시 만들지 말 것**
   컬럼: `id`(identity PK), `slug`, `summary`, `image_url`, `scheduled_date`(date),
   `published`(bool, default false), `published_at`, `local_post_id`, `error`, `created_at`
   - 부분 인덱스 `gbp_post_queue_due_idx` on `(scheduled_date) where published = false`
   - RLS **enabled, 정책 없음** → 서비스롤(`supabaseAdmin`)로만 접근된다. 클라이언트에서 직접 읽지 말 것
4. `gbp-sync.mjs`의 `postToGoogleBusinessProfile()` 구현
5. Vercel Cron 라우트 추가 → `src/app/api/cron/gbp-post/route.ts`
   - `vercel.json`의 `crons` 배열에 추가. **기존 weekly-report 항목을 지우지 말고 추가할 것**
   - 매주 화 10:00 KST = `{ "path": "/api/cron/gbp-post", "schedule": "0 1 * * 2" }` (Vercel cron은 UTC)
   - 인증은 `weekly-report/route.ts`의 기존 패턴을 그대로 따를 것
6. **먼저 1건만 실제 발행해서 검증.** GBP 게시물 목록과 실제 프로필에 뜨는지 확인
   - ⚠️ 검증은 **API 응답 + GBP 실제 화면**으로 할 것. 관장님 크롬 클릭테스트는 신뢰 불가(확장이 하이드레이션 깨뜨림)
7. 성공하면 관장님께 보고 → 예약작업 `gbp-monthly-post-batch` 삭제 여부 결정

**Create requests per day = 100** 이므로 주 1건 페이스는 쿼터 여유가 충분하다. 다만 디버깅 중 재시도 루프는 조심할 것.

---

## 5. 문안 작성 규칙 (큐를 채울 때 그대로 적용)

- 한국어 200~350자. GBP는 앞 2~3줄만 노출되므로 첫 문장에 핵심
- 블로그 제목 복붙 금지 → **검색하는 사람의 증상 언어**로 재작성
- 동탄/화성 지역어는 자연스럽게 1회 정도만
- **질환명 확정 진단·치료 효과 보장·수치 전후비교 금지.** 움직임·자세·운동은 전문가답게 구체적으로
- 사인오프 문구 반복 금지
- 부위·테마가 연속 주에 겹치지 않게 분산, 임상노트와 정보글 섞기

---

## 6. 이미 등록된 5건 (중복 발행 주의)

크롬으로 예약 완료된 건들이다. 큐에 다시 넣지 말 것.

| 예정일 | 슬러그 |
|---|---|
| 7/28(화) 10:00 | dongtan-tennis-elbow-wrist-pain-overuse |
| 8/4(화) 10:00 | knee-valgus-cause-was-collapsed-arch-left-foot |
| 8/11(화) 10:00 | shoulder-impingement-arm-raise-recovery |
| 8/18(화) 10:00 | leg-numbness-not-always-disc |
| 8/25(화) 10:00 | hip-stiffness-groin-tightness-sitting-dongtan |

이 5건은 **이미지 없이** 올라가 있다. 예약 상태에서는 ⋮ → 수정으로 이미지 소급 추가가 가능함을 확인했으므로, API로 `PATCH`가 되는지 시도해볼 것(안 되면 관장님이 UI에서 직접, 또는 그냥 두기).

전체 이력: `naemiso-brand/marketing/gbp-posted-log.md`

---

## 7. 하지 말 것

- **브라우저로 이미지 업로드 시도 금지.** 과거 2회 반나절씩 소모. API의 `sourceUrl` 방식만 사용
- 게시물 건수를 4~5로 고정하지 말 것 → **그 달 화요일 수만큼**(4번인 달과 5번인 달이 섞임)
- 그린테이블(폐업) 건드리지 말 것

---

## 참고 문서

- [Create Posts on Google (공식)](https://developers.google.com/my-business/content/posts-data)
- [localPosts.create 레퍼런스](https://developers.google.com/my-business/reference/rest/v4/accounts.locations.localPosts/create)
