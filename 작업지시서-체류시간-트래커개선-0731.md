# 작업지시서 — 체류시간 측정 + 유입분류 개선 (2026-07-31)

배경: 자체 트래커가 "페이지가 열렸다"까지만 기록해서, 열자마자 닫은 사람과 3분 읽은 사람이
똑같이 1로 잡힌다. 그래서 "진짜 사람이 왔다 갔는지" 판별이 안 된다.
동시에 referrer가 없는 접속(그 주 217건)이 전부 `direct`로 떨어져 "직접 방문"으로 오독되고 있다.

⚠️ 구글 유입 누락은 이 작업으로 **해결되지 않는다.** 구글이 referrer를 아예 안 보내기 때문에
받은 적 없는 정보를 복원할 방법이 없다. 구글 숫자는 계속 GA4·서치콘솔에서 가져온다.
이 작업의 목적은 "잘못 이름 붙은 것을 정직하게 만들고, 체류시간으로 실제 방문 여부를 판별"하는 것.

---

## 1. DB 스키마 변경

Supabase SQL Editor에서 실행:

```sql
alter table page_views
  add column if not exists duration_ms integer,
  add column if not exists scroll_depth smallint;

create index if not exists idx_page_views_created_duration
  on page_views (created_at desc, duration_ms);
```

- `duration_ms` — 페이지에 머문 시간(밀리초). 이탈 시점에 1회 갱신되므로 초기엔 NULL.
- `scroll_depth` — 최대 스크롤 도달률(0~100 정수).
- 기존 행은 전부 NULL로 남는다. 정상이다. **기존 데이터 백필 시도하지 말 것.**

---

## 2. 트래킹 API 수정 — `src/app/api/track/route.ts`

### 2-1. `detectSource()` 교체

`direct` 라벨을 없애고, referrer가 없을 때 랜딩 경로로 한 번 더 갈라낸다.

```ts
function detectSource(referrer: string | null, page?: string | null): string {
  if (!referrer) {
    // referrer가 없는 접속 = 출처 불명.
    // 다만 블로그 개별 글로 바로 진입한 경우는 검색 유입일 가능성이 매우 높다
    // (아무도 /blog/{슬러그}를 주소창에 직접 치지 않는다).
    if (page && /^\/blog\/[^/]+$/.test(page)) return "search_est";
    return "unknown";
  }
  try {
    const host = new URL(referrer).hostname;
    if (host.includes("google")) return "google";
    if (host.includes("naver")) return "naver";
    if (host.includes("kakao")) return "kakao";
    if (host.includes("instagram") || host.includes("ig.me")) return "instagram";
    if (host.includes("bodymiso.com")) return "internal";
    return "other";
  } catch {
    return "unknown";
  }
}
```

⚠️ `"direct"` 문자열은 코드베이스에서 완전히 제거한다. 남겨두면 또 오독한다.
호출부 2군데(`kakao_click` 분기, `page_views` 삽입) 모두 `detectSource(referrer, page)`로 인자 추가.

### 2-2. 체류시간 갱신 분기 추가

기존 이벤트 분기들과 같은 위치에, `page_views` 삽입 **위쪽**에 추가:

```ts
// 체류시간·스크롤 갱신 (이탈 시점에 sendBeacon으로 1회 도착)
if (type === "duration") {
  const { view_id, duration_ms, scroll_depth } = body;
  if (!view_id) return NextResponse.json({ ok: true });
  await supabaseAdmin
    .from("page_views")
    .update({
      duration_ms: Math.min(Math.max(Number(duration_ms) || 0, 0), 30 * 60 * 1000), // 30분 상한
      scroll_depth: Math.min(Math.max(Number(scroll_depth) || 0, 0), 100),
    })
    .eq("id", view_id);
  return NextResponse.json({ ok: true });
}
```

30분 상한을 두는 이유: 탭을 켜둔 채 자리를 비운 경우가 체류시간 평균을 통째로 망가뜨린다.

### 2-3. 삽입 응답에 id 반환

`page_views` 삽입을 `.insert(...)` → `.insert(...).select("id").single()` 로 바꾸고,
응답을 `NextResponse.json({ ok: true, view_id: data?.id })` 로 변경.
클라이언트가 이 id를 들고 있다가 나중에 갱신 요청에 쓴다.

---

## 3. Tracker 수정 — `src/components/Tracker.tsx`

기존 봇 필터·admin_flag 제외 로직은 **그대로 둔다.** 아래를 추가한다.

```tsx
"use client";

import { useEffect, useRef } from "react";
import { usePathname } from "next/navigation";
import { getOrCreateVisitorId, getOrCreateSessionId, isNewVisitor } from "@/lib/visitor";

export default function Tracker() {
  const pathname = usePathname();
  const viewIdRef = useRef<string | null>(null);
  const startRef = useRef<number>(0);
  const maxScrollRef = useRef<number>(0);
  const sentRef = useRef<boolean>(false);

  useEffect(() => {
    // (기존 봇 필터 / admin_flag 제외 로직 그대로 유지)

    viewIdRef.current = null;
    startRef.current = Date.now();
    maxScrollRef.current = 0;
    sentRef.current = false;

    const visitor_id = getOrCreateVisitorId();
    const session_id = getOrCreateSessionId();
    const is_new = isNewVisitor();

    fetch("/api/track", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        page: pathname,
        referrer: document.referrer || null,
        visitor_id,
        session_id,
        is_new_visitor: is_new,
      }),
      keepalive: true,
    })
      .then((r) => r.json())
      .then((j) => { viewIdRef.current = j?.view_id ?? null; })
      .catch(() => {});

    // 스크롤 최대 도달률 기록
    const onScroll = () => {
      const doc = document.documentElement;
      const total = doc.scrollHeight - window.innerHeight;
      const pct = total > 0 ? Math.round((window.scrollY / total) * 100) : 100;
      if (pct > maxScrollRef.current) maxScrollRef.current = Math.min(pct, 100);
    };
    window.addEventListener("scroll", onScroll, { passive: true });

    // 이탈 시 1회 전송
    const flush = () => {
      if (sentRef.current || !viewIdRef.current) return;
      sentRef.current = true;
      const payload = JSON.stringify({
        type: "duration",
        view_id: viewIdRef.current,
        duration_ms: Date.now() - startRef.current,
        scroll_depth: maxScrollRef.current,
      });
      // sendBeacon은 페이지가 사라지는 중에도 전송이 보장된다
      navigator.sendBeacon?.("/api/track", new Blob([payload], { type: "application/json" }));
    };

    // visibilitychange(hidden)가 모바일에서 가장 확실하다. pagehide는 보조.
    const onHide = () => { if (document.visibilityState === "hidden") flush(); };
    document.addEventListener("visibilitychange", onHide);
    window.addEventListener("pagehide", flush);

    return () => {
      flush(); // SPA 내부 이동 시에도 여기서 전송됨
      window.removeEventListener("scroll", onScroll);
      document.removeEventListener("visibilitychange", onHide);
      window.removeEventListener("pagehide", flush);
    };
  }, [pathname]);

  return null;
}
```

핵심 3가지:
- `sentRef`로 중복 전송을 막는다(cleanup과 pagehide가 둘 다 걸릴 수 있음).
- `sendBeacon`을 쓴다. 일반 fetch는 페이지가 닫히는 중에 취소된다.
- cleanup에서도 flush하므로 **SPA 내부 이동에도 체류시간이 남는다.**

---

## 4. 관리자 애널리틱스에 노출 — `src/app/api/admin/analytics/route.ts`

기존 `page_views` 조회 select에 `duration_ms, scroll_depth`를 추가하고, 아래 3개를 응답에 넣는다.

- `realVisitors` — 세션 중 `duration_ms >= 10000`인 세션 수 (10초 이상 = 진짜 방문)
- `bounceUnder10s` — `duration_ms < 10000`인 세션 수
- `topPagesByDuration` — 페이지별 평균 체류시간 Top 10
  (`duration_ms IS NOT NULL`인 행만 평균, 표본 3건 미만인 페이지는 제외)

⚠️ 평균 낼 때 NULL을 0으로 취급하지 말 것. 데이터 쌓이기 전 초기엔 NULL이 대부분이라
0으로 세면 전부 "0초"로 보인다. 반드시 `IS NOT NULL` 필터 후 평균.

/admin/analytics 화면에도 "진짜 방문자 / 10초 미만 이탈 / 글별 평균 체류시간" 3개 블록 추가.

---

## 5. 주간 리포트 메일에 추가 — `src/app/api/cron/weekly-report/route.ts`

`reportJson`에 필드 추가:

```
realVisitors      // 10초 이상 머문 세션 수
bounceUnder10s    // 10초 미만 세션 수
avgDurationSec    // 전체 평균 체류시간(초)
topPagesByDuration: [{ page, title, avgSec, samples }]
```

HTML 표에도 "진짜 방문자 N명 (10초 미만 이탈 N명)" 한 줄과
"오래 읽힌 글 Top 5" 표를 추가한다.

유입경로 표의 라벨도 바꾼다: `direct` → `출처 불명`, `search_est` → `검색 추정`.

---

## 6. 검증 (배포 후)

1. **원본 HTML이 아니라 실제 DB로 확인할 것.** 라이브에서 시크릿 창으로 블로그 글 하나 열고,
   20초 정도 읽으면서 끝까지 스크롤한 뒤 탭을 닫는다.
2. Supabase에서 확인:
   ```sql
   select page, source, duration_ms, scroll_depth, created_at
   from page_views order by created_at desc limit 5;
   ```
   - `duration_ms`가 20000 근처로 찍혀야 한다.
   - `scroll_depth`가 90 이상이어야 한다.
   - referrer 없이 블로그 글로 직행했으므로 `source`가 `search_est`여야 한다.
3. `direct` 문자열이 코드에 남아있지 않은지 확인:
   ```powershell
   Select-String -Path src\**\*.ts,src\**\*.tsx -Pattern '"direct"'
   ```
   결과가 없어야 한다.

⚠️ 관장님 크롬으로 클릭 테스트하지 말 것 — 확장이 하이드레이션을 깨서 이벤트가 안 붙는다.
시크릿 창이나 Playwright로 검증한다.

---

## 하지 말 것

- 기존 page_views 데이터 백필 시도 (불가능하고 의미도 없음)
- 구글 유입을 자체 트래커로 복원하려는 시도 (구조적으로 불가)
- heartbeat(주기적 핑) 방식 도입 — DB 쓰기가 10배 이상 늘어난다. 이탈 시 1회로 확정.
- 리포트 메일에서 GA4·서치콘솔 항목 제거 (구글 담당은 계속 그쪽)
