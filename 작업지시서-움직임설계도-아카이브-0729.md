# 움직임 설계도 아카이브 — 빌드 스펙

작성일: 2026-07-29 (코워크 기획, 구현은 로컬 Claude Code 담당)
선행 문서: `naemiso-brand/동작원리정의서/02_RMS-설계도-초안.md` (관장 O/△/X 회신 대기)
DB 스키마: `dongtan-site/supabase_movement_blueprints.sql`

> **착수 조건:** 관장 카드 판정 회신 + 이름 확정 전에는 4·5장(페이지 빌드)을 시작하지 않는다.
> 1~3장(테이블 생성·적재 스크립트)은 회신과 무관하게 먼저 진행 가능.

---

## 1. 목적

관장 임상 판단을 **기계가 읽을 수 있는 고정 스키마**로 아카이빙하고, 그중 원리 영역만 공개해 구글 E-E-A-T와 신뢰 자산을 만든다.

동시에 부수 효과 하나가 있고, 이게 사실상 이 작업의 두 번째 목적이다:
**현재 `/blog`는 목록 첫 페이지(최신 10건)에만 내부링크가 걸려 있어 오래된 임상노트가 링크 고아 상태다.** 설계도 카드가 `post_slugs`로 임상노트를 부위·패턴별로 끌어안으면, 전체 글에 상시 내부링크가 생긴다.

---

## 2. 공개 / 비공개 경계 (고정 규칙, 예외 없음)

| 공개 | 비공개 |
|---|---|
| 감별 신호를 일반인 언어로 (`pub_signal`) | 점수 조합 → 판정 기준선 (`prv_trigger`) |
| 뿌리 설명 (`pub_root`) | 회차별 동작·세팅·중량 (`prv_protocol`) |
| 먼저 잡는 **부위** (`pub_first_target`) | 다음 단계 전환 판정 기준 (`prv_progress_gate`) |
| 왜 이 순서인지 (`pub_why_order`) | 조건 제약 시 우선순위 규칙 (`prv_constraints`) |
| 관장 실제 문장 인용 (`pub_quote`) | 연결 회원 (`member_ids`) |

**금지 사항 3개**

1. 공개 필드에 **구체적 수치 기준선**을 쓰지 않는다 ("어깨 안정성이 무너집니다" O / "OHS 어깨안정성 0~13점이면" X)
2. 공개 필드에 **동작명**을 쓰지 않는다 (부위 수준까지만)
3. **전후 비교·개선율 수치는 어디에도 공개하지 않는다** (기존 확정 규칙)

기술적 보증: 공개 페이지는 `movement_blueprints_public` **뷰만** 조회한다. 원본 테이블은 anon 키로 접근 불가. 코드에서 원본 테이블을 직접 select 하는 경로를 만들지 않는다.

---

## 3. 용어 금지어

`진단` / `Diagnosis` / `치료` / 구체 질환명을 페이지 문안·URL·메타에 쓰지 않는다.
(2026-07-15 민감단어 순화 결정 유지 — `사이트-민감단어-순화리스트-0715.md`)

1단계 이름은 "측정", 페이지 명칭은 "움직임 설계도"로 통일.

---

## 4. 페이지 구조

### 4-1. 허브 — `/method`

1. 도입부 — 설계도가 무엇인지 3~4문장. "유행이 아니라 순서를 기록한 것"
2. **4단계 도식** (측정 → 감별 → 토대 → 적재). 각 단계 2~3문장
3. **감별 실물 2쌍** — 이 페이지의 핵심. 같은 검사·같은 점수가 정반대 처방으로 갈리는 비교를 나란히 배치
   - 어깨: 각도 제한형 ↔ 안정성 결핍형
   - 무릎: 고관절 기원 ↔ 발아치 기원
4. **부위별 카드 그리드** — 목·어깨 / 허리·골반 / 무릎·발목 / 전신
5. 우리가 하지 않는 일 (선 긋기) — 원칙 후보 9번 채택 시
6. CTA 1개만 — `/check`

### 4-2. 상세 — `/method/[slug]`

`pub_signal` → `pub_root` → `pub_first_target` → `pub_why_order` → `pub_quote` → **이 패턴으로 본 실제 케이스** (`post_slugs` 임상노트 링크 목록) → 짝 카드(`sibling_code`) 링크 → `/check` CTA

### 4-3. 진입 동선

- 전역 메뉴에 **새 항목 추가하지 않는다.** 연구노트와 같은 방식으로 붙인다
- `/blog/[slug]` 임상노트 하단 유틸블록 — "이 케이스가 속한 패턴 보기" 역링크 (양방향 링크가 SEO 핵심)
- `/check` 결과 화면 — "왜 이 순서로 보는지" 링크
- 푸터 1줄

### 4-4. SSR·SEO 요구사항

- **서버 렌더링 필수.** `useSearchParams`·클라이언트 state 목록 금지 (`작업지시서-블로그목록-SSR실패-useSearchParams제거-0726.md` 재발 방지)
- 검증은 **`curl`로 받은 원본 HTML**에서 카드 링크가 보이는지로 한다. 브라우저 DOM·Playwright 렌더 결과로 통과 처리하지 않는다
- 카드별 `generateMetadata`, JSON-LD는 `Article`이 아니라 `HowTo` 사용 금지 → `Article` + `about` 사용 (의료 절차로 오인될 스키마 회피)
- 존재하지 않는 slug는 404 (`notFound()`를 `generateMetadata` 단계에서)
- 한글 줄바꿈은 의미 단위 `inline-block` span 규칙 적용 (`dongtan-site/CLAUDE.md`)

---

## 5. 적재 스크립트

`scripts/upsert-blueprints.mjs` 신규 작성.

- 입력: `naemiso-brand/동작원리정의서/blueprints/*.md` (카드 1개 = 파일 1개, frontmatter에 code·slug·region·is_public·status)
- 동작: `code` 기준 upsert. `evidence_count`는 `member_ids` 길이로 자동 산출
- 실행: `node scripts/upsert-blueprints.mjs`
- **검증 필수:** 실행 후 `movement_blueprints_public` 뷰를 조회해 `prv_*` 컬럼이 한 개도 나오지 않는지 확인. 나오면 즉시 중단

---

## 6. 자동화 — 바디닷 워크플로우에 붙이기

기존 흐름(`miso-network/CLAUDE.md`)에 **3.6단계**를 추가한다.

```
1단계 바디닷 추출 → 2단계 스크립트 입력 → 3단계 실행(Supabase 저장)
  → 3.5단계 export-assessment.mjs (코워크용 json)
  → 3.6단계 match-blueprint.mjs  ← 신규
  → 4단계 배포
```

`scripts/match-blueprint.mjs` 하는 일:

1. 방금 저장된 `body_assessments` 수치를 각 카드의 `prv_trigger` 조건과 대조
2. 일치하는 패턴 코드를 후보로 출력 (예: `SH-01 각도제한형 / KN-02 발아치기원`)
3. 확정 시 해당 카드 `member_ids`에 추가 → `evidence_count` 자동 증가
4. 매칭되는 카드가 없으면 **"신규 패턴 후보"로 표시** — 이게 설계도가 스스로 늘어나는 경로다

**관장이 손대는 지점은 여기 하나뿐이다.** 후보가 맞는지 확인(Y/N) + 안 맞으면 새 패턴 이름 한 줄. 나머지는 자동.

`prv_trigger`가 채워지기 전(관장 회신 전)에는 3.6단계를 건너뛴다.

---

## 7. 완료 검증 체크리스트

- [ ] `curl https://www.bodymiso.com/method` 원본 HTML에 카드 링크 전부 포함
- [ ] `curl` 결과에 `prv_` 시작 필드값이 한 글자도 없음
- [ ] anon 키로 `movement_blueprints` 직접 조회 시 0행 또는 권한 거부
- [ ] 임상노트 → 패턴 카드 → 임상노트 양방향 링크 동작
- [ ] "진단"·"치료"·질환명이 페이지·메타·URL에 없음
- [ ] 없는 slug 접근 시 404 상태코드 (200 아님)
- [ ] 한글 줄바꿈 의미 단위 확인 (모바일 375px)

> 라이브 클릭 검증은 하지 않는다. 관장 크롬 확장이 하이드레이션을 깨는 사례가 있어(2026-07-27) 읽기 검증만 신뢰한다. 상호작용 확인이 필요하면 시크릿창 또는 Playwright.

---

## 8. 이 지시서 밖의 일

- 사진·이미지 첨부는 관장 담당 (로컬에서 admin 업로드)
- git commit / push는 로컬 Claude Code. 코워크는 이 지시서까지만
