# 작업지시서 — GBP 리뷰 답글 자동화

작성: 2026-08-01 (코워크) · 담당: 로컬 Claude Code
**선행 조건: `작업지시서-GBP-게시물-자동발행-0727.md`의 0-B(리프레시 토큰) / 0-C(account·location ID)가 끝나 있어야 한다.**
인증·환경변수는 그 문서와 **완전히 공유**한다. 여기서 새로 만들지 말 것.

---

## 1. 목표

내몸에미소 GBP에 달리는 고객 리뷰에 답글을 자동으로 단다.

답글은 로컬 검색 순위와 신뢰도 양쪽에 직접 영향을 준다. 지금은 아예 손을 못 대고 있으므로,
**"빠짐없이 달린다"가 1차 목표**이고 문안 품질은 그 다음이다.

---

## 2. API 스펙 (2026-08-01 공식 문서에서 확인함)

### 리뷰 목록 조회

```
GET https://mybusiness.googleapis.com/v4/accounts/{accountId}/locations/{locationId}/reviews
    ?orderBy=updateTime desc&pageSize=50
```

응답의 각 review에 들어있는 것: `reviewId`, `reviewer.displayName`, `starRating`
(`ONE`~`FIVE` 문자열), `comment`, `createTime`, `updateTime`, 그리고 **답글이 이미 있으면**
`reviewReply: { comment, updateTime }`.

### 답글 작성/수정

```
PUT https://mybusiness.googleapis.com/v4/accounts/{a}/locations/{l}/reviews/{reviewId}/reply
Content-Type: application/json

{ "comment": "답글 본문" }
```

- 답글이 없으면 생성, 있으면 덮어쓴다(같은 엔드포인트).
- 삭제는 `DELETE .../reply`
- 스코프 `https://www.googleapis.com/auth/business.manage` — **이미 등록돼 있음**
- **"등록정보가 인증(verified)된 경우에만 동작"** — "내몸에 미소"는 Verified라 문제없음

### ⚠️ 인증 함수를 재사용하지 말 것

`src/lib/googleAuth.ts`의 `getGoogleAccessToken()`은 **서비스 계정 JWT** 방식으로,
GA4·서치콘솔 전용이다. **GBP에는 안 통한다**(GBP는 사용자 OAuth 리프레시 토큰 필요).

→ `getGbpAccessToken()`을 **별도 함수로** 추가할 것. 기존 함수를 고치면 GA4 주간리포트가 깨진다.

```ts
// refresh_token → access_token 교환 (매 실행 시)
POST https://oauth2.googleapis.com/token
  grant_type=refresh_token
  &refresh_token={GBP_REFRESH_TOKEN}
  &client_id={GBP_CLIENT_ID}
  &client_secret={GBP_CLIENT_SECRET}
```

---

## 3. 🚨 발행 정책 — 별점으로 갈라야 한다

**전부 자동 발행하면 안 된다.** 낮은 별점에 AI가 만든 답글이 즉시 붙는 게 가장 큰 리스크다.
공개적으로 남고, 지우면 지운 티가 난다.

| 별점 | 처리 |
|---|---|
| **4~5점** | 답글 생성 → **즉시 자동 발행** |
| **1~3점** | 답글 생성 → **발행하지 않고 대기.** 관장님 승인 후 발행 |
| 별점만 있고 본문 없음 | 4~5점이면 짧은 감사 답글 자동, 1~3점이면 대기 |

승인 경로는 **주간리포트 메일과 같은 방식(Resend → Gmail)**으로 한다.
1~3점 리뷰가 감지되면 관장님께 메일 발송 → 메일에 리뷰 원문 + AI 초안 + `bodymiso.com/admin/reviews` 링크.
관장님이 admin에서 문안 고치고 "발행" 누르면 그때 PUT.

> 코워크가 크롬으로 대신 눌러주는 방식은 쓰지 말 것 — 확장이 하이드레이션을 깨서 클릭 검증이 안 된다.

---

## 4. 문안 규칙 (프롬프트에 그대로 넣을 것)

### 반드시 지킬 것

- **150자 내외.** 길면 아무도 안 읽고 템플릿 티만 난다
- 화자는 **브랜드("저희")**. 원장 개인이 아님
- 리뷰 내용에서 **구체적으로 한 가지를 집어** 언급한다 (어느 부위가 편해졌다고 했는지, 무엇이 좋았다는지)
- 존댓말, 담백하게. 과장된 감사 표현 반복 금지
- **별점만 있고 본문 없는 리뷰는 2~3문장**으로 짧게
- 부정 리뷰는 **반박하지 말고 오프라인 대화를 제안**한다

### 🚨 절대 금지

- **작성자 실명 호명 금지.** "○○님, 감사합니다" 식으로 이름을 부르지 말 것.
  리뷰에 건강 관련 내용이 있을 때 업체가 실명을 부르면 **그 사람이 우리 회원이고 그런 상태였음을
  공개적으로 확인해주는 셈**이 된다 (2026-07-27 관장 지시).
  → "회원님" 또는 호칭 없이. **단 "회원님"을 매번 첫머리에 기계적으로 붙이지도 말 것**
- **회원의 성과를 센터 공으로 가져오지 말 것.** 좋아진 건 본인이 한 것
- **질환명 확정.** "회전근개 파열이 좋아지셨다니" 같은 표현 금지
- **약물·주사·시술·수술 언급**
- **치료 효과 보장·단정.** "완치", "재발 없습니다" 류
- **수치 전후비교 노출** (개선율·각도 변화 등은 대외 공개 금지 자산)
- **리뷰어의 몸 상태를 제3자가 읽을 수 있게 확정 서술하지 말 것.** 리뷰어 본인이 쓴 범위 안에서만 받는다
- **사인오프 반복.** "내몸에미소 드림", "항상 건강하세요" 같은 마무리가 여러 답글에 똑같이 붙으면
  자동화 티가 나고 오히려 신뢰를 깎는다. **마무리 문장은 매번 달라야 한다**
- 답글에 링크·전화번호·가격 넣지 말 것

### 이미 답글이 달린 리뷰

`reviewReply`가 있으면 **건드리지 않는다.** PUT은 덮어쓰기라 관장님이 직접 쓴 답글을 날릴 수 있다.
(재작성은 관장님이 admin에서 명시적으로 요청한 경우에만)

---

## 5. 작업 순서

1. ~~Supabase `gbp_reviews` 테이블 생성~~ **✅ 2026-08-01 코워크가 생성 완료. 다시 만들지 말 것**
   - `review_id`(text PK), `star_rating`(smallint), `reviewer_name`, `comment`,
     `create_time`, `update_time`, `reply_draft`,
     `reply_status`(check: `pending`/`approved`/`published`/`skipped`, default `pending`),
     `published_at`, `error`, `fetched_at`
   - 부분 인덱스 `gbp_reviews_display_idx` on `(create_time desc) where star_rating >= 4` — 6-B 노출용
   - RLS **enabled, 정책 없음** → 서비스롤(`supabaseAdmin`)로만 접근. 사이트 노출도 서버에서 읽어 내려줄 것
2. `src/lib/gbpAuth.ts` — `getGbpAccessToken()` 추가 (3번 항의 별도 함수)
3. `src/app/api/cron/gbp-reviews/route.ts`
   - reviews.list 조회 → `reviewReply` 없고 DB에 없는 것만 신규로 취급
   - Claude(`claude-haiku-4-5-20251001`, 기존 `ask/route.ts`와 동일 모델·키)로 답글 초안 생성
   - 4~5점: 바로 PUT → `published`
   - 1~3점: `pending`으로 저장 → Resend로 관장님께 메일
   - cron 인증은 `weekly-report/route.ts` 패턴 그대로
4. `vercel.json` crons에 추가 — **매일 09:00 KST = `{ "path": "/api/cron/gbp-reviews", "schedule": "0 0 * * *" }`**
   - 기존 `weekly-report`, `gbp-post` 항목 지우지 말 것
5. ~~`src/app/admin/reviews/page.tsx`~~ — **1차 범위에서 제외(2026-08-02 결정)**
   현재 리뷰 3건이고 전부 고평점이다. 낮은 별점이 실제로 오기 전에 승인 화면부터 만드는 건 과투자다.
   1~3점이 감지되면 **메일로 초안을 보내는 것까지만** 하고, 그때 관장님이 GBP UI에서 직접 달거나
   코워크에 말하면 된다. 실제로 낮은 별점이 쌓이기 시작하면 그때 이 화면을 만든다.
   → DB의 `reply_status`는 `pending`으로 남아 있게만 할 것(나중에 화면 붙일 때 그대로 씀)
6. **검증: 가장 최근 5점 리뷰 1건에만 먼저 발행**해보고 GBP 실제 화면에서 확인
   - 확인은 API 응답 + GBP 화면. 크롬 클릭테스트로 판정하지 말 것
   - ⚠️ **이미 답글이 달린 리뷰를 고르지 말 것.** 2026-07-27에 코워크가 2건에 수동으로 답글을 달아뒀다.
     `reviewReply`가 없는 것 중에서 고를 것

---

## 6-B. 사이트 노출 (2026-08-01 추가 · **리뷰 5개 이상 쌓인 뒤 착수**)

현재 GBP 리뷰 2개(2026-07-26 기준). 센터에 리뷰 요청 QR을 걸어둔 상태라 곧 5개는 채워질 예정.
**5개가 되면** bodymiso.com에 리뷰 섹션을 붙인다. 그 전엔 만들지 말 것.

### 데이터 경로 — 추가 API 호출이 필요 없다

5번의 `gbp-reviews` cron이 **어차피 매일 `reviews.list`로 전체 리뷰를 받아온다.**
그때 `gbp_reviews` 테이블에 본문·별점·작성자·작성일을 같이 저장해두면,
사이트는 **Supabase에서만 읽으면 된다.**

- 페이지 요청마다 구글 API 호출 ❌ (느려지고 토큰 갱신 필요하고 의미도 없음)
- 반영 지연은 **최대 24시간**. 더 빠르게 원하면 cron을 6시간 주기로 바꾸면 됨
- `reviews.list`는 전체를 다 준다. "최신 5개만 온다"는 Places API 제약이고 우리와 무관

### 노출 규칙

- **4점 이상만 노출.** 1~3점은 화면에 안 띄운다 (2026-08-01 관장 결정)
- **최신순 5개** 고정
- ⚠️ **평균 별점(예: "4.8점")은 같이 표시하지 말 것.** 4점 이상만 골라 보여주면서 평균을 붙이면
  숫자와 목록이 안 맞아 골라 보여준 티가 난다. 개별 리뷰의 별만 표시
- 섹션 하단에 **"구글에서 전체 리뷰 보기"** 링크 (GBP 리뷰 페이지로). 이게 골라 보여주기의 상쇄 장치
- 리뷰어 이름은 `reviewer.displayName` 그대로. **`profilePhotoUrl` 핫링크는 쓰지 말 것** — 구글 CDN 직링크는 깨지기 쉽다. 이니셜 원형 배지로 대체
- 우리 답글이 있으면 리뷰 아래 같이 노출 (답글 자동화의 성과가 그대로 보임)

### 🚨 JSON-LD에 절대 넣지 말 것

`Review` / `AggregateRating` 구조화 데이터로 **넣으면 안 된다.**
구글 공식 가이드라인: 업체가 **자기 사이트에** 자기 리뷰를 `LocalBusiness`/`Organization`
구조화 데이터로 넣으면 별점 리치결과 **대상 제외**이고, "구글 비즈니스 리뷰 위젯"을 예시로 명시했다.
넣어봤자 별은 안 뜨고 위반 소지만 생긴다. **화면 표시만 하고 마크업은 하지 않는다.**

---

## 7. 하지 말 것

- 리뷰를 새로 만들거나 유도하는 자동화는 **범위 밖**이다 (구글 정책 위반 소지). 이 문서는 **답글만** 다룬다
- 이미 답글 있는 리뷰 덮어쓰기 금지
- 1~3점 자동 발행 금지
- `getGoogleAccessToken()` 수정 금지 (GA4 깨짐)

---

## 참고 문서

- [reviews.list](https://developers.google.com/my-business/reference/rest/v4/accounts.locations.reviews/list)
- [reviews.updateReply](https://developers.google.com/my-business/reference/rest/v4/accounts.locations.reviews/updateReply)
