# [내몸에미소 센터] 작업지시서 — 미소코치 전 페이지 플로팅 버튼

작성일: 2026-07-27
실행 순서: **`작업지시서-미소코치-이름변경및말투개선-0727.md` 를 먼저 끝낸 뒤 실행한다.**

---

## 배경 / 목적

미소코치(`/ask`)는 현재 **사실상 고아 페이지**다. 진입 경로를 전수 확인한 결과:

- 헤더 네비 ❌ (`src/components/Header.tsx` navLinks에 없음)
- 홈 ❌ / 푸터 ❌ / 블로그 글 ❌
- 유일한 입구: `src/app/check/CheckQuiz.tsx`의 결과 화면 크로스 프로모 카드 1개
  → **셀프체크를 끝까지 완주한 소수만** 존재를 안다.

안 쓰이는 원인은 기능이 아니라 동선이다. 전 페이지 우하단 플로팅 버튼으로 입구를 연다.

헤더 메뉴 추가는 하지 않는다 — 이미 5개라 모바일에서 길어진다.

---

## 작업 1. 컴포넌트 신설

**새 파일:** `src/components/AskFloatingButton.tsx`

```tsx
"use client";

import Link from "next/link";
import { usePathname } from "next/navigation";
import { MessageSquareIcon } from "@/components/Icons";

// 버튼을 띄우지 않을 경로
const HIDDEN_PREFIXES = ["/ask", "/admin"];

function track(event: string, properties?: Record<string, unknown>) {
  fetch("/api/track", {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ event, properties: properties ?? {} }),
  }).catch(() => {});
}

export default function AskFloatingButton() {
  const pathname = usePathname();
  if (HIDDEN_PREFIXES.some((p) => pathname === p || pathname.startsWith(p + "/"))) {
    return null;
  }

  return (
    <Link
      href="/ask"
      onClick={() => track("ask_floating_click", { from: pathname })}
      aria-label="미소코치에게 물어보기"
      className="fixed bottom-5 right-4 z-40 flex items-center gap-2 rounded-full bg-[#7B2D8B] px-4 py-3 text-white shadow-lg
                 transition-colors hover:bg-[#6a2578] md:bottom-8 md:right-8 md:px-5 md:py-4"
    >
      <MessageSquareIcon size={20} className="shrink-0" />
      <span className="text-sm font-bold md:text-base">미소코치</span>
    </Link>
  );
}
```

**확인할 것:** `MessageSquareIcon`은 `src/components/Icons.tsx`에 이미 존재한다
(`src/app/ask/AskConcierge.tsx`에서 import 중). 없으면 그 파일에서 이름을 맞춘다.

---

## 작업 2. 레이아웃에 삽입

**파일:** `src/app/layout.tsx`

### 2-1. import 추가 (상단 import 블록)

```tsx
import AskFloatingButton from "@/components/AskFloatingButton";
```

### 2-2. 렌더 위치

`<RouteProgressProvider>{children}</RouteProgressProvider>` **바로 아래**, `<Tracker />` 위에 넣는다.

```tsx
<RouteProgressProvider>{children}</RouteProgressProvider>
<AskFloatingButton />
<Tracker />
<Analytics />
```

---

## 작업 3. 겹침 확인

**확인 완료:** `src/components/ScrollToTop.tsx`는 화면에 버튼을 그리지 않는다
(경로 바뀔 때 `window.scrollTo(0,0)`만 하고 `null`을 반환). **겹치지 않는다.**

남은 확인은 이것뿐:

- 모바일(375px 폭)에서 본문 텍스트·기존 버튼을 가리지 않는지
- `/check`, `/check/pain` 결과 화면 맨 아래 CTA 카드와 시각적으로 부딪히지 않는지
  (둘 다 고정 요소가 아니라 스크롤되는 카드이므로 문제는 없을 것으로 보이나 눈으로 확인)

---

## 작업 4. 트래킹 이벤트 등록

새 이벤트 `ask_floating_click`이 관리자 화면에 집계되도록 한다.

**파일:** `src/app/admin/analytics/page.tsx`

- 742행 부근 "미소코치 사용 현황" 카드 묶음에 카드 하나를 추가한다.
- 기존 카드(`events.counts["ai_ask_submit"]`)와 동일한 형태로:

```tsx
<div>
  <p className="text-sm text-gray-500">플로팅 버튼 클릭</p>
  <p className="text-3xl font-bold text-[#7B2D8B]">{events.counts["ask_floating_click"] ?? 0}</p>
</div>
```

→ **클릭 수 대비 `ai_ask_submit` 수**를 보면 "버튼은 눌렀는데 질문은 안 한" 이탈이 보인다.
이 숫자가 3단계(멀티턴 도입) 판단 근거가 된다.

---

## 하면 안 되는 것

- 헤더 navLinks에 `/ask` 추가 ❌ (메뉴 과다)
- 자동으로 펼쳐지는 말풍선·팝업 ❌ (시니어 대상, 방해 요소)
- `/admin` 하위에 노출 ❌ (위 HIDDEN_PREFIXES로 이미 차단)

---

## 검증

1. `npm run build` — 타입 에러 0
2. `npm run dev` 후:
   - `/` `/blog` `/blog/{아무-글}` `/programs` `/check` → 버튼 보임
   - `/ask` → 버튼 **안 보임**
   - `/admin` → 버튼 **안 보임**
3. 모바일 폭(375px)에서 본문·기존 버튼과 겹치지 않는지
4. 버튼 클릭 → `/ask` 이동 + 네트워크 탭에 `/api/track` 호출 확인
5. **라이브에서 반복 테스트 금지** — 로컬 dev로만 확인한다 (애널리틱스 오염)

---

## 관장님이 할 일

없음. 배포 후 휴대폰으로 `www.bodymiso.com` 열어서
우하단 보라색 "미소코치" 버튼이 잘 보이는지, 손가락에 걸리적거리지 않는지만 봐주세요.
위치가 불편하면 말씀해주시면 옮깁니다.
