# [내몸에미소 센터] 작업지시서 — 블로그 글 하단 유틸 유입 블록

작성일: 2026-07-27
실행 순서: `작업지시서-미소코치-이름변경및말투개선-0727.md` 이후.
(플로팅 버튼 지시서와는 서로 독립이라 순서 상관없음)

---

## 배경 / 목적

**이게 1단계에서 가장 큰 구멍이다.**

구글 SEO 유입이 전부 떨어지는 곳은 `/blog/[slug]`인데, 이 페이지에는 유틸 링크가 **하나도 없다.**
현재 글 하단에 있는 것:

- 이전 글 / 다음 글 (`AdjacentPosts`)
- "목록으로 돌아가기"

셀프체크·통증지도·미소코치·상담 CTA가 **전부 없다.** 40건 가까운 글로 모은 트래픽이
글 하나 읽고 그대로 이탈하고 있다.

목표: 글을 다 읽은 사람을 **"내 몸은 어떤가?"** 로 넘긴다.

---

## 설계 원칙

1. **글 본문과 이전/다음 글 사이**에 넣는다. 맨 아래는 이미 안 읽힌다.
2. 글 주제에 맞는 부위를 자동으로 골라 보여준다. 무관한 유틸을 나열하면 광고로 읽힌다.
3. **부위 판별은 기존 `blogKeywords` 맵을 재사용한다.** 새 매핑 테이블을 만들지 않는다.
   (`src/app/check/pain/painData.ts`에 이미 6부위 × 키워드가 정의돼 있음)
4. 카드는 **최대 3개**. 더 늘리면 아무것도 안 눌린다.
5. `tag` 컬럼(임상노트/SEO/칼럼/시니어/기능개선/체형교정/재활)은 **부위 판별에 쓰지 않는다.**
   부위 정보가 없기 때문. 제목+발췌 텍스트로 판별한다.

---

## 작업 1. 컴포넌트 신설

**새 파일:** `src/components/PostUtilBlock.tsx`

```tsx
"use client";

import Link from "next/link";
import { blogKeywords, painAreas, type AreaKey } from "@/app/check/pain/painData";

function track(event: string, properties?: Record<string, unknown>) {
  fetch("/api/track", {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ event, properties: properties ?? {} }),
  }).catch(() => {});
}

// 제목+발췌에서 가장 많이 겹치는 부위를 고른다. 못 고르면 null.
function detectArea(text: string): AreaKey | null {
  let best: AreaKey | null = null;
  let bestHits = 0;
  (Object.keys(blogKeywords) as AreaKey[]).forEach((key) => {
    const hits = blogKeywords[key].filter((kw) => text.includes(kw)).length;
    if (hits > bestHits) {
      bestHits = hits;
      best = key;
    }
  });
  return bestHits > 0 ? best : null;
}

export default function PostUtilBlock({
  slug,
  title,
  excerpt,
}: {
  slug: string;
  title: string;
  excerpt?: string | null;
}) {
  const area = detectArea(`${title} ${excerpt ?? ""}`);
  const areaLabel = area ? painAreas[area].label : null;

  const cards = [
    area
      ? {
          href: `/check/pain?area=${area}`,
          eyebrow: "통증지도",
          title: `${areaLabel} 통증, 원인 확인하기`,
          desc: "체크 몇 개로 어떤 패턴인지 확인해보세요",
          event: "post_util_click_pain",
        }
      : {
          href: "/check/pain",
          eyebrow: "통증지도",
          title: "아픈 부위 골라서 원인 확인하기",
          desc: "목·어깨·허리·골반·무릎·발목",
          event: "post_util_click_pain",
        },
    {
      href: "/check",
      eyebrow: "1분 셀프체크",
      title: "내 체형은 어떤 유형일까",
      desc: "거북목·골반·허리·전신 — 7일 교정 루틴까지",
      event: "post_util_click_check",
    },
    {
      href: "/ask",
      eyebrow: "미소코치",
      title: "증상을 그대로 물어보기",
      desc: "AI가 24시간 답해드려요",
      event: "post_util_click_ask",
    },
  ];

  return (
    <aside className="mt-14 rounded-2xl bg-[#FAF5FB] p-5 md:p-6">
      <p className="mb-1 text-sm font-bold text-gray-900">
        <span className="inline-block">글을 읽으셨다면,</span>{" "}
        <span className="inline-block">내 몸은 어떤지 확인해보세요</span>
      </p>
      <p className="mb-4 text-xs text-gray-500">모두 무료입니다</p>

      <div className="space-y-2.5">
        {cards.map((c) => (
          <Link
            key={c.href}
            href={c.href}
            onClick={() => track(c.event, { slug, area: area ?? "none" })}
            className="block rounded-xl border border-[#f0e4f3] bg-white px-4 py-3.5 transition-colors hover:border-[#7B2D8B]"
          >
            <p className="mb-0.5 text-xs font-semibold text-[#9B4DAB]">{c.eyebrow}</p>
            <p className="text-sm font-bold text-gray-900 md:text-base">{c.title}</p>
            <p className="mt-0.5 text-xs text-gray-500 md:text-sm">{c.desc}</p>
          </Link>
        ))}
      </div>
    </aside>
  );
}
```

### 주의 — import 경로

`painData.ts`는 현재 `src/app/check/pain/` 안에 있다. 컴포넌트에서 app 폴더를 import 하는 게
빌드에 문제되면 **`painData.ts`를 `src/lib/painData.ts`로 옮기고**
`src/app/check/pain/PainMap.tsx`의 import 경로를 같이 고친다. 데이터 내용은 손대지 않는다.

---

## 작업 2. 글 페이지에 삽입

**파일:** `src/app/blog/[slug]/page.tsx`

### 2-1. import 추가

```tsx
import PostUtilBlock from "@/components/PostUtilBlock";
```

### 2-2. 삽입 위치

`<article dangerouslySetInnerHTML={{ __html: html }} />` **바로 아래**,
`{/* 이전/다음 글 네비게이션 */}` 주석 **위**에 넣는다.

```tsx
<article dangerouslySetInnerHTML={{ __html: html }} />

<PostUtilBlock slug={post.slug} title={post.title} excerpt={post.excerpt} />

{/* 이전/다음 글 네비게이션 */}
<div className="mt-16 pt-8 border-t border-gray-100">
```

**확인 완료:** 이 페이지의 posts 쿼리는 `.select("*")`이므로 `post.excerpt`가 이미 들어온다.
쿼리를 고칠 필요 없다.

---

## 작업 3. 통증지도 `?area=` 파라미터 지원

블록에서 `/check/pain?area=knee` 형태로 보내므로, 통증지도가 해당 부위를 **미리 선택된 상태**로
열어야 한다. 안 그러면 링크 문구와 실제 화면이 어긋난다.

**파일:** `src/app/check/pain/PainMap.tsx`

- 컴포넌트 마운트 시 `window.location.search`에서 `area` 값을 읽는다.
  (`useSearchParams`는 이 사이트에서 SSR 실패 이력이 있으므로 **쓰지 말 것** —
  `작업지시서-블로그목록-SSR실패-useSearchParams제거-0726.md` 참고)
- 값이 `painAreas`의 키(`neck` `shoulder` `back` `pelvis` `knee` `ankle`) 중 하나면
  `selected`에 넣고 **바로 체크리스트 화면(`view = "checklist"`)으로 시작**한다.
- 유효하지 않은 값이면 무시하고 평소대로 지도 화면부터 시작한다.

이 `useEffect`는 **메인 컴포넌트 `PainMap`(419행부터)** 안에 넣는다.
`setSelected` / `setView`가 거기 정의돼 있다.

```tsx
useEffect(() => {
  const p = new URLSearchParams(window.location.search).get("area");
  if (p && p in painAreas) {
    setSelected(new Set([p as AreaKey]));
    setView("checklist");
    setEntryArea(p);   // ↓ 아래 연계 작업 참고
  }
}, []);
```

### 연계 — `작업지시서-통증지도-결과저장-0727.md`

그 지시서를 **먼저 했다면** `entryArea` state가 이미 있으므로 `setEntryArea(p)`를 같이 호출한다.
**아직 안 했다면** `setEntryArea(p)` 줄은 빼고 진행한다. 나중에 그 지시서에서 추가된다.

---

## 작업 4. 트래킹 이벤트 집계 추가

**파일:** `src/app/admin/analytics/page.tsx`

새 이벤트 3종을 카드로 추가한다. "블로그 → 유틸 전환" 이라는 제목의 묶음으로 새로 만든다.

- `post_util_click_pain` — 통증지도
- `post_util_click_check` — 셀프체크
- `post_util_click_ask` — 미소코치

기존 카드와 같은 형식(`{events.counts["..."] ?? 0}`)을 쓴다.

> 이 3개 숫자가 **블로그 트래픽이 실제로 유틸로 흐르는지**를 보여주는 지표다.
> 2단계·3단계 판단은 여기 숫자를 보고 한다.

---

## 하면 안 되는 것

- 카드 4개 이상 ❌
- 글 맨 아래(이전/다음 글 뒤)에 배치 ❌ — 도달률이 크게 떨어짐
- 본문 중간 삽입 ❌ — 읽는 흐름을 끊고, `dangerouslySetInnerHTML`이라 구현도 지저분해짐
- 새 부위 매핑 테이블 생성 ❌ — `blogKeywords` 재사용

---

## 한글 줄바꿈 규칙 (필수)

이 블록의 모든 문구는 의미 단위가 갈라지지 않게 처리한다.
두 덩어리 이상인 문장은 `<span className="inline-block">`으로 감싼다.
규칙 원문: `dongtan-site/CLAUDE.md`

---

## 검증

1. `npm run build` — 타입 에러 0
2. **원본 HTML로 검증한다.** 브라우저 DOM이 아니라 크롤러가 받는 것을 봐야 한다:
   ```powershell
   curl.exe -s http://localhost:3000/blog/hwaseong-knee-pain-descending-stairs-cause | findstr /C:"/check/pain"
   ```
   → 링크가 원본 HTML에 나와야 한다.
3. 부위 판별 확인 (dev 서버에서 각 글 열어보기):
   - `hwaseong-knee-pain-descending-stairs-cause` → **무릎**
   - `plantar-fasciitis-ankle-calf-cause` → **발목**
   - `lumbar-disc-exercise-dongtan` → **허리**
   - `why-consistent-exercise-doesnt-change-your-body` → 부위 없음 → 기본 문구
4. `/check/pain?area=knee` 직접 접속 → 무릎 체크리스트로 바로 시작하는지
5. `/check/pain` (파라미터 없이) → 기존대로 지도 화면부터 시작하는지 (회귀 확인)
6. 모바일 폭 375px에서 카드 문구가 어색하게 줄바꿈되지 않는지

---

## 관장님이 할 일

없음.
배포 후 글 하나 열어서 하단 블록 문구가 어색하지 않은지만 봐주시면 됩니다.
