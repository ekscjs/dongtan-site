# 작업지시서 — /blog 목록 쿼리 최적화 (2026-07-27)

## 배경

관장 제보: 모바일에서 `/programs` → 햄버거 → "칼럼"을 누르면 **프로그램 페이지가 그대로 있는 채로 상단 바만 한참 왔다갔다** 한다. "이전 화면 유지 + 상단 진행 바"는 GitHub·YouTube도 쓰는 표준 패턴이지만, **전환이 0.3초 안에 끝날 때만** 자연스럽다. 지금은 `/blog`가 느려서 그 패턴이 무너진 것이다.

→ **진행 바 UI를 더 손대지 말고 `/blog` 응답 속도를 고친다.**

## 발견된 문제

`src/app/blog/page.tsx` 17행:

```ts
const { data } = await supabase.from("posts").select("*")
```

- `posts.content`(글 본문 전체)를 **모든 글에 대해** 가져온다. 목록 화면이 실제로 쓰는 필드는 `id, title, slug, excerpt, tag, created_at, publish_at` 뿐이다 (`BlogPageClient.tsx` 확인함).
- 그 데이터가 클라이언트 컴포넌트 `BlogPageClient`의 props로 넘어가므로, **잘라낸 10건의 본문이 RSC 페이로드에 실려 브라우저까지 전송**된다.
- `revalidate = 60`이 선언돼 있지만 이 라우트는 `searchParams`를 읽어 동적 렌더이므로 Full Route Cache가 적용되지 않는다. 즉 **매 요청마다 Supabase를 친다.**

---

## 0단계 — 먼저 측정 (추측으로 고치지 말 것)

로컬 `npm run build && npm start` 상태에서:

```bash
for i in 1 2 3; do
  curl -s -o /dev/null -w "%{time_starttransfer}s  total=%{time_total}s  size=%{size_download}\n" http://localhost:3000/blog
done
```

`/about`, `/programs`와 비교해서 **몇 배 느린지, 응답 크기가 얼마나 큰지 숫자를 보고에 그대로 적을 것.** 이 숫자가 1·2단계 전후 비교 기준이 된다.

---

## 1단계 — 필요한 컬럼만 조회

```ts
// 현재
.select("*")

// 수정
.select("id,title,slug,excerpt,tag,created_at,publish_at")
```

`Post` 타입 전체를 요구하는 시그니처와 안 맞으면, `src/app/blog/page.tsx` 안에 목록 전용 타입을 새로 만든다:

```ts
type PostListItem = Pick<Post, "id" | "title" | "slug" | "excerpt" | "tag" | "created_at" | "publish_at">;
```

`BlogPageClient`의 props 타입도 `Post[]` → `PostListItem[]`로 좁힌다. **`src/lib/supabase.ts`의 `Post` 타입 자체는 건드리지 말 것** — 상세 페이지(`blog/[slug]`)와 admin이 `content`를 쓴다.

`getPostCategory(p.tag)`, `formatDate(post)` 등이 그대로 동작하는지 타입체크로 확인.

---

## 2단계 — 요청 간 캐시 (1단계 후에도 느리면만)

**1단계만으로 목표(아래 검증 3번)를 달성하면 2단계는 하지 말 것.** 캐시는 신규 글 노출 지연 리스크가 있고(0a25154에서 한 번 겪음), 한 번에 두 개를 바꾸면 무엇이 효과였는지 구분이 안 된다.

필요할 때만:

```ts
import { unstable_cache } from "next/cache";

const getPosts = unstable_cache(
  async (): Promise<PostListItem[]> => { /* 기존 쿼리 */ },
  ["blog-post-list"],
  { revalidate: 60, tags: ["posts"] }
);
```

주의:
- 쿼리 안의 `new Date().toISOString()`(예약발행 필터)이 캐시 채운 시점에 고정된다. **최대 60초까지 예약 글 노출이 늦어질 수 있다** — 허용 범위인지 보고에 명시하고 진행할 것.
- 기존 `cache()`(요청 내 중복 제거)는 `generateMetadata`와 본문이 같은 쿼리를 두 번 치는 걸 막는 용도다. `unstable_cache`로 바꿔도 그 효과는 유지되지만 **두 개를 겹쳐 쓰지 말 것.**

---

## 하지 말 것

- `loading.tsx` 생성 (금지 사유는 `CLAUDE.md`)
- 진행 바(`RouteProgress.tsx`) 디자인·동작 변경 — 이번 건의 원인이 아니다
- 이전 페이지를 흐리게 처리(dim)하는 등의 시각 효과 추가 — 속도부터 고치고, 그래도 어색하면 그때 별건으로 판단한다
- `Post` 타입 자체 축소
- 1·2단계 동시 적용

---

## 검증

1. `npx tsc --noEmit`, `npm run build` 통과
2. **회귀 확인**
   ```bash
   curl -s -o /dev/null -w "%{http_code}\n" "http://localhost:3000/blog?page=9999"        # 404
   curl -s -o /dev/null -w "%{http_code}\n" "http://localhost:3000/blog/없는슬러그"        # 404
   curl -s http://localhost:3000/blog | grep -o 'href="/blog/[^"]*"' | wc -l              # 10 이상
   curl -s http://localhost:3000/blog | grep -o '<link rel="canonical"[^>]*>'             # /blog
   curl -s "http://localhost:3000/blog?cat=임상노트" | grep -o 'href="/blog/[^"]*"' | wc -l  # 1 이상
   ```
3. **속도·크기 개선** — 0단계와 동일한 명령을 다시 돌려 전후 비교. `size_download`가 눈에 띄게 줄고 `time_total`이 짧아져야 한다. **숫자를 전후로 나란히 보고할 것.**
4. 글 상세 페이지(`/blog/{아무 슬러그}`)가 본문을 정상 출력하는지 — 1단계가 상세 페이지 쿼리를 건드리지 않았는지 확인
5. 목록의 카테고리 탭·페이지네이션이 정상 동작하는지
6. 커밋 후 push

## 보고에 포함할 것

- 0단계 측정 숫자 (`/blog` vs `/about` vs `/programs`)
- 1단계 적용 후 같은 숫자
- 2단계를 했는지 안 했는지와 그 판단 근거
- 검증 2번 출력 원문
