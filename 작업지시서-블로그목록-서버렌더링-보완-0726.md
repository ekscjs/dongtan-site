# 작업 지시서 — /blog 목록 서버 렌더링 보완 (0d7d7ef 후속, 2026-07-26)

0d7d7ef 커밋 검증 결과 나온 보완 사항. **관리자 화면(`src/app/admin/page.tsx`)은 검증 전부 통과했으니 건드리지 말 것.**

---

## 검증에서 확인된 문제

`https://www.bodymiso.com/blog`의 **서버 응답 HTML을 그대로 받아서** 파싱한 결과:

| 검사 항목 | /blog | ?page=2 | ?page=4 |
|---|---|---|---|
| `<a href="/blog/...">` 글 링크 | **0개** | **0개** | **0개** |
| `<a href="/blog?page=N">` 페이지 링크 | **0개** | **0개** | **0개** |
| canonical | ✅ 1개, 정확 | ✅ 1개, 정확 | ✅ 1개, 정확 |
| `<title>` | 홈과 동일 | 홈과 동일 | 홈과 동일 |

브라우저에서 JS 렌더 후에는 정상입니다 (글 링크 10개, `A:/blog?page=2` `page=3` `page=4` 실제 앵커 확인, `<button>` 0개).
즉 Playwright 검증이 틀린 게 아니라, **Playwright는 렌더 후를 본 것**이고 크롤러가 받는 초기 HTML은 여전히 비어 있습니다.

`src/app/blog/page.tsx`가 서버 컴포넌트로 바뀌었지만 `return <BlogPageClient />` 뿐이라, 글 데이터는 여전히 클라이언트 `useEffect` → `/api/posts` 경로로만 들어옵니다.

이번 작업의 목적이 "내부링크를 크롤 가능하게" 였으므로, 이 부분이 남으면 절반만 달성입니다.

---

## 1. 글 목록을 서버에서 렌더링 (핵심)

`src/app/blog/page.tsx` (이미 서버 컴포넌트임):

- Supabase에서 발행글 목록을 **서버에서** 조회 (`src/app/api/posts/route.ts`의 쿼리와 동일 조건: `published=true` + `publish_at is null or <= now`, `publish_at desc`).
  - `/api/posts`를 fetch 하지 말고 `@/lib/supabase`를 서버에서 직접 호출할 것 (자기 자신에게 HTTP 호출하는 구조 만들지 말 것)
- 조회 결과를 `<BlogPageClient initialPosts={posts} />`로 전달
- `BlogPageClient`는 `initialPosts`를 `useState`의 초기값으로 사용, `loading` 초기값 `false`
  - 기존 `useEffect` fetch는 **제거**해도 됨 (서버에서 이미 최신 데이터를 받으므로). 제거하면 목록 스켈레톤 깜빡임도 같이 사라짐
- 캐시: `export const revalidate = 60;` 정도로 두되, 예약 발행 시각이 지나면 반영돼야 하므로 60초 초과 금지. `/api/admin/posts` 쪽에 이미 있는 `revalidatePath("/blog")`가 그대로 동작하는지 확인할 것

**확인 방법 (렌더된 DOM 말고 원본 HTML을 봐야 함):**
```
curl -s "https://www.bodymiso.com/blog" | grep -o 'href="/blog/[^"]*"' | wc -l   # 10 이상 나와야 함
curl -s "https://www.bodymiso.com/blog?page=4" | grep -o 'page=[0-9]' | head     # 페이지 링크 나와야 함
```
Playwright/브라우저 DOM으로 확인하면 이 문제를 못 잡습니다. **반드시 원본 HTML 기준으로 확인할 것.**

## 2. 페이지별 title / description

현재 `/blog`, `?page=2,3,4` 전부 `<title>`이 홈과 동일한 "내몸에미소 | 동탄 허리·체형·재활 운동센터"입니다.
블로그 목록인데 홈 제목을 쓰고 있고, 페이지 간 중복 title이라 검색결과에서 서로 잡아먹습니다.

`generateMetadata`에 추가:

- 1페이지: `블로그 | 내몸에미소` + description은 기존 페이지 소개 문구 활용
- 2페이지 이상: `블로그 (N페이지) | 내몸에미소`
- 카테고리 지정 시: `임상노트 | 내몸에미소`, `임상노트 (2페이지) | 내몸에미소`

## 3. 검색 결과 개수 표시 (관리자, 마이너)

검색 결과가 1페이지 이내면 페이지네이션 영역이 통째로 숨겨지면서 `전체 N개 중 X–Y` 카운터까지 같이 사라집니다.
검색해서 몇 건 걸렸는지가 안 보이므로, **카운터는 페이지네이션과 분리해서 항상 표시**할 것 (페이지 번호만 숨김).

---

## 완료 후 확인

- [ ] `curl`로 받은 원본 HTML에 `/blog/{slug}` 링크가 10개 이상 있는지 (**렌더 후 DOM 아님**)
- [ ] 원본 HTML에 `/blog?page=2` 링크가 있는지
- [ ] `?page=4`의 원본 HTML에 마지막 페이지 글들이 들어있는지
- [ ] canonical이 여전히 페이지별로 정확히 1개인지 (2번 작업하다 깨지기 쉬움)
- [ ] 글 상세 → 뒤로가기 스크롤 복원이 여전히 되는지
- [ ] 새 글 발행 후 60초 이내에 `/blog`에 반영되는지
- [ ] 관리자에서 검색 시 결과 개수가 보이는지
- [ ] `npm run build` 통과 → commit + push
