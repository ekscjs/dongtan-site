# 작업지시서 — 자체 트래커에서 작업용 브라우저 트래픽 제외

작성: 2026-08-03 (코워크) · 담당: 로컬 Claude Code

---

## 1. 배경 — 코워크에서 진단한 내용

관장 질문 2가지에서 시작:
1. "크롤링/AI봇/구글색인 방문이 전부 방문자로 카운팅되는 것 같다, 실방문자가 몇 명인지 궁금하다"
2. "코워크가 브라우저로 작업하는 날 방문자가 20명대 → 40명대로 급증한다, 이건 확실하다"

### 1차 확인 — 자체 트래커 vs GA4 (30일)

| 항목 | 자체 트래커 | GA4 |
|---|---|---|
| 방문자/세션 | 673명(고유 visitor_id) | 세션 624건 |
| 페이지뷰 | 1,174건 | 상위 10페이지 합만 1,317건 |

→ 규모가 비슷하고 오히려 GA4 페이지뷰가 더 높음. **크롤러·AI봇이 자체 트래커를 부풀렸다는 가설은 이 비교로는 확인 안 됨.** (Tracker.tsx가 JS 실행 기반이라 Googlebot·GPTBot·ClaudeBot 등 비-JS 크롤러는 애초에 안 잡히고, UA에 bot/crawl/spider/headless 있으면 명시 제외 중이라 구조적으로 타당함)

### 2차 확인 — visitor_id별 집계에서 실제 원인 발견

Supabase `page_views`를 `visitor_id`로 그룹핑한 결과:

- visitor_id `f0692a2d-ec8e-4782-9d0a-1128813aeb0d` 하나가 **6/26~8/3 사이 30개 서로 다른 날짜**에 걸쳐 **총 130회 이상** 접속
- 하루 최대 47페이지뷰(7/27)
- 가장 크게 튄 날(7/31, 전체 41명·69페이지뷰)에서 이 visitor_id 혼자 **24페이지뷰(35%)** 차지
- 방문 페이지: `/`, `/about`, `/method`, `/programs`, `/ask`, 그리고 개별 `/blog/{slug}` 다수 — **콘텐츠 QA/검수 동선과 정확히 일치**, 실사용자 패턴 아님

결론: **관장 지적이 맞음.** 작업용 브라우저(코워크 Claude in Chrome, 로컬 Claude Code의 Chrome MCP, 혹은 라이브에서 직접 확인하는 습관)가 실방문자로 집계되고 있고, 이게 작업일 스파이크의 상당 부분을 설명함.

이미 한 번 확인됐던 문제([[feedback_test_on_local_not_live]], 2026-07-07)가 재발 + 이번에 수치로 확정됨.

---

## 2. 목표

작업용 브라우저 접속을 `page_views`(및 파생 집계: 관리자 애널리틱스, 주간리포트 메일)에서 제외한다.

---

## 3. 구현 방법

기존 `admin_flag` 쿠키 제외 패턴을 그대로 재사용한다 (`Tracker.tsx`에 이미 있는 로직):

```ts
// src/components/Tracker.tsx — 기존 admin_flag 체크 바로 아래에 추가
if (document.cookie.split(";").some((c) => c.trim() === "work_flag=1")) {
  return;
}
```

서버 쪽(`/api/track/route.ts`)도 클라이언트 우회 대비 동일 쿠키를 헤더에서 확인해 방어적으로 한 번 더 걸러주는 걸 권장.

### 쿠키를 심는 주체

- **코워크**: 세션 시작 시 Claude in Chrome의 `javascript_tool`로 `document.cookie = "work_flag=1; domain=.bodymiso.com; path=/; max-age=2592000"` 1회 실행. 이번 대화부터 코워크가 직접 적용 예정.
- **로컬 Claude Code**: 블로그팀 자동화(금요세션 등)에서 Chrome MCP로 라이브 접속할 때도 동일 쿠키가 필요. 자동화 스크립트/워크플로 시작부에 반영할 것.
- **관장 본인이 직접 라이브에서 확인할 때**는 쿠키가 없으니 정상적으로 방문자로 잡힘 — 이건 의도된 동작(실사용자 확인 목적이 아니라도 실제 사람 접속이므로 굳이 막을 이유 없음).

---

## 4. 과거 오염 데이터 처리 — 옵션 A로 확정 (2026-08-03 관장 확인)

`f0692a2d-ec8e-4782-9d0a-1128813aeb0d`는 작업 트래픽으로 사실상 확정.

**옵션 A로 진행할 것** (되묻지 말고 바로 실행):

1. `page_views`에 `is_internal boolean not null default false` 컬럼 추가
2. `UPDATE page_views SET is_internal = true WHERE visitor_id = 'f0692a2d-ec8e-4782-9d0a-1128813aeb0d';` 로 과거 행 백필
   - (참고) 이 visitor_id 외에 추가로 의심되는 게 더 있는지, 배포 전에 3번 항목 검증 쿼리로 한 번 더 훑어볼 것 — `count(*) desc`로 상위 10~15개 정도 재확인해서 비슷한 패턴(30일 중 다수 활동일 + `/blog/{slug}` 다건 방문 + 짧은 간격) 있으면 같이 백필
3. `/api/track/route.ts`의 insert 로직에서 `work_flag` 쿠키 감지 시 `is_internal: true`로 저장하도록 반영 (3번 항목의 서버 방어 로직과 같이 처리)
4. 집계 쿼리 전부에 필터 추가: `src/app/api/admin/analytics/route.ts`, `src/app/api/cron/weekly-report/route.ts`의 `page_views` select 쿼리마다 `.eq('is_internal', false)` 추가
   - `kakao_clicks`, `events` 등 다른 트래킹 테이블도 같은 문제 있는지 훑어보고, 있으면 동일 패턴 적용

과거 스파이크(7/27, 7/31)도 이걸로 설명 가능해지고 그래프가 깨끗해짐.

---

## 5. 검증

1. `work_flag` 쿠키 적용 후 코워크가 라이브 페이지 여러 개 이동 → Supabase `page_views`에서 해당 세션이 새로 안 잡히는지(또는 `is_internal=true`로 잡히는지) 확인
2. 배포 며칠 후 `/admin/analytics` 일별 그래프에서 "작업일 스파이크" 패턴이 사라지는지 확인
3. **curl 원본 HTML 기준으로 검증할 것** — Playwright/브라우저 DOM은 JS 실행 후 상태라 크롤러가 실제로 받는 것과 다를 수 있음([[feedback_seo_verify_raw_html]] 참고)

---

## 6. 참고 — 이번 지시서 범위 아닌 별도 이슈

유입경로(source) 분류가 상당수 깨져 있음: 구글 검색 유입을 자체 트래커는 30일간 17건만 "google"로 잡는데, 같은 기간 GA4는 "검색" 채널만 548세션. 구글 검색 결과 클릭 시 리퍼러가 안 넘어오는 경우가 많아 `direct`/`unknown`으로 잘못 분류되는 것으로 추정. **방문자 수 자체는 맞지만 "어디서 왔는지"가 틀리게 찍히는 문제** — 필요시 별도 지시서로 다룰 것.
