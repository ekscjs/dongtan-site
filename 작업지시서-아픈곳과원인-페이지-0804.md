# 작업지시서 — 「아픈 곳과 원인이 다른 자리들」 신규 페이지

작성: 2026-08-04 · 코워크 → 로컬 Claude Code
**전제: 관장 판정란 승인 후 착수.**

원고: `naemiso-brand/동작원리정의서/10_아픈곳과원인-페이지원고.md`
신규 라우트: **`src/app/method/pain-origin/page.tsx`**

---

## 0. 먼저 할 일 — 슬러그 실재 확인

표 21행에서 임상노트로 링크를 걸 겁니다. **로컬 `blog-drafts/` 폴더는 발행 근거가 아닙니다**(확정 규칙).
`scripts/list-posts.mjs`로 DB에서 실제 발행된 슬러그를 확인하고, 있는 것만 링크합니다.

확인 대상:

```
knee-valgus-cause-was-collapsed-arch-left-foot
dongtan-clinical-multi-symptom-thoracic-first
shoulder-imbalance-thoracic-mobility-first
shoulder-both-sides-pain-whole-body-tension-pelvis
cervicogenic-headache-neck-tension-dongtan
alignment-first-before-resistance-training
plantar-fasciitis-ankle-calf-cause
hip-stiffness-groin-tightness-sitting-dongtan
pelvic-tilt-belly-shape
why-deep-back-pain-wont-go-away        ← ⚠️ 로컬 파일 없음. 실재 여부 확인 필요
leg-numbness-not-always-disc
balance-is-sensory-not-just-strength
flexible-but-unstable-hip-mobility-knee-pattern
athlete-strong-but-injured-mobility-not-strength
pelvic-pain-but-symmetry-was-fine-senior
scoliosis-numbers-look-fine-but-looks-crooked
pain-gone-not-fully-recovered-function
```

- **있으면** → 해당 행 오른쪽에 "관련 기록" 링크 (`/blog/{slug}`)
- **없으면** → 링크 없이 텍스트만. **행은 지우지 않습니다**
- 마지막 4행(손목·발목·종아리·턱)은 인터뷰에서 나온 것이라 대응 글이 없습니다. 링크 없음이 정상입니다

이 페이지의 부수 목적이 여기 있습니다. **`/blog`는 최신 10건만 내부링크가 걸려 있어서
오래된 임상노트가 링크 고아 상태입니다.** 이 표가 그걸 해소합니다.

---

## 1. 라우트

**동적 라우트(`[slug]`)를 만들지 마세요.** 페이지 하나입니다. 정적 라우트로 갑니다.

```
src/app/method/pain-origin/page.tsx
```

`/method` 본문 하단에 이 페이지로 가는 링크를 한 줄 넣습니다.
**카드 그리드나 관련 페이지 섹션을 새로 만들지 마세요.** 문장 안의 링크로 충분합니다.
위치는 `/method` 6장 「여기까지만 적습니다」 바로 뒤가 자연스럽습니다.

---

## 2. 메타

```
title:       "아픈 곳과 원인이 다른 자리들 | 미소 운동법 · 내몸에미소 동탄"
description: "무릎이 아프면 고관절을, 팔이 안 올라가면 골반을 먼저 봅니다.
              내몸에미소가 증상별로 먼저 보는 곳과 그렇게 판단하는 신호 21가지."
canonical:   https://www.bodymiso.com/method/pain-origin
```

- OG 이미지는 `/method`와 같은 것을 재사용합니다. 새로 만들지 마세요
- JSON-LD는 `/method`와 같은 구조. `author`는 실명(Person) 유지, 화면에는 브랜드만
- **`sitemap.ts`에 이 경로 추가**

---

## 3. 본문

원고 `10_` 파일의 `# 아픈 곳과 원인이 다른 자리들` 이하 ~ CTA 한 줄까지가 전부입니다.
**문장을 그대로 씁니다. 요약·확장 금지.**

### 표 렌더링

21행 3열입니다. 모바일에서 3열 표는 깨집니다.

- **데스크톱** — 표
- **모바일** — 행별 카드로 전환. `증상` 굵게 → 그 아래 `흔히 지목되는 곳 → 저희가 먼저 보는 곳`
- `/class/breathing`의 카드 스타일(`rounded-xl` + 연보라 배경)을 재사용합니다. **새 색 만들지 마세요**

### ⚠️ 한글 줄바꿈

이 페이지는 표가 본문이라 특히 잘 깨집니다. `CLAUDE.md` 규칙 적용.

```
고관절 앞쪽 / 발 아치 / 호흡 · 코어 / 골반 · 전신 긴장
발목 · 종아리 / 고관절 · 발이 땅을 딛는 방식
```

- `·` 나열은 flex 항목으로 쪼갭니다
- "저희가 먼저 보는 곳" 열은 짧은 구가 많아 **`inline-block` span으로 묶어 통째로 넘기세요**
- 표 헤더 "흔히 지목되는 곳"이 두 줄로 갈리지 않게 처리

---

## 4. 하지 말 것

- **"아픈 곳은 범인이 아니다"를 이 페이지에서 다시 설명하지 마세요.**
  `/method` 2장에 이미 있고, 반복하면 두 페이지가 서로 갉아먹습니다.
  원고는 의도적으로 설명 없이 목록으로 들어갑니다. 분량이 비어 보여도 채우지 마세요
- **확률·비율 표현 금지.** "대부분", "70%" 같은 말을 넣지 마세요. 원고에 없습니다
- **각도·점수 기준선을 넣지 마세요.** 신호는 공개, 수치 기준선은 비공개
- 원고에 없는 FAQ·체크리스트·"이런 분께 추천" 섹션을 만들지 마세요 (기존 과다반복 항목)

---

## 5. 검증 — curl 원본 HTML로

브라우저 DOM·Playwright 결과는 근거가 안 됩니다.

```bash
curl -s https://www.bodymiso.com/method/pain-origin | grep -o '<title>[^<]*</title>'
curl -s https://www.bodymiso.com/method/pain-origin | grep -c '고관절'
curl -s https://www.bodymiso.com/method/pain-origin | grep -o 'href="/blog/[a-z-]*"' | sort -u | wc -l
curl -s https://www.bodymiso.com/method | grep -c 'pain-origin'
curl -s https://www.bodymiso.com/sitemap.xml | grep -c 'pain-origin'
```

- [ ] title 정상
- [ ] 표 21행이 원본 HTML에 있음 (JS 렌더 후가 아니라)
- [ ] `/blog/` 링크가 실재 슬러그로만 걸려 있음 — 404 없음
- [ ] `/method`에서 이 페이지로 가는 링크 1개
- [ ] sitemap 포함
- [ ] 모바일 폭 375px에서 표가 카드로 전환되고 구가 갈리지 않음

---

## 6. 끝나고

```powershell
cd C:\Users\송경천\Documents\Claude\Projects\naemiso-brand\dongtan-site
npx tsc --noEmit
git add -A
git commit -m "feat: 아픈 곳과 원인이 다른 자리들 페이지 신설"
git push
```

배포 후 5번 검증. 실패 항목만 보고해주세요.
`why-deep-back-pain-wont-go-away` 실재 여부는 결과와 상관없이 알려주세요 —
`05_` 문서에 남은 미확인 항목이라 코워크가 기록을 닫아야 합니다.
