# 작업 지시서 — /blog 범위 밖 페이지 번호 처리 (08aaafa 후속, 2026-07-26)

08aaafa 검증은 전부 통과했습니다. 아래 한 건만 남았습니다. **다른 건 건드리지 말 것.**

## 문제

`https://www.bodymiso.com/blog?page=999` 요청 시:

- 마지막 페이지 글 9개를 그대로 반환 (200 OK)
- `<title>` = `블로그 (999페이지) | 내몸에미소`
- `<link rel="canonical" href="https://www.bodymiso.com/blog?page=999">` ← **자기 자신을 canonical로 선언**

즉 존재하지 않는 페이지 번호마다 **4페이지와 내용이 같은 별개의 색인 대상 URL**이 무한히 생깁니다.
자기 자신을 canonical로 잡고 있어서 구글이 중복으로 걸러내지도 못합니다.
이 사이트는 이미 서치콘솔 미색인 목록에 스팸봇발 쿼리스트링 URL이 잡힌 이력이 있어서 실제로 노출된 위험입니다.

## 수정

`src/app/blog/page.tsx`:

- 전체 글 수로 `totalPages` 계산 (카테고리 필터가 걸린 경우 해당 카테고리 기준)
- `page`가 `1 미만`이거나 `totalPages 초과`면 `notFound()` 호출 → 404 반환
  - `import { notFound } from "next/navigation"`
  - 글이 0건이라 `totalPages`가 0인 경우는 1페이지를 정상 응답으로 처리할 것 (빈 목록 화면). 이때 404 내보내지 말 것
- `page`가 숫자가 아닌 값(`?page=abc`)일 때도 동일하게 404
- `generateMetadata`도 같은 판정을 쓰되, 404가 될 URL에는 canonical을 만들지 말 것

## 확인

```
curl -s -o /dev/null -w "%{http_code}\n" "https://www.bodymiso.com/blog?page=999"   # 404
curl -s -o /dev/null -w "%{http_code}\n" "https://www.bodymiso.com/blog?page=abc"   # 404
curl -s -o /dev/null -w "%{http_code}\n" "https://www.bodymiso.com/blog?page=4"     # 200
curl -s -o /dev/null -w "%{http_code}\n" "https://www.bodymiso.com/blog"            # 200
```

- [ ] 위 4개 상태코드 정상
- [ ] `?page=4` 원본 HTML에 글 9개 그대로 있는지 (기존 동작 안 깨졌는지)
- [ ] 카테고리 지정 + 범위 밖 페이지(`?cat=임상노트&page=99`)도 404인지
- [ ] `npm run build` 통과 → commit + push
