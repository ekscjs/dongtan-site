# 작업지시서 — 주간 애널리틱스 리포트 메일 자동 발송 (2026-07-26)

## 배경 / 왜 하는가

금요일 블로그 자동화 세션이 애널리틱스 데이터를 가져오려면 **코워크가 크롬으로 `/admin/analytics`에 접속**해야 했다.
크롬이 안 켜져 있으면 매번 실패 → 관장이 매주 크롬을 챙겨야 하는 구조였고 실제로 거의 실패했다.

원인 확인(2026-07-26): 코워크 샌드박스에서 **Supabase(`dfkqvobimrjhilihczvp.supabase.co`)와 `bodymiso.com` 모두 네트워크 차단**되어 있음(curl 000).
반면 `github.com`은 열림. 즉 코워크가 데이터를 **직접 가져올 통로가 크롬밖에 없었다**.

**해결 방향 전환**: 코워크가 "가져오는" 대신, **Vercel 서버가 매주 금요일 새벽에 스스로 집계해서 관장 지메일로 메일을 보낸다.**
지메일은 코워크에 MCP로 연결돼 있어 크롬 없이 읽힌다. 관장 PC가 꺼져 있어도 동작한다.

---

## 1. 공통 모듈 추출 — `src/lib/googleAuth.ts` (신규)

현재 `src/app/api/admin/ga4/route.ts` 와 `src/app/api/admin/search-console/route.ts` 에 **`getAccessToken()` 함수가 scope만 다르게 중복**되어 있다. 크론 라우트에서도 필요하므로 하나로 뺀다.

```ts
// src/lib/googleAuth.ts
// GA4 / Search Console 공용 — 서비스 계정 JWT로 액세스 토큰 발급
export async function getGoogleAccessToken(scope: string): Promise<string> {
  const raw = process.env.GOOGLE_SERVICE_ACCOUNT_JSON;
  if (!raw) throw new Error("GOOGLE_SERVICE_ACCOUNT_JSON 환경변수 없음");

  const sa = JSON.parse(raw);
  const now = Math.floor(Date.now() / 1000);

  const header = Buffer.from(JSON.stringify({ alg: "RS256", typ: "JWT" })).toString("base64url");
  const payload = Buffer.from(
    JSON.stringify({ iss: sa.client_email, scope, aud: "https://oauth2.googleapis.com/token", exp: now + 3600, iat: now })
  ).toString("base64url");

  const unsigned = `${header}.${payload}`;
  const crypto = await import("crypto");
  const sign = crypto.createSign("RSA-SHA256");
  sign.update(unsigned);
  const jwt = `${unsigned}.${sign.sign(sa.private_key, "base64url")}`;

  const tokenRes = await fetch("https://oauth2.googleapis.com/token", {
    method: "POST",
    headers: { "Content-Type": "application/x-www-form-urlencoded" },
    body: `grant_type=urn%3Aietf%3Aparams%3Aoauth%3Agrant-type%3Ajwt-bearer&assertion=${jwt}`,
  });
  const tokenData = await tokenRes.json();
  if (!tokenData.access_token) throw new Error(`액세스 토큰 발급 실패: ${JSON.stringify(tokenData)}`);
  return tokenData.access_token;
}
```

기존 두 라우트의 로컬 `getAccessToken()` 을 삭제하고 이걸 import해서 쓰도록 교체:
- ga4: `getGoogleAccessToken("https://www.googleapis.com/auth/analytics.readonly")`
- search-console: `getGoogleAccessToken("https://www.googleapis.com/auth/webmasters.readonly")`

**동작이 바뀌면 안 된다.** 교체 후 `/admin/analytics`에서 GA4·검색어 섹션이 그대로 나오는지 반드시 확인(검증 3번).

---

## 2. 크론 라우트 신규 — `src/app/api/cron/weekly-report/route.ts`

```ts
export const dynamic = "force-dynamic";
export const maxDuration = 60;
```

### 2-1. 인증

Vercel Cron은 `CRON_SECRET` 환경변수가 설정돼 있으면 `Authorization: Bearer <CRON_SECRET>` 헤더를 붙여 호출한다.
수동 테스트용으로 쿼리스트링도 허용한다.

```ts
const secret = process.env.CRON_SECRET;
const auth = req.headers.get("authorization");
const key = new URL(req.url).searchParams.get("key");
if (!secret || (auth !== `Bearer ${secret}` && key !== secret)) {
  return NextResponse.json({ error: "Unauthorized" }, { status: 401 });
}
```

### 2-2. 집계 내용 (최근 7일, KST 기준)

기존 `src/app/api/admin/analytics/route.ts` 의 KST 날짜 헬퍼(`KST_OFFSET_MS`, `kstDateStr`, `kstMidnightUtc`)와
`fetchAllRows()` 페이지네이션 헬퍼를 **그대로 복사해서** 쓴다(공통 lib로 빼는 리팩터링은 이번 범위 아님 — 건드릴 파일 수를 늘리지 말 것).

Supabase 직접 조회(`supabaseAdmin`):

| 항목 | 소스 |
|---|---|
| 방문자 수 (7일, 고유 visitor_id) | `page_views` |
| 페이지뷰 수 (7일) | `page_views` |
| 지난주 대비 증감 (7~14일 전과 비교) | `page_views` |
| 유입경로 top | `page_views.source` |
| 인기글 top 5 (제목까지) | `page_views.page` + `posts` 조인(기존 `slugToTitle` 방식 재사용) |
| 상담 신청 건수 (7일) | `leads` |
| 카카오 버튼 클릭 (7일) | `kakao_clicks` |
| 셀프체크 실행 수 (7일) | `self_check_results` |

Search Console (7일, `sc-domain:bodymiso.com`):
- 검색어 top 10 — query / clicks / impressions / ctr / position
- 유입된 페이지 top 5

GA4 (프로퍼티 541281945, 7일):
- 채널별 세션, 기기별 세션

**개별 소스는 실패해도 전체가 죽지 않게 각각 try/catch로 감싸고, 실패한 섹션은 `"(수집 실패: 이유)"` 로 표기해서 메일에 그대로 넣는다.** 조용히 빈값으로 넘기면 나중에 원인 추적이 안 된다.

### 2-3. 메일 발송 (Resend)

`src/app/api/lead/route.ts` 의 Resend 사용 패턴을 그대로 따른다.

```ts
from: "내몸에미소 <onboarding@resend.dev>",
to: process.env.NOTIFY_EMAIL!,
subject: `[주간 리포트] ${기간종료일} · 방문자 ${방문자수}명 · 신청 ${신청수}건`,
```

**제목 앞의 `[주간 리포트]` 는 코워크가 지메일에서 검색하는 키다. 절대 바꾸지 말 것.**

본문 구조 — 두 부분:

1. **사람이 읽는 HTML 요약** — 위 표 항목을 간결한 표로. 지난주 대비 증감은 `▲12` / `▼3` 형태로.
2. **기계가 읽는 JSON 블록** — 코워크가 파싱해서 쓴다. HTML 맨 아래에 이 형태로 정확히 넣는다:

```html
<div style="display:none;max-height:0;overflow:hidden;mso-hide:all;">
  <pre id="report-json">{"period":{"start":"2026-07-24","end":"2026-07-30"},"visitors":123,"pageviews":456,"visitorsPrev":110,"leads":2,"kakaoClicks":1,"selfChecks":5,"sources":[{"source":"google","count":80}],"topPages":[{"page":"/blog/xxx","title":"...","count":30}],"queries":[{"query":"동탄 체형교정","clicks":3,"impressions":120,"ctr":2.5,"position":8.2}],"topSearchPages":[],"ga4Channels":[],"ga4Devices":[],"errors":[]}</pre>
</div>
```

감싼 `<div>`는 메일 본문에서 사람 눈에 안 보이게 숨기는 용도일 뿐이다.
`<pre>` 태그와 `id="report-json"`은 그대로 유지해야 코워크가 파싱할 수 있다.

- JSON은 **한 줄로** 출력(줄바꿈 넣지 말 것 — 지메일 본문에서 줄이 잘려 파싱이 깨진다)
- 숫자는 문자열이 아니라 숫자 타입으로
- 수집 실패한 섹션은 `errors` 배열에 `"searchConsole: 토큰 발급 실패"` 형태로 담는다

### 2-4. 응답

성공 시 `{ ok: true, sent: true, visitors, leads }` 반환. Vercel 크론 로그에서 바로 확인 가능하게.

---

## 3. 스케줄 등록 — `vercel.json` (신규 생성)

현재 `vercel.json`이 없다. 새로 만든다.

```json
{
  "crons": [
    { "path": "/api/cron/weekly-report", "schedule": "0 22 * * 4" }
  ]
}
```

- Vercel 크론은 **UTC 기준**. `0 22 * * 4` = 목요일 22:00 UTC = **금요일 07:00 KST**
- 코워크 금요 블로그 세션이 09:00 KST라 2시간 여유 확보
- Vercel Hobby 플랜은 "하루 1회보다 자주"는 배포 자체가 실패하지만 **주 1회는 허용**됨. 다만 지정 시각의 **해당 1시간 내 임의 시점**에 실행됨(07:00~07:59 KST) — 09:00까지 여유 있으므로 문제없음

---

## 4. 환경변수 추가 (로컬 `.env.local` + Vercel 대시보드 둘 다)

```
CRON_SECRET=<랜덤 32자 이상 문자열 생성해서 넣기>
```

- `GOOGLE_SERVICE_ACCOUNT_JSON`, `RESEND_API_KEY`, `NOTIFY_EMAIL`, `SUPABASE_SERVICE_ROLE_KEY` 는 이미 있음 — 건드리지 말 것
- Vercel에 `CRON_SECRET` 넣은 뒤 **재배포해야** 크론이 등록된다

---

## 5. 검증 항목 (전부 통과해야 완료)

1. `npx tsc --noEmit` 통과
2. `npm run build` 통과 (`vercel.json` 크론 표현식 오류는 배포 때 터지므로 로컬 빌드로는 안 잡힘 — 표현식 직접 눈으로 재확인)
3. **회귀 확인**: 로컬 dev에서 `/admin/analytics` 접속 → GA4 섹션(국가/도시/기기/채널)과 검색어 섹션이 **기존과 동일하게** 나오는지. 1번 리팩터링이 여길 건드렸으므로 필수. **라이브 사이트로 확인하지 말 것**(애널리틱스에 방문자로 집계됨)
4. 인증 확인: `curl http://localhost:3000/api/cron/weekly-report` → **401**
5. 수동 실행: `curl "http://localhost:3000/api/cron/weekly-report?key=<CRON_SECRET>"` → 200 + 실제로 관장 지메일에 `[주간 리포트]` 메일 도착
6. 도착한 메일에서 `<pre id="report-json">` 안의 JSON이 **한 줄이고 `JSON.parse()` 가능**한지 확인 (`node -e` 로 실제 파싱해볼 것)
7. 숫자 교차검증: 메일의 방문자 수 / 신청 건수가 `/admin/analytics` 화면의 7일 수치와 일치하는지
8. `errors` 배열이 비어 있는지 확인. 비어있지 않으면 그 섹션 원인 해결 후 재실행
9. 커밋 후 **push까지 진행**(배포돼야 크론이 등록되고, 실패해도 사이트 사용자 영향 없는 신규 라우트임)
10. 배포 후 Vercel 대시보드 → Settings → Cron Jobs 에 항목이 등록됐는지 확인

## 손대지 말 것

- `src/app/api/admin/analytics/route.ts` 의 기존 응답 필드·집계 로직 (다른 지시서에서 카카오 섹션을 따로 수정 중 — 충돌 주의, 그 작업이 먼저면 그것부터 끝내고 시작)
- `src/app/api/lead/route.ts`
- 기존 환경변수 값
- `from` 주소(`onboarding@resend.dev`) — 도메인 인증 작업은 별개 사안

## 다음 단계 (코워크가 담당, 로컬 작업 아님)

배포 완료되면 코워크가 `blog-team-friday-session` 예약작업 지시문을 수정한다:
"크롬으로 /admin/analytics 접속" → "지메일에서 `[주간 리포트]` 최신 메일 읽어 JSON 파싱".
**예약작업의 스케줄(cronExpression)은 건드리지 않는다** — 7/31 임시 one-time 설정이 걸려 있고 별도 복구 예정.
