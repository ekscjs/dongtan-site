# [내몸에미소 센터] 작업지시서 — 통증지도 결과 익명 저장

작성일: 2026-07-27
실행 순서: 독립. 다른 지시서와 순서 상관없음. **다만 미룰수록 손해다.**

---

## 배경 / 목적

**지금 통증지도를 쓰는 사람들의 답변이 그냥 버려지고 있다.**

| | 셀프체크 `/check` | 통증지도 `/check/pain` |
|---|---|---|
| 답변 저장 | ✅ `/api/self-check` → `self_check_results` | ❌ **없음** |
| 남는 것 | 답변 전체 + 결과 유형 + 위험도 | 클릭 이벤트 2개(`pain_map_start`, `pain_map_complete`)뿐 |

`src/app/check/pain/PainMap.tsx`를 확인한 결과, `supabase` import는 있으나
**관련 칼럼을 읽어오는 용도로만** 쓰이고 저장 로직은 없다.

지나간 답변은 되살릴 수 없다. **1단계 5개 중 가장 먼저 해도 되는 작업이다.**
(연구노트 4050 데이터랩의 다음 재료가 여기서 나온다)

---

## 개인정보 원칙

- 이름·연락처 **저장 안 함**
- `anon_id`는 셀프체크와 동일하게 방문자 트래킹의 `visitor_id`(localStorage `_miso_vid`) 재사용
- RLS 켜고 공개 정책 만들지 않음 → 서버(service role)만 읽고 씀
- **이 데이터로 만든 경과·개선율 통계는 공개하지 않는다.** 초기 스냅샷 분포만 연구노트에 쓸 수 있다.

---

## 작업 1. 테이블 생성 — ✅ **이미 완료됨 (2026-07-27, 코워크가 Supabase에서 직접 실행)**

`public.pain_map_results` 테이블은 **이미 생성돼 있다.** 다시 실행할 필요 없다.
검증 결과: 컬럼 9개 정상 / `rls_enabled = true` / `policy_count = 0`(공개 정책 없음 = 서버만 접근).

아래 SQL은 **기록용으로 파일만 만들어 두라.** 실행은 하지 말 것.
(`create table if not exists`라 다시 돌려도 무해하지만 불필요)

**새 파일:** `dongtan-site/supabase_pain_map.sql`

```sql
-- 내몸에미소: 통증지도(/check/pain) 결과 익명 저장 테이블
-- 사용법: Supabase 대시보드 > SQL Editor 에 붙여넣고 Run. 한 번만 실행하면 됩니다.
-- 개인정보(이름·연락처) 없음 — 순수 익명. anon_id는 방문자 트래킹 visitor_id 재사용.

create table if not exists public.pain_map_results (
  id            bigint generated always as identity primary key,
  created_at    timestamptz not null default now(),
  anon_id       text,          -- localStorage _miso_vid 재사용

  areas         text[],        -- 선택한 부위 키 배열 예: {neck,back}
  area_count    int,           -- 선택 부위 수

  -- 부위별 체크 답변 원본. 예: {"neck":[true,false,...],"back":[...]}
  answers_raw   jsonb,

  -- 매칭된 결과 카드 제목 배열. 예: {거북목·라운드숄더 패턴,경추 가동성 저하}
  findings      text[],
  -- 우선순위 1(주의) 카드 수
  priority1_count int,

  entry_area    text           -- ?area= 로 들어온 경우 그 값, 아니면 null
);

create index if not exists pain_map_results_created_at_idx
  on public.pain_map_results (created_at desc);
create index if not exists pain_map_results_anon_id_idx
  on public.pain_map_results (anon_id);

alter table public.pain_map_results enable row level security;
```

**실행 불필요.** 위에 적은 대로 2026-07-27에 이미 적용됐다.

---

## 작업 2. API 라우트 신설

**새 파일:** `src/app/api/pain-map/route.ts`

`src/app/api/self-check/route.ts`와 **완전히 같은 패턴**으로 만든다.
특히 **실패해도 200을 반환하고 조용히 넘어가는 부분**을 그대로 지킨다
(저장 실패가 사용자 화면을 망치면 안 됨).

```ts
import { NextRequest, NextResponse } from "next/server";
import { supabaseAdmin } from "@/lib/supabaseAdmin";

export async function POST(req: NextRequest) {
  try {
    const body = await req.json();
    const {
      anon_id,
      areas,
      area_count,
      answers_raw,
      findings,
      priority1_count,
      entry_area,
    } = body;

    await supabaseAdmin.from("pain_map_results").insert({
      anon_id: anon_id || null,
      areas: Array.isArray(areas) ? areas : null,
      area_count: area_count ?? null,
      answers_raw: answers_raw ?? null,
      findings: Array.isArray(findings) ? findings : null,
      priority1_count: priority1_count ?? null,
      entry_area: entry_area || null,
    });

    return NextResponse.json({ ok: true });
  } catch (err) {
    // 저장 실패는 조용히 처리 (사용자 경험 방해 금지)
    console.error("[pain-map]", err);
    return NextResponse.json({ ok: false }, { status: 200 });
  }
}
```

---

## 작업 3. PainMap에서 호출

**파일:** `src/app/check/pain/PainMap.tsx`

### 3-1. 호출 위치 — `handleDone`이 아니라 `ResultView` 안이다

**주의.** 434행 `track("pain_map_complete", ...)` 옆에 붙이면 안 된다.
`findings`는 그 시점에 존재하지 않는다 — **`ResultView` 컴포넌트 내부(236행)에서 계산된다.**

따라서 저장은 **`ResultView` 안의 `useEffect`** 에서 한다.
`findings`, `highPriority` 계산 바로 아래에 넣는다.

```tsx
// ResultView 내부, findings/highPriority 계산 직후
const savedRef = useRef(false);

useEffect(() => {
  if (savedRef.current) return;   // 중복 insert 방지
  savedRef.current = true;

  fetch("/api/pain-map", {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({
      anon_id: getOrCreateVisitorId(),
      areas: selected,
      area_count: selected.length,
      answers_raw: answers,
      findings: findings.map((f) => f.title),
      priority1_count: highPriority.length,
      entry_area: entryArea ?? null,
    }),
  }).catch(() => {});
  // eslint-disable-next-line react-hooks/exhaustive-deps
}, []);
```

`useRef`를 상단 import에 추가한다: `import { useState, useEffect, useRef } from "react";`

### 3-2. `anon_id` — 이미 있는 함수를 쓴다

**새로 만들지 말 것.** `src/lib/visitor.ts`에 `getOrCreateVisitorId()`가 이미 있고,
셀프체크(`src/app/check/CheckQuiz.tsx` 97행)와 `KakaoButton`이 이 함수를 쓰고 있다.

`PainMap.tsx` 상단에 추가:
```tsx
import { getOrCreateVisitorId } from "@/lib/visitor";
```

### 3-3. `entry_area` — prop으로 내려준다

`ResultView`는 메인 컴포넌트의 자식이므로, `?area=` 값을 **prop으로 전달**해야 한다.

1. 메인 컴포넌트(`PainMap`)에 `const [entryArea, setEntryArea] = useState<string | null>(null);` 추가
2. `?area=` 파싱하는 `useEffect`에서 `setEntryArea(p)` 같이 호출
3. 결과 화면 렌더 부분(약 519행)에 prop 추가:
   ```tsx
   <ResultView
     selected={selectedOrdered}
     answers={answers}
     entryArea={entryArea}
     onReset={reset}
   />
   ```
4. `ResultView`의 props 타입에 `entryArea?: string | null` 추가

`?area=` 파싱은 `작업지시서-블로그글하단-유틸블록-0727.md`의 **작업 3**에서 추가된다.
**그 지시서를 아직 안 했으면** `entryArea`는 항상 `null`이 되게 두고 필드만 준비해둔다.
(나중에 그 지시서를 실행하면 자동으로 채워진다)

→ 이 값으로 "블로그에서 넘어온 사람 vs 직접 들어온 사람"의 완주율 차이를 볼 수 있다.

---

## 작업 4. "AI 분석 결과" 문구 수정

**파일:** `src/app/check/pain/PainMap.tsx` (약 366행, 예약 CTA 블록)

현재:
```tsx
<p className="text-white font-bold text-lg mb-1">
  AI 분석 결과, 정확한 평가는<br />움직임 검사까지 해야 합니다.
</p>
```

**문제:** 통증지도는 AI가 아니라 고정 규칙(`matchFn`) 매칭이다. 과장이고,
진짜 AI인 미소코치와 혼동된다.

변경:
```tsx
<p className="text-white font-bold text-lg mb-1">
  <span className="inline-block">체크 결과만으로는 여기까지예요.</span>{" "}
  <span className="inline-block">정확한 건 움직임을 봐야 합니다.</span>
</p>
```

---

## 하면 안 되는 것

- 이름·연락처 필드 추가 ❌
- 저장 실패 시 사용자에게 에러 표시 ❌ (조용히 넘어간다)
- 결과 로직(`matchFn`)·문항 변경 ❌ — **이번 작업은 저장만 켠다.**
  로직 개편은 2단계 통증지도 리뉴얼에서 한다. 지금 바꾸면 저장 데이터에 기준이 두 개 섞인다.

---

## 검증

1. `npm run build` — 타입 에러 0
2. ~~테이블 생성 확인~~ — 완료됨 (컬럼 9개 / RLS on / 정책 0개 검증 끝)
3. `npm run dev` → `/check/pain` → 무릎 선택 → 체크 3개 → 결과 화면
4. Supabase 대시보드에서 `pain_map_results` 행이 **1건만** 들어갔는지 확인
   (중복 방지 동작 확인 — 새로고침·뒤로가기 후 재확인)
5. `answers_raw`, `findings`, `areas`가 비어 있지 않은지 확인
   → **빈 값이면 JSON 구조 문제를 의심할 것** (바디닷 measurements 때와 같은 유형의 버그)
6. 저장 API를 일부러 실패시켰을 때(예: 테이블명 오타) 결과 화면이 정상 표시되는지

---

## 관장님이 할 일

**없습니다.** SQL은 2026-07-27에 코워크가 Supabase에서 직접 실행·검증까지 끝냈습니다.
로컬 Claude Code가 코드 작업만 하면 됩니다.
