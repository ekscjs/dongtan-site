# [내몸에미소 센터] 작업지시서 — 미소AI → 미소코치 이름 변경 + 말투 개선 + 상담 CTA 상시화

작성일: 2026-07-27
실행 순서: **1단계 5개 지시서 중 가장 먼저 실행한다.** (다른 지시서들이 "미소코치" 이름을 전제로 함)

---

## 배경 / 목적

- "AI"라는 이름은 차별성이 없고, 주 고객층(4050 여성·시니어)에게 범용 챗봇을 연상시켜
  "우리 센터가 답해준다"는 느낌이 나지 않는다.
- "미소쌤"은 센터 실제 트레이너 호칭과 겹치고 사람으로 오해할 소지가 있어 제외.
- **"미소코치"** 로 확정. 단 정체는 감추지 않는다 — 화면에 "AI가 24시간 답해드려요"를 병기한다.
- 이름만 바꾸면 톤이 안 따라와서 어색하므로 시스템 프롬프트 말투 지침을 같이 넣는다.
- 상담 CTA가 지금은 AI 판단(`show_consult`)에 따라 나올 때만 나온다 → 응급 아닌 경우 항상 노출로 바꾼다.

**URL `/ask`는 변경하지 않는다.** (SEO·기존 링크 보존)

---

## 작업 1. 시스템 프롬프트 수정

**파일:** `src/app/api/ask/route.ts`

### 1-1. 첫 줄 이름 교체 (25행 부근)

변경 전:
```
너는 내몸에미소(동탄) 센터의 AI, 이름은 **미소AI**다.
```

변경 후:
```
너는 내몸에미소(동탄) 센터의 AI 코치, 이름은 **미소코치**다.
```

### 1-2. 말투 지침 블록 추가

`SYSTEM_PROMPT` 안에서 **"우리 콘텐츠(posts·방 목록)에 없는 내용을 지어내지 않는다."** 바로 위에
아래 블록을 통째로 삽입한다.

```
**말투 지침 — 이름이 '코치'인 만큼 톤도 맞춘다:**
- 첫 문장은 반드시 공감으로 연다. 원인 분석부터 시작하지 않는다.
  (예: "앉을 때마다 아프면 하루 종일 신경 쓰이시죠.")
- 한 문장은 40자 안쪽으로 짧게 끊는다. 쉼표로 길게 잇지 않는다.
- 전문 용어는 쓰되 바로 뒤에 괄호로 쉬운 말을 붙인다.
  (예: "흉추(등 가운데 뼈)가 굳으면")
- "~하셔야 합니다" 대신 "~해보시면 좋아요"처럼 권하는 말로 쓴다.
- 이모지는 쓰지 않는다.
- answer 전체는 4~6문장. 길게 쓰지 않는다.
```

### 1-3. 재생성 요청 문구의 표현 유지

`callClaude(query, posts, "이전 답변에 특정 질환명 단정 표현이…")` 부분은 그대로 둔다.
(이름과 무관)

---

## 작업 2. 화면 문구 교체

아래 위치의 "미소AI"를 전부 "미소코치"로 바꾼다.

| 파일 | 행(대략) | 현재 | 변경 후 |
|---|---|---|---|
| `src/app/ask/page.tsx` | 5 | `title: "미소AI에게 물어보기 — 증상 안내 \| 내몸에미소 동탄"` | `title: "미소코치에게 물어보기 — 증상 안내 \| 내몸에미소 동탄"` |
| `src/app/ask/page.tsx` | 9 | openGraph title 내 `미소AI` | `미소코치` |
| `src/app/ask/AskConcierge.tsx` | 91 | `>미소AI</p>` | `>미소코치</p>` |
| `src/app/check/CheckQuiz.tsx` | 305 | `>미소AI</p>` | `>미소코치</p>` |
| `src/app/check/CheckQuiz.tsx` | 306 | `미소AI에게 물어보기` | `미소코치에게 물어보기` |
| `src/app/check/CheckQuiz.tsx` | 816 | `미소AI에게도 물어볼까요?` | `미소코치에게도 물어볼까요?` |
| `src/app/admin/analytics/page.tsx` | 742 | `미소AI 사용 현황` | `미소코치 사용 현황` |

`src/app/api/ask/route.ts`의 description 문구에도 "미소AI"가 없는지 최종 grep으로 확인한다.

**검증용 명령:**
```powershell
cd C:\Users\송경천\Documents\Claude\Projects\naemiso-brand\dongtan-site
findstr /s /n "미소AI" src\*.tsx src\*.ts
```
→ 결과가 0건이어야 한다.

---

## 작업 3. `/ask` 진입 화면에 정체 표기 추가

**파일:** `src/app/ask/AskConcierge.tsx` (91행 부근)

변경 전:
```tsx
<p className="text-sm font-semibold text-[#9B4DAB] uppercase tracking-widest mb-3">미소AI</p>
```

변경 후:
```tsx
<div className="mb-3">
  <p className="text-sm font-semibold text-[#9B4DAB] tracking-widest">미소코치</p>
  <p className="text-xs text-gray-400 mt-0.5">AI가 24시간 답해드려요</p>
</div>
```

> `uppercase` 클래스는 한글엔 효과가 없으므로 제거한다.

---

## 작업 4. 상담 CTA 상시 노출

**파일:** `src/app/ask/AskConcierge.tsx` (약 210행, `{result.show_consult && (` 블록)

### 4-1. 조건 제거

변경 전:
```tsx
{result.show_consult && (
  <div className="mt-5 bg-gradient-to-br from-[#7B2D8B] to-[#9B4DAB] rounded-2xl p-6 text-center text-white">
```

변경 후 — 조건을 없애고 항상 렌더한다(응급 분기는 이미 위쪽에서 갈라지므로 여기 도달하지 않음):
```tsx
<div className="mt-5 bg-gradient-to-br from-[#7B2D8B] to-[#9B4DAB] rounded-2xl p-6 text-center text-white">
```
그리고 이 블록을 닫던 `)}`를 `</div>`에 맞게 정리한다.

### 4-2. 문구 교체 + 클릭 트래킹 추가

```tsx
<p className="font-bold mb-1">정확한 건 상담에서</p>
<p className="text-purple-200 text-sm md:text-base lg:text-lg mb-4">
  <span className="inline-block">움직임을 직접 확인하고</span>{" "}
  <span className="inline-block">맞춤 방향을 안내해 드려요</span>
</p>
<KakaoButton
  onClick={() => track("ai_ask_consult_click")}
  className="block w-full bg-white text-[#7B2D8B] font-bold py-3.5 rounded-full hover:bg-gray-100 transition-colors"
>
  카카오로 무료 상담
</KakaoButton>
```

- 이모지(💬) 제거 — 말투 지침과 톤을 맞춘다.
- `KakaoButton`이 `onClick` prop을 받지 않으면 `src/components/KakaoButton.tsx`에
  `onClick?: () => void`를 추가하고 버튼에 연결한다.
- 위 관련 칼럼 헤더의 `📖` 이모지도 같이 제거한다.

### 4-3. `show_consult` 필드는 남겨둔다

API 응답 스키마·타입에서 `show_consult`를 지우지 말 것. (나중에 다시 쓸 수 있고,
프롬프트 JSON 스키마와 어긋나면 파싱이 깨진다.)

---

## 한글 줄바꿈 규칙 (필수)

이번 작업으로 새로 넣거나 수정하는 모든 한글 문구는
**의미 단위가 갈라지지 않게** 처리한다. `word-break: keep-all`만으로는 부족하다.

- 두 덩어리 이상인 문장은 각 덩어리를 `<span className="inline-block">`으로 감싼다
- `·`로 나열되는 항목은 flex 항목으로 쪼갠다

규칙 원문: `dongtan-site/CLAUDE.md`

---

## 검증

1. `npm run build` — 타입 에러 0
2. `npm run dev` 후 `http://localhost:3000/ask` 접속
   - 상단에 "미소코치 / AI가 24시간 답해드려요" 표시되는지
   - "앉으면 허리가 아파요" 입력 → 답변 첫 문장이 공감으로 시작하는지
   - 답변 문장들이 짧게 끊겨 있는지, 이모지가 없는지
   - **상담 CTA가 항상 나오는지** (여러 증상으로 3회 이상 테스트)
3. `findstr /s /n "미소AI" src\*.tsx src\*.ts` → 0건
4. `/check` 셀프체크 완주 → 결과 하단 크로스 프로모에 "미소코치" 표기 확인

---

## 관장님이 할 일

없음. 로컬 Claude Code가 끝내고 커밋·푸시까지 하면 됨.
배포 후 `www.bodymiso.com/ask`에서 답변 톤만 한 번 봐주시면 됩니다.
