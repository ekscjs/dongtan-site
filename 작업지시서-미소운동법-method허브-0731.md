# 미소 운동법 `/method` 허브 — 빌드 스펙

작성일: 2026-07-31 (코워크 기획, 구현은 로컬 Claude Code 담당)
기준 원고: `naemiso-brand/동작원리정의서/04_운동정의-페이지원고.md` **v2.1 확정본**
관련 문서: `작업지시서-움직임설계도-아카이브-0729.md` (일부 대체 — 아래 0장 참조)

> **착수 조건 없음. 지금 바로 시작한다.**
> 센터장 판정 6개가 2026-07-31 전부 완료되어 원고가 확정됐다.
> 감별 카드 내용은 아직 안 나왔지만, 카드는 **빈 슬롯**으로 두고 페이지를 먼저 띄운다.

---

## 0. 이 문서와 0729 지시서의 관계 ★ 먼저 읽을 것

`작업지시서-움직임설계도-아카이브-0729.md`는 폐기가 아니라 **부분 개정**이다.
아래 표대로만 처리한다. 헷갈리면 이 문서가 우선한다.

| 0729 지시서 | 상태 |
|---|---|
| 1장 목적 | **유효** |
| 2장 공개/비공개 경계 | **유효 (그대로 승계)** |
| 3장 용어 금지어 | **유효.** 단 페이지 명칭만 변경 (아래) |
| **4장 페이지 구조** | **폐기 → 이 문서 2장으로 대체** |
| **4-3 진입 동선** | **폐기 → 이 문서 3장으로 대체** (기존: "전역 메뉴에 추가하지 않는다" → **추가한다**로 뒤집힘) |
| 4-4 SSR·SEO | **유효 (그대로 승계)** |
| 5장 적재 스크립트 | **유효하나 이번 작업 범위 아님.** 카드 데이터가 나온 뒤 별도 진행 |
| 6장 바디닷 3.6단계 | **유효하나 이번 작업 범위 아님** |
| 7장 검증 체크리스트 | **유효 (이 문서 6장에 통합)** |

**명칭 변경 (전역):**

- ~~RMS / Rehab Movement System~~ → 폐기. 국내 재활교육 **MRS**(Movement Reeducation System)와 업계·단어 구성이 충돌한다.
- ~~"움직임 설계도"~~ → 페이지 명칭으로 쓰지 않는다.
- **확정 명칭: 「미소 운동법」.** 화면 문구·nav 라벨·메타 타이틀 전부 이걸로 통일한다.
- URL은 `/method` 그대로 유지 (영문 슬러그, 변경 없음).

---

## 1. 이번 작업의 범위 — 네 덩어리

| # | 작업 | 파일 |
|---|---|---|
| A | `/method` 허브 신설 | `src/app/method/page.tsx` (신규) |
| B | 헤더 nav 교체 | `src/components/Header.tsx` |
| C | `/programs` 최상단 두 갈래 분기 | `src/app/programs/page.tsx` |
| D | `/research-notes` 를 `/method` 하위에서 링크 | `src/app/method/page.tsx` |

네 개는 한 커밋으로 묶어도 된다. 서로 의존한다(B에서 바디 리셋 세션을 내리면 C가 그걸 받아야 하므로 **B와 C는 반드시 같이 나간다**).

---

## 2. 작업 A — `/method` 허브 신설

`src/app/method/page.tsx` 신규 생성. **서버 컴포넌트**로 만든다(`"use client"` 금지).

원고 `04_운동정의-페이지원고.md` v2.1의 ①~⑩을 **순서 그대로** 옮긴다.
문안을 새로 쓰지 않는다. 원고에 있는 문장이 최종 문장이다.

### 2-1. 섹션 순서 (원고와 1:1)

```
① 히어로        — 정의 한 문장 + "아픈 곳부터 만지지 않습니다"
② 같은 점수, 다른 몸  ★ 히어로 바로 아래. 이 페이지의 엔진
③ 우리가 깨우는 자리는 여섯 개뿐입니다
④ 해봤습니다. 그리고 안 됐습니다   (항목 2개 — 신전 / 아픈쪽)
⑤ 한 회차의 목표는 무게가 아닙니다
⑥ 아직 저희도 못 푼 것
⑦ 순서  [측정] → [감별] → [토대] → [적재]
⑧ 우리가 하지 않는 것
⑨ 부위별 기록      ← 이번엔 빈 슬롯 (2-2 참조)
⑩ CTA
```

**②의 잠금 문장 "무엇으로 갈랐는지 — 그 판정 기준은 공개하지 않습니다" 는 반드시 그 자리에 둔다.**
이 문장이 페이지의 후킹 장치다. 톤을 부드럽게 고치거나 위치를 내리지 않는다.

### 2-2. ⑨ 부위별 기록 — 이번엔 빈 슬롯으로

감별 카드 내용이 아직 확정되지 않았다. **DB 연동(`movement_blueprints`)은 이번 작업에서 하지 않는다.**

대신 이렇게 만든다.

- 카드 그리드 영역만 마크업으로 잡아둔다 (목·어깨 / 허리·골반 / 무릎·발목)
- 데이터는 **파일 상단 상수 배열**에서 읽는다. 지금은 빈 배열 `[]`
- 배열이 비면 섹션 전체를 렌더하지 않는다 (`{cards.length > 0 && ...}`)
- 나중에 카드가 나오면 **배열만 채우면 되도록** 한다. 컴포넌트 재작업이 없어야 한다
- 카드 1장 = `{ slug, region, signal, root, firstTarget, whyOrder }`

> 이 슬롯 설계가 이번 작업의 핵심이다. 여기를 대충 잡으면 다음 주에 페이지를 다시 뜯게 된다.

### 2-3. ⑨ 자리에 지금 넣을 것 — 연구노트 (작업 D)

카드가 비어 있는 동안 ⑨ 자리를 **연구노트 링크**가 채운다.

현재 `/research-notes`는 전역 메뉴에 없고 `/blog` 목록과 `/check` 결과 화면에서만 링크가 걸려 있다.
**사실상 링크 고아 상태다.** `/method`가 허브가 되면서 이걸 같이 주워야 한다.

- 섹션 제목: **"쌓고 있는 기록"**
- `/research-notes` 로 가는 카드 1장 (현재 4050 여성 리포트가 들어 있음)
- 문구 예: "회원 실측 데이터를 익명으로 모아 정리하고 있습니다"
- **표본 수를 부풀리지 않는다.** 쓸 거면 "회원 34명 전수 측정" 처럼 실제 숫자를 그대로 쓴다

### 2-4. ⑩ CTA

버튼 2개. `[ 첫 평가 문의하기 ]` `[ 내 몸 상태 1분 체크 ]` → 각각 카카오 상담 / `/check`

### 2-5. 메타데이터

```
title:       "미소 운동법 — 내몸에미소가 몸을 보는 순서 | 동탄"
description: 아픈 곳부터 만지지 않는 이유, 같은 점수가 정반대 처방으로 갈리는 지점까지.
canonical:   https://www.bodymiso.com/method
JSON-LD:     Article + about   (HowTo 금지 — 의료 절차로 오인될 스키마)
```

**"진단"·"치료"·구체 질환명을 문안·메타·URL에 쓰지 않는다.**
(`사이트-민감단어-순화리스트-0715.md` 유지)

---

## 3. 작업 B — 헤더 nav 교체

`src/components/Header.tsx` 상단 `navLinks` 배열.

**현재 (5개)**

```
/about            센터 소개
/programs         프로그램
/blog             칼럼
/check            몸 상태 체크
/class/breathing  바디 리셋 세션    ← 내린다
```

**변경 후 (5개 유지)**

```
/about            센터 소개
/method           미소 운동법        ← 신설. 2번째 자리
/programs         프로그램
/blog             칼럼
/check            몸 상태 체크
```

- **6개로 늘리지 않는다.** `max-w-5xl` 컨테이너에 로고 160px + `lg:text-lg` 링크 5개 + `gap-8` + 무료상담 버튼이 이미 꽉 차 있다. 6번째를 넣으면 데스크톱에서 줄바꿈이 난다
- 라벨은 **"미소 운동법"** (4글자). 기존 라벨과 길이가 비슷해 레이아웃이 안 깨진다
- `navLinks` 하나만 고치면 데스크톱·모바일 양쪽에 자동 반영된다 (같은 배열을 돌린다). 모바일 오버레이를 따로 손대지 않는다
- **드롭다운/하위메뉴를 만들지 않는다.** 데스크톱 hover와 모바일 아코디언이 갈리고, 클릭이 한 단계 늘어난다
- `isActive` 판정은 기존 `pathname.startsWith(link.href + "/")` 로직 그대로 — `/method/[slug]` 가 나중에 생겨도 동작한다

---

## 4. 작업 C — `/programs` 최상단 두 갈래 분기

nav에서 바디 리셋 세션을 내리는 대신, **프로그램 페이지 첫 화면에서 두 상품이 동시에 보이게** 한다.
(드롭다운은 열어야 보이지만 이건 안 열어도 보인다 — 노출은 오히려 이쪽이 강하다)

### 4-1. 지금 상태

`src/app/programs/page.tsx`

```
112줄  히어로
126줄  신뢰 지표
138줄  공식 인증
154줄  프로그램 빠른 이동   ← 통증완화 / 재활 / 체형교정 앵커
166줄  각 프로그램 섹션
214줄  비포에프터
235줄  가격
251줄  CTA
```

바디 리셋 세션은 **이 페이지에 아예 언급이 없다.** 별도 구멍이므로 이번에 같이 막는다.

### 4-2. 넣을 것

**154줄 "프로그램 빠른 이동" 자리를 두 갈래 분기 블록으로 교체한다.**

```
저희는 두 가지 방식으로 진행합니다

┌────────────────────┐  ┌────────────────────┐
│ 1:1 정규 세션        │  │ 바디 리셋 세션       │
│ 개인 맞춤            │  │ 4주 과정 · 주 1회    │
│ 통증완화 · 재활 · 체형 │  │                    │
│ → 아래에서 자세히     │  │ → 자세히 보기       │
└────────────────────┘  └────────────────────┘
     (페이지 내 앵커)         (/class/breathing)
```

- 좌측 카드 = 기존 3종(통증완화·재활·체형교정) 앵커를 그대로 품는다. 기존 앵커 이동 기능을 잃지 않는다
- 우측 카드 = `/class/breathing` 로 이동
- **가격을 쓰지 않는다.** 바디 리셋 세션은 페이지에 가격 표기 없이 "문의"로 가기로 확정돼 있다 (2026-07-20)
- 모바일은 세로 2단

### 4-3. 하지 말 것

- 기존 3종(통증완화·재활·체형교정)을 "정규 세션" 하위로 **구조 변경하지 않는다.** 카드로 감싸기만 한다
- 가격표(무료상담 / 1회체험 3만 / 8주 48만 / 10회권 60만)는 **손대지 않는다**

---

## 5. 공개 금지 — 0729 지시서 2장 승계

`/method` 페이지에 아래를 **한 글자도 넣지 않는다.**

| 항목 | 사유 |
|---|---|
| 판정 기준 수치선 (몇 점이면 어느 형인지) | 잠금이 페이지 엔진. 공개하면 무기가 사라진다 |
| 실제 동작명 · 세팅 · 회차 구성 · 중량 | 처방 비공개 원칙 |
| **턱 드는 큐잉** | 2026-07-29 비공개 확정 (논란 소지) |
| **전후 비교 · 개선율 수치** | 기존 확정 규칙. 초기 상태 스냅샷까지만 공개 가능 |
| 삭제된 기각 방법 2개 (마사지 관련) | 2026-07-31 판정 X. 되살리지 말 것 |

원고 v2.1 맨 아래 "🔒 이 페이지에 넣지 않는 것" 블록과 동일하다. 충돌하면 원고가 기준이다.

---

## 6. SSR · 한글 · 검증

### 6-1. 필수

- **서버 렌더링.** `useSearchParams`·클라이언트 state 목록 금지 (`작업지시서-블로그목록-SSR실패-useSearchParams제거-0726.md` 재발 방지)
- **한글 줄바꿈은 의미 단위.** `word-break: keep-all` 만으로는 못 막는다. 의미 단위를 `inline-block` span으로 묶고, `·` 나열은 flex 항목으로 쪼갠다 (`dongtan-site/CLAUDE.md` 규칙). 히어로 정의 문장과 ② 섹션이 길어서 여기가 제일 잘 깨진다
- 375px 모바일에서 육안 확인

### 6-2. 완료 체크리스트

- [ ] `curl https://www.bodymiso.com/method` **원본 HTML**에 ①~⑩ 본문이 전부 들어 있다
- [ ] 같은 원본 HTML에 `/research-notes` 링크가 있다
- [ ] `curl https://www.bodymiso.com/programs` 원본 HTML에 "바디 리셋 세션" + `/class/breathing` 링크가 있다
- [ ] 헤더 원본 HTML에 `/method` 링크가 있고, `/class/breathing` 링크는 없다
- [ ] 데스크톱 1280px에서 nav 5개가 한 줄에 들어간다 (줄바꿈 없음)
- [ ] "진단"·"치료"·질환명이 `/method` 문안·메타·URL에 없다
- [ ] 5장 금지 항목이 원본 HTML에 하나도 없다 (특히 턱 큐잉·개선율)
- [ ] 375px에서 한글 줄바꿈이 구 단위로 갈라지지 않는다
- [ ] `next build` 통과 + 타입체크 통과

> **검증은 `curl` 원본 HTML로 한다.** 브라우저 DOM·Playwright 렌더 결과로 통과 처리하지 않는다.
> JS 렌더 후를 보면 크롤러가 실제로 받는 것을 못 잡는다 (2026-07-26 커밋 `0d7d7ef`가 이걸로 반쪽 완료를 통과시킨 전례).
>
> **라이브 클릭 검증은 하지 않는다.** 관장 크롬 확장이 하이드레이션을 깨는 사례가 있다 (2026-07-27).
> 상호작용 확인이 필요하면 시크릿창 또는 Playwright.

---

## 7. 이 지시서 밖의 일

- **감별 카드 내용** — 2026-07-31 세션 녹음 → 추출 → 관장 판정 후 별도. 이번엔 빈 슬롯만
- **`movement_blueprints` DB 연동 / `upsert-blueprints.mjs`** — 카드 확정 후 0729 지시서 5장으로
- **`match-blueprint.mjs` 바디닷 3.6단계** — 0729 지시서 6장, 이번 범위 아님
- **사진·이미지** — 관장 담당 (로컬에서 admin 업로드)
- **git commit / push** — 로컬 Claude Code. 코워크는 이 지시서까지만

---

## 8. 왜 지금 하는가 (착수 근거)

감별 녹음을 기다렸다가 합치지 않는 이유.

1. **녹음 내용은 뼈대에 안 들어간다.** ①~⑩은 판정 완료로 확정됐고, 녹음은 ⑨ 카드 재료다. 카드는 허브 아래 얹는 콘텐츠라 페이지가 먼저 서 있어야 얹을 자리가 생긴다
2. **합치면 3~4일 멈춘다.** 녹음 → 전사 → 추출 → 판정이 다 끝나야 착수하게 된다
3. **색인 시계.** 신규 URL은 발견 후에도 크롤링까지 한참 걸린다(블로그 글 다수가 "발견됨–미크롤링" 상태). `/method`는 앞으로 자산 콘텐츠 전부가 매달릴 허브라 이 시계를 하루라도 빨리 시작시키는 게 이득이다
4. **병렬로 굴러간다.** 관장=녹음 / 코워크=지시서·매핑표 / 로컬=구현
