# 작업지시서 — 애널리틱스 "카카오 상담 버튼 클릭" 섹션 개편 (2026-07-26)

## 배경 / 문제

`/admin/analytics` 의 카카오 상담 버튼 클릭 섹션이 **최근 30일 6클릭**인데 일별 꺾은선(LineChart)으로 그려져 있음.
API가 **클릭이 있는 날짜만** 내려주기 때문에(0인 날은 배열에 아예 없음) 6개 점이 전부 count=1 →
`max=1` → 값이 전부 상단에 붙어서 **완전히 평평한 선**이 나옴. 정보량이 0.

즉 두 가지 문제가 겹쳐 있음:
1. **제로필 누락** — 클릭 0인 날짜가 데이터에 없어서 추이가 아니라 "클릭 있었던 날 목록"이 선으로 그려짐
2. **집계 단위 과다 세분화** — 월 6건 규모에서 일별은 신호가 안 생김

## 결론 (확정)

- 일별 꺾은선 → **주별 막대(최근 8주)** 로 교체. 월별은 지금 막대 1~2개라 무의미하므로 채택하지 않음.
- 이 규모에서 추이보다 유용한 건 **개별 클릭 내역**(언제 / 어느 페이지에서 / 어디로 들어온 사람이 눌렀나)이므로
  주별 막대 아래에 **최근 클릭 내역 테이블**을 추가한다. `kakao_clicks`에 `page` · `source` · `created_at` 컬럼이 이미 있어 추가 마이그레이션 불필요.
- 상단 숫자 카드 2개(클릭 수 / 페이지뷰 대비 클릭률)는 **그대로 유지**(30일 기준 유지 — 분모인 페이지뷰가 30일이라 기간 일치 필요).

---

## 1. API 수정 — `src/app/api/admin/analytics/route.ts`

### 1-1. 조회 기간 확장

기존 카카오 쿼리는 `monthStart`(30일) 기준. 8주 = 56일이 필요하므로 별도 시작점을 추가한다.

```ts
// 기존: const monthStart = kstMidnightUtc(30);
const eightWeeksStart = kstMidnightUtc(55); // 오늘 포함 8주 커버
```

카카오 쿼리를 이 범위로 바꾼다(기존 `kakaoRows` 쿼리 교체). `page`, `source`도 같이 select:

```ts
const kakaoRows = await fetchAllRows<{ created_at: string; page: string | null; source: string | null }>(() =>
  supabaseAdmin
    .from("kakao_clicks")
    .select("created_at, page, source")
    .gte("created_at", fmt(eightWeeksStart))
    .order("created_at", { ascending: false })
);
```

### 1-2. `kakaoTotal` 은 30일 기준 유지

쿼리 범위가 56일로 넓어졌으므로, 총합은 **30일 것만** 따로 세야 한다(클릭률 분모와 기간 일치).

```ts
const monthStartStr = kstDateStr(monthStart);
const kakaoTotal = kakaoRows.filter(
  (r) => kstDateStr(new Date(r.created_at)) >= monthStartStr
).length;
```

### 1-3. 주별 집계 (제로필 필수)

- 주 경계 = **KST 월요일 시작 ~ 일요일 끝**
- 최근 8주를 **클릭 0인 주까지 전부 포함**해서 오래된 주 → 최신 주 순서로 생성
- 라벨은 `M/D~M/D` 형식 (예: `7/20~7/26`)

```ts
// KST 기준 해당 날짜가 속한 주의 월요일 날짜 문자열
const kstMondayStr = (d: Date) => {
  const shifted = new Date(d.getTime() + KST_OFFSET_MS);
  const dow = shifted.getUTCDay();            // 0=일 … 6=토
  const diff = dow === 0 ? 6 : dow - 1;       // 월요일까지 되감을 일수
  shifted.setUTCDate(shifted.getUTCDate() - diff);
  return shifted.toISOString().slice(0, 10);
};

// 최근 8주치 월요일 목록(오래된 주 → 최신 주)
const weekKeys: string[] = [];
for (let i = 7; i >= 0; i--) {
  weekKeys.push(kstMondayStr(kstMidnightUtc(i * 7)));
}

const kakaoWeekCounts: Record<string, number> = {};
for (const key of weekKeys) kakaoWeekCounts[key] = 0;   // ← 제로필
for (const row of kakaoRows) {
  const key = kstMondayStr(new Date(row.created_at));
  if (key in kakaoWeekCounts) kakaoWeekCounts[key] += 1;
}

const kakaoWeekly = weekKeys.map((key) => {
  const mon = new Date(key + "T00:00:00Z");
  const sun = new Date(mon.getTime() + 6 * 86400000);
  const label = (d: Date) => (d.getUTCMonth() + 1) + "/" + d.getUTCDate();
  return { label: label(mon) + "~" + label(sun), count: kakaoWeekCounts[key] };
});
```

### 1-4. 최근 클릭 내역 (최대 10건)

```ts
const kakaoRecent = kakaoRows.slice(0, 10).map((r) => ({
  datetime: new Date(new Date(r.created_at).getTime() + KST_OFFSET_MS)
    .toISOString().slice(0, 16).replace("T", " "),   // "2026-07-23 14:05" (KST)
  page: r.page ?? "unknown",
  source: r.source ?? "direct",
}));
```

### 1-5. 응답 필드

`kakaoChart` 는 **제거**하고 아래로 교체(다른 곳에서 쓰이지 않는지 grep으로 확인 후 삭제):

```ts
kakaoWeekly,   // { label, count }[]  — 8주, 0 포함
kakaoRecent,   // { datetime, page, source }[]
kakaoTotal,    // 30일 총 클릭 수 (기존 의미 유지)
```

---

## 2. 프론트 수정 — `src/app/admin/analytics/page.tsx`

### 2-1. 타입

`AnalyticsData` 에서 `kakaoChart: DailyPoint[]` 제거하고 추가:

```ts
kakaoWeekly: { label: string; count: number }[];
kakaoRecent: { datetime: string; page: string; source: string }[];
```

### 2-2. 섹션 교체 (현재 541~566행 부근)

- 헤더 부제: `(최근 30일)` → `(주별 · 최근 8주)`
- 빈 상태 판정: `kakaoChart.length === 0` → `kakaoTotal === 0 && kakaoRecent.length === 0`
- `<LineChart data={kakaoChart} mode="month" />` → `<BarChart data={kakaoWeekly} />`
  (기존 `BarChart`(108행) 그대로 재사용. label 칼럼 `w-28`이라 `7/20~7/26` 들어감)
- 숫자 카드 2개는 그대로 두되, 라벨을 `클릭 수` → `클릭 수 (30일)`, `페이지뷰 대비 클릭률` → `클릭률 (30일)` 로 수정해 기간 혼동 제거
- 막대 아래에 클릭 내역 테이블 추가:

```tsx
{kakaoRecent.length > 0 && (
  <div className="mt-5">
    <p className="text-xs font-semibold text-gray-400 mb-2">최근 클릭 내역</p>
    <div className="overflow-x-auto">
      <table className="w-full text-xs">
        <thead>
          <tr className="text-gray-400 border-b border-gray-100">
            <th className="text-left py-2 font-medium">시각</th>
            <th className="text-left py-2 font-medium">누른 페이지</th>
            <th className="text-left py-2 font-medium">유입경로</th>
          </tr>
        </thead>
        <tbody>
          {kakaoRecent.map((r, i) => (
            <tr key={i} className="border-b border-gray-50">
              <td className="py-2 text-gray-600 whitespace-nowrap">{r.datetime}</td>
              <td className="py-2 text-gray-700">{pageLabel(r.page, data?.slugToTitle)}</td>
              <td className="py-2 text-gray-500">{SOURCE_LABELS[r.source] ?? r.source}</td>
            </tr>
          ))}
        </tbody>
      </table>
    </div>
  </div>
)}
```

기존 `pageLabel()`(90행) · `SOURCE_LABELS`(58행) 재사용.

---

## 3. 검증 항목 (전부 통과해야 완료)

1. `npx tsc --noEmit` 통과
2. 로컬 dev(`npm run dev`)에서 `/admin/analytics` 접속 — **라이브 사이트로 확인하지 말 것**(애널리틱스에 방문자로 집계됨)
3. 주별 막대가 **8개** 나오는지 확인 — 클릭 0인 주도 라벨과 함께 표시되어야 함(막대는 최소폭 3%로 보임)
4. 주별 막대 count 합계 ≥ `kakaoTotal`(56일 ≥ 30일) 인지 확인. 반대면 집계 범위가 어긋난 것
5. 최근 클릭 내역 시각이 **KST**로 나오는지 확인(UTC로 9시간 밀려 나오면 1-4 변환 오류)
6. 클릭이 0건인 상태를 강제로 만들었을 때(쿼리 임시 필터) 빈 상태 문구가 정상 노출되는지
7. 커밋만 하고 push는 관장 승인 후

## 참고 — 손대지 말 것

- `kakaoTotal` / 클릭률의 30일 기준은 유지(분모 = 30일 페이지뷰)
- `src/app/api/track/route.ts` 의 `kakao_click` 저장 로직은 변경 없음
- `LineChart` 컴포넌트 자체는 다른 섹션(방문자·전환율)에서 계속 쓰므로 삭제하지 말 것
