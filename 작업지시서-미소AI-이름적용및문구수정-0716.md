# 작업지시서 — "미소AI" 이름 반영 + 차별화 문구 + 버그 수정 (2026-07-16)

> 배경: `/check` 페이지 3번째 카드 라벨을 "AI 컨시어지"에서 브랜드 이름 **"미소AI"**로 확정.
> 동시에 관장님 지적사항 2개 반영: ①"둘 다 무료" 문구가 카드 3개인데 안 고쳐진 버그,
> ②이 AI가 챗GPT 같은 범용 AI와 뭐가 다른지 설명이 안 되는 문제 — "동탄에서 실제 트레이닝한
> 회원 사례로 쓴 콘텐츠 중에서 답한다"는 차별화 문구를 눈에 보이는 곳에 명시.
> **주의:** "AI가 우리 데이터로 학습됐다"는 표현은 기술적으로 부정확함(모델을 학습시킨 게 아니라
> Claude가 우리가 쓴 콘텐츠 중에서 골라 답하는 방식) — 아래 문구들은 전부 "~로 쓴 콘텐츠/사례
> 중에서 찾아 답한다"는 정확한 표현으로만 작성돼 있으니 그대로 반영, 임의로 "학습" 같은 단어로
> 바꾸지 말 것.

## 1. `src/app/check/CheckQuiz.tsx`

- L203: `둘 다 무료 · 1~3분이면 완료됩니다` → `셋 다 무료 · 1~3분이면 완료됩니다`
- L258: `AI 컨시어지` → `미소AI`
- L259: `AI에게 물어보기` → `미소AI에게 물어보기`
- L261: `증상을 그대로 말하면 관련 글·셀프체크·운동을 찾아드려요` →
  `동탄에서 실제 트레이닝한 회원 사례로 쓴 글 중에서 답을 찾아드려요`

## 2. `src/app/ask/page.tsx` (메타데이터)

- L5 title: `"AI에게 물어보기 — 증상 안내 | 내몸에미소 동탄"` → `"미소AI에게 물어보기 — 증상 안내 | 내몸에미소 동탄"`
- L6 description: `"증상을 그대로 말하면 관련 칼럼·셀프체크·운동·프로그램을 찾아드립니다. 동탄 내몸에미소 무료 서비스."`
  → `"증상을 그대로 말하면 동탄에서 실제 트레이닝한 회원 사례로 쓴 콘텐츠 중에서 관련 칼럼·셀프체크·운동·프로그램을 찾아드립니다. 내몸에미소 무료 서비스."`
- L9 openGraph title: `"AI에게 물어보기 — 증상 안내 | 내몸에미소"` → `"미소AI에게 물어보기 — 증상 안내 | 내몸에미소"`
- L10 openGraph description: `"증상을 그대로 말하면 관련 칼럼·셀프체크·운동·프로그램을 찾아드립니다."`
  → `"증상을 그대로 말하면 동탄 실제 트레이닝 사례 기반 콘텐츠 중에서 관련 칼럼·셀프체크·운동·프로그램을 찾아드립니다."`

## 3. `src/app/ask/AskConcierge.tsx`

- L79: `AI에게 물어보기` (작은 라벨) → `미소AI`
- L83: `증상을 편하게 적어주시면 관련 글·셀프체크·운동을 찾아드려요` →
  `증상을 편하게 적어주시면, 동탄에서 실제 트레이닝한 회원 사례로 쓴 글 중에서 답을 찾아드려요`
- L80~81 큰 제목("어떤 증상이 궁금하세요?")은 그대로 둘 것 — 안 건드림.

## 4. `src/app/api/ask/route.ts` (시스템 프롬프트에 이름만 추가)

`SYSTEM_PROMPT` 맨 첫 문장만 아래처럼 바꾸고 **그 뒤 가드레일 내용은 전부 그대로 유지**(한 글자도
건드리지 말 것 — 이미 신중하게 확정된 안전 문구임):

- 기존: `너는 내몸에미소(동탄) 센터의 **움직임·운동·자세교정 전문 컨시어지**다. 근골격계 불균형,`
- 변경: `너는 내몸에미소(동탄) 센터의 AI, 이름은 **미소AI**다. **움직임·운동·자세교정 전문 컨시어지**
  역할을 한다. 근골격계 불균형,`

## 검증
1. `/check` 페이지 상단 문구가 "셋 다 무료 · 1~3분이면 완료됩니다"로 보이는지.
2. 세 번째 카드 라벨·제목이 "미소AI" / "미소AI에게 물어보기"로 바뀌었는지, 설명 문구에
   "동탄에서 실제 트레이닝한 회원 사례" 문구가 들어갔는지.
3. `/ask` 페이지 진입화면 상단 라벨이 "미소AI"로, 입력창 위 안내문에도 같은 차별화 문구가
   보이는지. 브라우저 탭 제목(title)도 "미소AI에게 물어보기..."로 바뀌었는지.
4. `/ask`에서 실제 질문 하나 넣어서 기존처럼 정상적으로 답이 나오는지(시스템 프롬프트 수정으로
   기존 가드레일 동작이 깨지지 않았는지 — 진단 확정·약물 언급 안 나오는지 재확인).
5. `npm run build` 통과.

## 커밋 / 푸시
```
git add -A
git commit -m "feat: 미소AI 이름 반영 + 실제 트레이닝 사례 기반 차별화 문구 추가 + 셋다무료 문구 수정"
git push
```
